# 公司内部流程审核平台——需求文档

> 文档状态：持续更新  
> 当前版本：2.23
> 最后更新：2026-08-26
> 当前阶段：正式后端设计与首版实现准备

## 1. 文档维护约定

- 本文档是本项目需求的统一记录。
- 后续新增、调整或删除需求时，应同步更新本文档。
- 已确认需求与待确认事项分开记录，避免将讨论建议误认为最终需求。
- 重要变更应追加到文末的“变更记录”。

## 2. 项目目标

建设一个部署在公司内网、供公司内部使用的流程审核网站。平台需要支持创建多个彼此独立的审核流程，并让管理员通过直观的可视化方式配置流程及其初始表单。

前端交互原型继续作为业务流程、权限和接口行为的演示基线；正式后端目标技术栈已切换为 .NET 10 / ASP.NET Core 10，后续按新架构实现数据库持久化、本地附件存储、邮件 Outbox 和服务端安全边界。仓库中已有的 NestJS 代码仅视为待替换的旧骨架，不能作为目标架构或完整生产后端。浏览器本地数据与 Mock API 仅用于前端过渡和自动化验证，不属于正式业务数据源。

## 3. 用户与角色

### 3.1 平台角色

- 流程管理员：创建、配置、发布和停用流程，配置流程表单与审批节点。
- 流程发起人：选择已发布流程，填写初始表单并发起流程。
- 审批人：处理分配给自己的审批任务，执行通过或驳回。

角色与权限用于控制用户可以进入哪些系统页面、使用哪些系统管理功能，不直接承担具体流程节点的人员分配。

- 一个用户可以同时关联多个角色，用户的系统页面权限和操作权限取所有有效角色权限的并集。
- 用户管理页需要展示用户的全部角色，并支持通过可搜索的多选控件增加或移除角色。
- 角色与用户为多对多关系，不能在用户表中使用单个角色字段保存。
- 非内置角色支持物理删除，但删除前必须执行服务端引用检查；仍有关联用户、流程权限组、任一流程版本可见范围或其他业务资格引用时禁止删除，并提示先解除可变引用或停用角色。超级管理员内置角色始终不可删除。
- 用户通过多个角色获得同一权限时只计算一次；移除其中一个角色后，如果仍通过其他角色拥有该权限，则权限继续有效。
- 权限管理中“任务中心”的动作权限统一为“查看、审核、关闭”。“审核”包含审批节点的通过、确认和驳回，不再设置独立的“驳回”动作权限；节点处理方式仍决定当前节点能否驳回。
- “关闭”是独立于审核和发起的系统动作权限。关闭流程必须同时满足“任务中心-关闭”动作权限和该实例锁定流程版本的关闭流程权限组资格；拥有审核权限、发起权限或属于审批/受理流程权限组均不自动获得关闭资格。
- 首版默认角色中，研发、质量和生产审核员拥有任务查看与审核权限；文控专员拥有任务查看、审核和关闭权限，以支持文控上传/确认节点及流程关闭。实际可处理或关闭的具体流程仍由对应流程权限组限制。

系统预计存在 200 名以上用户。用户相关页面和选择器需要按照可继续增长的用户规模设计，不以一次性展示全部用户为前提。

### 3.2 业务示例中的角色

- 发起方：填写表单并提交发起流程实例；普通用户中只有实际创建该实例的人可以修改发起内容、按需更换附件并在驳回后重新提交，超级管理员不受此限制。是否可以关闭流程由独立的关闭流程权限组决定。文档审核流程中通常由文控承担发起方职责。
- 研发：并行审核文档。
- 质量：并行审核文档。
- 生产：并行审核文档。

### 3.3 流程权限组

系统需要在用户、角色和权限之外提供独立的“流程权限组”概念，用于配置业务流程的发起、关闭和审批人员。

- 流程权限组是可跨流程复用的全局业务配置。
- 流程权限组可以加入任意用户，不要求组成员拥有同名系统角色。
- 流程权限组除了直接添加用户外，还可以关联一个或多个系统角色；拥有任一关联角色的全部有效用户自动成为该流程权限组的有效成员。
- 角色关联采用动态关系，不把角色用户一次性复制成直接成员。用户获得或失去角色、角色成员变化时，流程权限组的有效成员立即同步变化。
- 流程权限组的有效成员等于“直接添加的有效用户”与“所有关联角色中的有效用户”的并集，并按照用户标识去重。
- 同一用户同时通过直接添加、多个角色等方式进入流程权限组时，成员列表只显示一次，但需要标明“直接添加”和“来自角色”的全部来源。
- 移除流程权限组中的某个角色关联时，只移除仅通过该角色进入的成员；仍被直接添加或通过其他关联角色进入的用户继续保留资格。
- 首版不提供针对角色成员的单独排除名单；如某个角色中的个别用户不应获得该流程资格，应调整角色关联或使用更精确的角色。
- 停用账号在任何情况下都不属于有效成员。角色被停用时不能再被新的流程权限组关联；已有关联的成员资格是否失效按系统角色停用规则立即计算，不影响历史操作记录。
- 一个用户可以同时属于多个流程权限组。
- 流程权限组至少包含名称、后台自动生成的唯一编号、说明、状态和成员列表。
- 流程权限组列表的更新时间统一显示为用户本地时间文本 `YYYY-MM-DD HH:mm`，不显示秒；正式接口返回的 ISO 8601 时间不得直接显示给用户，带时区的时间必须先换算为浏览器本地时间。
- 流程定义、版本快照和运行时任务只保存流程权限组的稳定标识；发起中心、流程发起页、流程设计器、发布预览、版本详情和其他用户界面必须根据当前权限组主数据解析并显示权限组名称，不得直接展示内部 UUID、编码或关联键。历史异常数据无法解析时统一显示“未识别流程权限组”，不能回退暴露内部标识。
- 流程权限组本身不直接授予系统页面权限，其业务能力由它在流程中的绑定位置决定。
- 流程可以选择一个或多个流程权限组作为发起权限组；有效成员可以发起该流程。
- 审批节点可以选择一个“审批/受理”流程权限组作为节点执行组；自由流程可以选择一个或多个同用途的权限组作为受理人范围。
- 流程可以选择一个或多个流程权限组作为关闭权限组；有效成员可以关闭该流程的运行实例，关闭资格不由发起、审批或受理资格隐含获得。
- 同一个流程权限组可以被多个流程复用，也可以在不同流程中承担不同用途。
- 流程权限组的“允许用途”采用多选配置，首版选项统一为“发起、审批/受理、关闭”，至少选择一项；“审批/受理”在固定审批流程中用于审批节点，在自由流程中用于受理人范围。流程基本信息和流程设计器按照当前配置位置过滤可选权限组。
- “适用流程”不在流程权限组中手工勾选，而是根据各正式版本中的实际引用关系自动统计。一个权限组可以显示多个已引用流程；尚未被引用时显示“暂未关联流程”。
- 流程权限组编辑页只读展示已引用流程；引用关系只能通过流程基本信息或流程设计器建立和解除，避免两处配置不一致。
- 典型流程权限组包括：`PDF流程_文控权限组`、`PDF流程_研发权限组`、`PDF流程_生产权限组`、`PDF流程_质量权限组`。
- 流程权限组成员发生变化时，不需要修改用户的系统角色或权限，也不需要修改流程图。
- 审批记录需要保存实际执行操作的用户。
- 流程权限组人员变更立即影响运行中的权限：新增成员立即获得相应资格，被移除成员立即失去相应资格。
- 用户发起、关闭、查看或提交待办操作时，后端需要根据流程权限组当前有效成员再次校验资格。
- 用户角色变化和流程权限组角色关联变化也属于成员变化，必须立即影响运行中的发起、关闭、审核、受理、转交和异常改派资格。
- 移除角色或角色关联前，界面应提示将失去流程权限组资格的用户数量以及受影响的运行中待办数量；确认后仍立即生效。
- 流程权限组删除使用独立的“流程权限组-删除”权限；已被任一流程版本或节点配置引用的流程权限组不可删除，只能停用。
- 停用的流程权限组不能再被新流程配置引用，但不影响已经发布的流程和运行中的流程实例。

### 3.4 用户登录

- 正式后端为每个用户保存“登录方式”，可选“域登录”或“密码登录”；普通用户新建时默认选择“域登录”。登录页仍统一提交账号和密码，由后端根据该账号已保存的登录方式选择认证提供方，客户端不得提交或覆盖登录方式。
- “域登录”使用公司配置的 AD/LDAP 域服务校验账号密码。FlowPilot 中必须预先存在同登录账号且处于启用状态的用户；使用本次登录用户 UPN 直接 bind 后，搜索结果必须同时唯一匹配同一个规范化账号和该 UPN，不能把一个绑定身份映射为另一个搜索身份。域认证成功不自动创建用户，也不从域组自动授予角色、流程权限组或组织数据。域密码只在本次认证调用中使用，不写入数据库、会话、审计或技术日志。
- 域服务不可用、连接超时或配置错误时，域登录用户返回明确的服务不可用错误，不得自动降级为本地密码登录。已识别为“密码登录”的用户始终只校验本地密码散列，无论登录成功、密码错误或账号停用都不得调用 LDAP 或执行 LDAP 可用性探测；失败统一返回 `401 INVALID_CREDENTIALS`。未知账号可使用匿名 RootDSE 可用性探测统一域侧 401/503 响应。域登录属于账号密码校验，不包含 Windows 集成认证或免密单点登录。
- “密码登录”使用 FlowPilot 本地密码散列校验。只有密码登录用户保存 ASP.NET Core `PasswordHasher<TUser>` 生成的自描述、版本化密码散列；域登录用户的本地密码散列必须为空。实现必须使用 `PasswordVerificationResult.SuccessRehashNeeded` 支持参数升级后的渐进重哈希，不自行设计或固定底层 PBKDF2 字符串格式。
- 用户管理页面需要支持管理员创建账号、选择登录方式、启用或停用账号及为密码登录用户重置密码。用户列表和用户详情显示登录方式；新增及编辑表单使用“域登录 / 密码登录”单选或下拉控件，普通用户默认“域登录”。
- 非内置用户支持物理删除，但必须使用独立的“用户管理-删除”权限，并在删除前由服务端检查角色、流程权限组、流程版本可见人/通知收件人、流程实例、审批任务、附件及其他历史业务引用。只有完全无引用且不是当前登录账号的用户可以删除；存在可变配置引用时提示管理员先解除，存在历史业务记录时只能停用。删除成功同时使该用户全部会话失效。超级管理员账号始终不可删除。
- 新增用户和启用账号必须至少分配一个系统角色；已有用户停用后允许清空全部系统角色，以便管理员先解除角色引用再执行删除。清空角色不等于删除，最终删除仍由服务端完成全部引用检查。
- 新增域登录用户不填写初始密码；新增密码登录用户必须填写初始密码。编辑已有用户时不展示任何已有密码；从域登录改为密码登录时必须在同一次操作中设置新密码，从密码登录改为域登录时清除本地密码散列。登录方式发生变化后，该用户的全部现存会话立即失效。
- 密码重置只适用于密码登录用户；域登录用户不显示或禁用“重置密码”，其密码由域系统维护。账号状态仍只能通过用户列表中的启用/停用操作修改。
- 用户编号作为系统内部稳定标识由后端自动生成，不在用户管理列表、查询条件和编辑界面中展示；管理员通过登录账号和员工姓名识别、搜索用户。
- 每个用户需要维护可用于流程通知的邮箱地址；用户管理支持查看、新增和修改邮箱，并校验邮箱格式。邮件收件人配置保存稳定用户标识，发送时读取该用户最新的启用状态和邮箱地址。
- 本地密码只保存安全散列，不保存或展示明文密码。密码登录用户的密码最低长度为1个字符，不要求数字、大小写字母、特殊字符或其他复杂度组合。
- 连续登录失败不锁定账号；密码登录用户的本地凭据失败直接按“登录账号 + 来源 IP”和来源 IP 总量执行临时速率限制，域登录用户在域服务已确认可达后的凭据失败使用相同限制。域服务不可用响应使用独立的仅来源 IP 速率桶和短时共享负缓存，不计入账号密码失败桶；超过相应限制均返回 `429`，不得改变账号启停状态。
- 正式后端使用服务端不透明会话。浏览器 Cookie 只保存高强度随机令牌，数据库只保存令牌散列；会话闲置8小时失效，登录满24小时必须重新认证。
- 用户被停用或管理员重置密码后，该用户的全部现存会话立即失效。正式接口只供同源浏览器前端使用，不提供 API Key、服务账号或第三方集成认证。
- 登录成功后，浏览器只记住最近一次成功使用的登录账号；再次进入正式构建的登录页时自动回填账号。正式构建不保存或回填密码；Debug 登录页固定使用唯一超级管理员账号 `superadmin` 并预填演示密码 `1`，不在登录页提供其他演示身份，进入系统后再通过顶栏切换演示身份；登录失败不得覆盖上一次成功账号。

### 3.5 系统内置超级管理员

- 系统必须预置唯一的超级管理员账号及“超级管理员”内置角色；首版交互原型账号为 `superadmin`，演示密码为 `1`，Debug 登录页只提供该账号，其他演示身份仅在超级管理员登录后的顶栏切换中提供。正式后端不得使用演示密码，首次初始化时从服务器外部敏感配置文件读取初始密码并仅写入一次安全散列；数据库完成初始化后不再用该配置覆盖现有密码，部署人员可以删除初始密码配置项。
- 超级管理员的登录方式固定为“密码登录”，不允许配置或切换为域登录；用户管理只读展示该登录方式。超级管理员登录不依赖域服务，因此域服务不可用时仍可使用其本地密码登录和排查配置。
- 超级管理员始终拥有全部页面权限、系统动作权限和全部流程的数据查看权限，其授权不读取普通角色权限矩阵，不能被普通管理员减少或覆盖。
- 超级管理员默认可以创建、配置、发布、停用所有流程定义，并可以发起、审核、驳回、关闭、重新提交、复制新建以及处理自由协作流程。
- 超级管理员执行流程操作属于系统级越权授权，不要求加入发起、审批/受理或关闭流程权限组，也不要求成为自由流程受理人。
- 超级管理员账号和内置角色在用户管理、角色管理及权限管理中可以查看，但账号资料、状态、密码、角色成员和权限矩阵均不可通过页面或业务 API 修改，也不能停用或删除。服务器管理员可以在服务停止时通过专用离线命令重置超级管理员密码，不能直接编辑数据库散列。
- 超级管理员不加入任何流程权限组，不计入权限组有效成员数量，也不出现在发起时指定审批人、自由流程受理人、转交人员、异常改派人员等流程人员名单或候选人选择器中。
- 超级管理员实际执行操作时仍必须保存真实操作人，并显示在审批记录、协作时间线和操作审计日志中；“不显示在流程人员名单”不等于隐藏其已经发生的操作记录。
- 首次登录不强制修改密码。
- 连续登录失败不锁定账号。

#### 3.5.1 正式后端模拟身份

- 正式构建中只有“真实登录操作者”为系统内置超级管理员时才显示顶栏模拟身份框；普通管理员即使拥有全部普通权限也不能使用该能力。
- 模拟身份必须由后端会话建立，前端不得通过改写本地状态、用户标识请求头或访问令牌切换正式身份。会话同时保存真实操作者和当前生效用户。
- 模拟期间所有页面权限、动作权限、流程权限组资格和数据范围均按当前生效用户重新计算；真实超级管理员权限不得透传给模拟用户。
- 发起和处理流程时，业务记录中的发起人、处理人等业务参与者保存当前生效用户；不可变操作审计同时保存真实操作者、当前生效用户、模拟会话标识和原因。
- 发起模拟身份时必须选择一个启用的非内置目标用户，选择后直接切换、不弹出确认框；客户端随请求提交固定的切换来源说明供审计使用。后端将已经通过密码认证的真实超级管理员会话作为切换授权，跳过目标用户的域认证或本地密码认证，不要求也不接收目标用户密码，并直接返回已按目标用户生效的登录会话状态。禁止模拟超级管理员、停用用户和链式模拟；从一个目标切换到另一个目标时先结束原模拟会话，再建立新模拟会话。目标用户被停用时模拟会话立即失效并恢复真实身份或要求重新登录。
- 模拟身份的有效期不得超过原登录会话的剩余有效期；退出模拟、退出登录、会话过期或真实操作者会话失效时，模拟状态一并失效。
- 切换或退出模拟身份后，前端必须废弃上一身份的权限和业务查询缓存，并按新生效用户重新获取可见流程、实例、待办和目录数据，避免跨身份数据残留。
- `pnpm build:debug` 继续保留浏览器 Mock 的直接演示身份切换，真实演示身份为超级管理员时同样跳过目标身份的密码验证，选择后不弹出确认框并立即返回登录状态；演示身份列表与用户管理共用同一份用户数据，展示全部启用用户、排除停用用户，并固定将超级管理员放在第一项；选择框支持按账号、姓名和角色搜索。该行为供单浏览器原型操作，不属于正式认证机制，也不得出现在 `pnpm build` 产物中。

### 3.5 部门与职务

- 每个用户需要配置一个所属部门和一个职务；所属部门与职务均为必填项。
- 部门采用最多两级的树形结构，例如一级部门“研发”下可以包含二级部门“软件”“硬件”。
- 用户可以直接归属一级部门，也可以归属某个二级部门，不强制必须选择到最末级。
- 例如研发经理可以配置为“部门：研发，职务：经理”；软件工程师可以配置为“部门：研发 / 软件，职务：员工”。
- 用户记录引用的每一级部门都必须存在于部门架构中；演示数据中的生产人员统一关联“生产 / 一车间”或“生产 / 二车间”，不得只在用户列表中显示不存在的部门路径。
- 一个用户首版只归属一个部门节点；不支持同时归属多个部门。
- 职务作为全局组织字典维护，不从属于某一个具体部门；系统初始提供“经理”和“员工”，管理员可以根据公司实际岗位继续维护其他职务。
- 部门管理页使用“部门架构 / 职务管理”两个页签：部门架构维护最多两级的部门树，职务管理维护全局职务字典。
- “部门架构 / 职务管理”切换控件必须清晰区分当前选中项：选中项使用主题色文字和图标、浅主题色背景、可见边框与加粗字重，未选中项保持中性色，并提供悬停反馈。
- 部门管理的“部门架构 / 职务管理”与任务中心的“我的待办 / 可代办”使用同一套主切换控件规范，包括高度、圆角、边框、背景、主题色选中态、阴影、字重和悬停反馈；任务数量徽标属于任务中心的内容扩展，但不能改变页签主体的选中风格。
- 职务记录至少包含后台自动生成的稳定标识、职务名称、排序、状态、已关联用户数和说明；职务名称全局唯一。
- 职务支持新增、重命名、排序、启用、停用和删除。删除部门或职务统一使用独立的“部门管理-删除”权限；尚未被用户引用的职务可以删除，已有用户引用的职务不可删除，管理员需要先将相关用户调整到其他职务。
- 停用职务不能再分配给新用户；已有用户继续保留并显示该历史职务，编辑该用户时仍需展示当前停用值，并允许改为其他启用职务。
- 用户新增和编辑页面从职务字典动态读取可选项，只允许新选择启用职务，不在前端硬编码“经理/员工”。
- 部门、职务和角色相互独立：部门表示组织归属，职务表示组织身份，角色表示系统权限；不能根据“经理”职务自动授予管理权限。
- 部门记录至少包含后台自动生成的唯一编号、名称、上级部门、层级、排序、状态和说明。
- 一级部门可以没有二级部门；二级部门不能继续创建下级部门。
- 已有用户关联或仍有下级部门的部门不可删除，可以停用；停用部门不能再分配给新用户，原有用户仍显示历史部门并由管理员迁移到有效部门。
- 用户编辑页使用支持搜索的部门树选择器，一级和二级节点均可选择；列表和详情按“一级部门 / 二级部门”的完整路径展示。
- 用户列表支持按一级部门、二级部门、职务和角色组合筛选，200+ 用户场景使用服务端分页与索引。

## 4. 流程管理

### 4.1 多流程管理

- 系统可创建多个流程。
- 每个流程可以拥有不同的流程图、初始表单、发起流程权限组及审批节点流程权限组配置。
- 流程列表需要展示流程名称、状态、当前版本和最近更新时间等基本信息。
- 流程管理概览以流程定义为入口，不展示实例数量，也不直接提供版本编辑操作；每条流程默认通过“查看”进入版本记录，再根据具体版本的状态执行复制新建版本或编辑。版本实例数量继续在版本记录中展示，用于判断该版本是否允许编辑或删除。
- 流程定义和流程版本必须使用稳定的内部关联标识明确区分，不能根据名称、表单内容或编号前缀推断两次配置是否属于同一个流程。
- 点击全局“新建流程定义”始终创建新的流程定义；基本信息首次保存成功时立即生成正式编号版本 V1，不创建草稿或临时版本。即使从已有流程复制配置，也属于新的流程定义。
- 从任意版本执行“复制新建版本”时，在同一流程定义下创建下一个正式编号版本并继续使用原流程菜单。未发布且实例数为 0 的版本可以直接编辑；当前发布版本必须先取消发布，且实例数为 0，才能编辑同一版本。
- “新建流程定义”弹窗中填写的流程名称、流程类型和说明必须直接写入 V1；进入基本信息步骤后应显示同一份数据，不能退回“未命名流程”或示例流程数据。
- 全局“新建流程”创建的流程必须拥有独立的基本信息、初始表单、系统列表字段、流程拓扑和发布快照存储空间；除用户主动执行“复制新建”外，不得继承 PDF 审核或其他内置示例流程的字段、附件要求、审批节点、流程权限组及列表列定义。
- 流程类型在新建时确定，创建后不可手工切换；固定审批和自由协作分别进入各自固定的配置步骤。

### 4.2 流程设计方式

- 使用可视化画布配置流程。
- 支持将节点拖入画布。
- 支持在节点之间连线。
- 支持选择节点后编辑该节点的属性。
- 可视化流程设计器提供“自动布局”按钮，按当前连线关系将节点整理为从左到右的层级结构；同层并行节点纵向排列，并在布局完成后自动适应画布视口。
- 可视化流程设计器不提供“恢复示例”入口，避免用户误将当前版本覆盖为演示拓扑；需要调整时直接编辑当前节点与连线，或从流程版本记录基于指定完整版本复制新建版本。
- 表单设计采用统一单列布局。

### 4.3 首版节点类型

首版限定为以下节点：

1. 开始节点
2. 审批节点
3. 结束节点

独立条件分支、互斥网关、抄送节点、自动任务等暂不纳入首版范围；首版支持在审批节点上配置“执行条件”，条件不满足时跳过该审批节点，不改变连线本身的并行/串行语义。

首版不设置独立的“并行分支”节点，并行关系直接由连线拓扑表达：

- 同一个开始节点或审批节点连接到两个及以上审批节点时，系统自动识别为并行开始。
- 两条及以上路径连接到同一个后续节点时，系统自动识别为 AND 汇聚；所有前置路径审批通过、确认或因条件不满足而跳过后才继续流转。
- 任一并行路径驳回后，当前轮次立即结束，其他尚未完成的并行待办自动取消，不进入汇聚后的节点。
- 每个并行分叉的各条路径必须汇聚到同一个后续节点；流程发布校验需要检查分叉与汇聚拓扑是否合法。
- 条件分支或互斥分支未来通过独立的“条件分支”节点表达，不能与首版的多出线并行语义混用。

### 4.4 审批人配置

- 审批节点通过选择流程权限组确定候选处理人员。
- 例如“研发审核”节点可以选择 `PDF流程_研发权限组`。
- 属于该流程权限组的人员可以查看并执行对应节点允许的操作。
- 审批节点的“处理方式”可以配置为“审批”或“确认”。旧节点和未保存该字段的历史版本默认按“审批”处理。
- “审批”允许处理人通过或驳回；“确认”只允许确认，不提供驳回入口。通过、确认和驳回统一需要审核任务处理权限。
- 流程权限组内任意一名有效成员完成通过、确认或驳回后，该审核节点即完成。
- 节点完成后，其他流程权限组成员不再显示该待办，也不能重复提交操作。
- 多名成员同时提交时，只接受第一个成功完成节点的操作，其他提交应返回“任务已被处理”的明确结果。
- 流程定义保存流程权限组标识，不直接保存一组用户标识，避免人员变化时频繁修改并重新发布流程。
- 节点配置中可以选择是否在流程实际发起表单中，从所选流程权限组的成员中指定一名具体审批人。
- 当启用“发起时选择具体审批人”后，运行时表单应显示对应的人员选择字段，并只展示所选流程权限组的有效成员。
- 发起时指定的人员是该节点的默认责任人，不形成排他授权；同一流程权限组的其他有效成员仍保留临时代办权限。
- 流程实例创建后，如果当前轮尚未产生任何通过、确认或驳回结果，具有该实例发起内容编辑权限的人员可以在详情编辑态重新选择已开启“发起时选择具体审批人”的节点默认责任人。保存时需要校验人员仍属于节点当前有效流程权限组，并同步更新本轮对应的未激活或待处理任务；不创建新轮次，同组其他成员的代办资格不变。
- 当前轮任一节点产生通过、确认或驳回结果后，默认责任人与发起内容同时锁定，不再允许修改；驳回待处理与重新提交不自动开放默认责任人修改。
- 指定人员的任务默认只出现在该人员的“我的待办”中，流程权限组其他成员的默认待办列表不展示该任务。
- 流程权限组成员可以切换到“可代办”Tab，查看其所属流程权限组的全部未完成任务，包括已指定给其他成员的任务。
- 流程权限组成员从“可代办”进入任务后，可以直接执行该节点允许的通过或驳回操作，不要求先修改默认责任人。
- 未配置指定人员的任务，默认显示在该流程权限组所有有效成员的“我的待办”中。
- 代办操作完成后，审核记录需要同时保存默认责任人、实际处理人，并标识该操作是否属于代办。
- 代办人员执行审核时不需要填写额外的代办原因；通过或驳回仍遵循该节点原有的审批意见规则。
- 默认责任人与代办人员并发提交时，仍遵循节点第一个成功提交的操作生效、其余提交提示任务已处理的规则。
- 用户角色和系统权限仍负责控制用户能否进入审核功能；流程权限组负责控制用户能否处理具体业务节点。

### 4.5 发起与关闭权限

- 每个流程需要配置一个或多个发起流程权限组。
- 每个流程还需要独立配置一个或多个关闭流程权限组；同一个权限组可以同时被选为发起组和关闭组，但两项资格分别保存和校验。
- 流程基本信息编辑页使用可搜索的多选控件分别维护发起流程权限组、关闭流程权限组，以及自由协作流程的审批/受理流程权限组，不通过重复添加字段配置多个组。
- 自由协作流程的审批/受理流程权限组支持多选，首位受理人和后续流转候选人取所有所选组当前有效成员的并集并去重。
- 只有发起流程权限组的当前有效成员可以发起该流程。
- 只有同时拥有“任务中心-关闭”动作权限且属于关闭流程权限组的当前有效成员，才可以关闭该流程的任意流程实例；不要求是该实例的实际发起人、审批人或当前受理人。
- 关闭操作原则上可以在任意流程实例状态执行；已经关闭的实例不能重复关闭。驳回状态下是否允许关闭，由该流程版本的“驳回后的处理方式”决定。
- 关闭流程时弹出二次确认，后端再次校验当前用户是否仍属于该实例锁定版本配置的关闭流程权限组。
- 关闭成功后取消该实例所有未完成待办，实例进入不可恢复的“已关闭”终态。
- 系统保存关闭前状态、关闭人和关闭时间，并在流程时间线中展示。

### 4.6 流程定义状态

流程需要支持以下状态：

- 未发布：当前没有发布版本，不能发起新实例。
- 已发布：存在唯一发布版本，可被用户选择和发起。
- 已停用：不能再发起新实例，但应保留历史记录。

流程定义状态由发布版本指针和启停操作自动计算，不在基本信息页面提供手工状态下拉框：没有发布版本时为“未发布”，存在唯一发布版本且流程未停用时为“已发布”，管理员执行停用后为“已停用”。流程定义不再使用“草稿”状态。

- 流程定义保存稳定的菜单身份和路由标识，流程名称及菜单显示名称属于完整版本快照的一部分。
- 首次发布或切换发布版本时，流程定义的当前名称和左侧菜单显示名称必须在同一事务中同步为新发布版本保存的流程名称；菜单路由和稳定流程标识不随名称变化。
- 未发布流程不在流程发起中心和流程清单菜单中展示，即使存在历史实例也只允许从流程管理、实例监控或其他具备权限的运维入口查看；重新发布任一可用版本后，流程清单菜单恢复显示。

- 停用作用于整个流程定义，只禁止新发起实例和复制新建实例；不改变各版本状态，也不取消当前发布版本。
- 流程停用后，运行中实例继续审批、驳回、重新提交、关闭或进行自由协作流转，任务中心和流程清单中的已有数据继续保留。
- 停用流程仍可创建、编辑、发布或切换版本，但这些操作不得自动重新启用流程；管理员需要单独执行“启用”。
- 重新启用后，新实例使用当时的发布版本，编号按照该版本的前缀继续取号；没有发布版本时仍不能发起。

### 4.7 流程实例状态

- 审核中：存在尚未完成的审批节点。
- 驳回待处理：当前审核轮次被驳回，等待发起方按流程版本配置修改后重新提交，或由关闭流程权限组关闭；配置为“自动关闭流程”时不进入该状态。
- 已完成：流程所有审批节点均已通过、确认或因条件不满足而跳过。
- 已关闭：关闭流程权限组的授权人员主动关闭流程，属于不可恢复的终态。

### 4.8 流程版本

- 流程需要保留版本记录，版本记录至少展示版本号、界面状态、是否允许编辑、实例数量、首次发布时间、最近发布时间、发布操作人、校验摘要和更新时间。
- 流程定义总实例数和各版本实例数必须按实际流程实例的稳定 `definitionId`、`versionId` 关联统一统计，不能维护一套与流程清单脱节的独立展示数字；Debug Mock 也遵循相同规则，并在读取旧浏览器数据时自动重新核算。
- 版本列表统一显示三个互斥状态：“校验未通过”“可发布”“已发布”。“校验未通过”使用红色，表示版本已保存但完整校验失败；“可发布”使用蓝色，表示校验通过但不是当前发布版本；“已发布”使用绿色，表示当前流程唯一用于新实例的版本。
- 数据层分别保存校验结果和发布关系，不能把两者混成一个可被多处修改的状态字段。流程定义保存的 `publishedVersionId` 是当前发布版本的唯一事实来源；版本界面根据该指针与最新校验结果派生上述状态。
- 一个流程定义可以有多个正式编号版本，但最多只能有一个发布版本，也允许没有发布版本。不存在“生效/失效版本”和“草稿版本”的概念。
- 每次保存版本时自动执行完整配置与依赖校验，并更新校验摘要和校验时间。发布或切换发布版本前必须再次校验；校验未通过时禁止发布。
- 发布或切换版本时，在同一事务中将 `publishedVersionId` 指向目标版本；原发布版本自动变为未发布状态，并根据最新校验结果显示“可发布”或“校验未通过”。切换过程不得出现两个发布版本。
- 当前发布版本不允许直接编辑或删除。需要修改同一版本时必须先明确执行“取消发布”；取消发布后仍只有实例数为 0 的版本可以编辑或删除。
- “可发布”只表示版本通过校验并具备发布条件，不等于一定可编辑。已经产生实例的未发布历史版本永久只读，但仍可在重新校验通过后再次切换为发布版本；界面需要使用只读图标或说明展示编辑限制。
- 版本列表中的“查看”必须打开该版本的完整只读快照，不能只显示字段和节点数量。管理员至少可以查看基本信息与权限范围、初始表单的全部字段属性和表格列、系统列表字段配置，以及固定审批的实际节点连线、节点权限组、指定人员方式、可修改字段和驳回规则；自由协作版本需要展示受理范围和固定协作规则。查看发布版本或已有实例的只读版本时不得出现可修改控件。
- 新发起实例始终使用提交发生时的发布版本；已经创建的实例永久关联其发起时版本，后续发布、版本切换、取消发布或流程停用均不能改变该关联。
- 已有实例被驳回后修改并重新提交时仍使用创建实例时关联的原版本，包括原版本表单、审批拓扑、可修改字段、规则和编号前缀快照；不得因当前发布版本变化而迁移实例版本。
- 后台和权限模型必须区分“发布流程版本”与“提交/重新提交流程实例”，两种操作使用不同接口、状态校验和审计事件。流程发起页面的主按钮统一使用“提交”，驳回后的再次提交使用“重新提交”，不再使用“提交并发起”“发布流程实例”或“重新发布”文字。
- 流程清单只在流程定义存在当前发布版本时提供入口。进入后展示该流程全部历史版本的实例，列表字段和查询配置统一读取当前发布版本；切换发布版本后列表口径同步切换，但历史实例数据本身不发生变化。历史实例不存在当前发布版本定义的字段时显示为空，不自动合并其他版本的字段列。
- 发布版本因流程权限组停用、成员变化等外部依赖而在后续运行检查中失败时，版本主状态仍显示“已发布”，同时展示独立的运行异常提示并阻止新实例创建；系统不得在没有管理员操作的情况下自动取消发布。

#### 4.8.1 完整版本快照

- 每个版本必须保存一份可以独立校验、渲染和运行的完整配置快照，正式运行时不得依赖上一版本进行递增合并或通过版本差量还原完整配置。
- 完整快照至少包含基本信息及编号前缀、初始表单完整结构、稳定字段标识与选项、系统和业务列表字段及 Excel 导出字段配置、动态查询配置、固定审批拓扑或自由协作规则、节点处理方式、重复修改和邮件通知配置、流程规则、附件限制、流程权限组引用以及额外可见范围引用。
- 流程权限组、角色和用户等全局共享数据在版本中保存稳定标识引用，不复制其成员列表；其成员变化仍按照本文既定规则动态生效。“完整快照”是指流程自身的全部配置完整，不是复制所有外部主数据。
- 复制新建版本时，先完整复制所选来源版本的快照，再在新版本中修改。例如 V1 包含字段 A，基于 V1 创建 V2 并新增字段 B，则 V2 的完整快照同时包含 A 和 B；删除 V1 后，V2 仍可独立运行。
- 创建新版本时可以选择任意现存版本作为复制来源，来源版本不要求是当前发布版本或版本号最大的版本。例如流程已有版本 1、2、3 时，可以基于版本 1 的完整快照创建版本 4。
- 新版本创建完成后不得在运行时依赖来源版本。来源版本标识只用于界面提示和审计追溯，不能作为读取表单、流程图、规则或权限配置的父级链路；即使来源版本随后被删除，新版本仍必须完整可用。
- 后台复制快照时，如果配置保存在 JSON 中，应复制完整结构到新版本记录；如果采用表单字段、节点等子表，应为新版本复制全部子记录并生成新的版本内记录标识，同时保留跨版本稳定字段标识。
- 任意现存版本都可以作为复制来源创建下一个正式编号版本。当前发布版本不可直接编辑；取消发布且实例数为 0 后允许继续编辑同一版本。任何已有实例的版本都不可编辑，只能复制新建版本。

#### 4.8.2 版本号、保存与校验

- 版本号统一使用 `V1、V2、V3……` 的整数流水格式，不使用 `1.0、1.1` 等主次版本格式。新流程定义的基本信息首次保存成功时生成正式版本 V1；后续从任意版本复制新建时取得下一个从未使用过的版本号，不根据来源版本号递增，也不覆盖历史版本。
- 版本号由流程定义上的单调递增计数器分配，不能在创建时临时使用“当前剩余版本最大值 + 1”计算。即使较高版本被删除，已经分配过的版本号也不能再次使用。
- 流程定义和版本都不使用草稿状态。基本信息、初始表单和流程设计步骤的“保存”直接写入当前正式编号版本，并在保存成功后自动执行完整校验。
- 保存不等于发布。保存成功后版本根据校验结果显示“校验未通过”或“可发布”；只有管理员执行“发布此版本”后才成为“已发布”。
- 允许同一流程同时存在多个未发布版本；它们各自保存完整快照，互不覆盖。创建新版本时必须明确显示来源版本和新版本号。
- 版本被删除后，其版本号不得重新分配或用于新版本，剩余版本也不重新编号。例如删除 V1 后，V2 仍保持 V2，下一版本仍为 V3。
- 流程配置向导内点击“上一步 / 下一步”时，如果当前步骤存在修改，系统先自动保存当前正式版本并更新校验结果，保存成功后再切换步骤；没有修改时直接切换。保存失败或当前输入存在无法保存的格式错误时停留在当前页并提示原因。完整流程校验未通过不阻止切换步骤，版本保存后标记为“校验未通过”，在发布页集中展示问题。
- 切换版本、通过页面级返回或菜单离开编辑页面时，仍需提示保存、放弃或取消离开；未点击保存且未通过向导步骤切换自动保存的浏览器临时输入不属于版本数据。
- 未保存离开保护适用于所有会产生临时输入的页面和编辑器，包括流程发起表单、流程基本信息、表单设计器、流程设计器、实例可编辑内容、自由协作回复与表单，以及用户、部门、职务、角色和流程权限组编辑。应用内导航或主动关闭编辑抽屉时使用统一确认对话框；有独立保存能力的编辑器提供“保存并离开”，没有草稿或通用保存动作的页面提供“放弃修改并离开/留在当前页”；刷新或关闭浏览器使用原生未保存提示。保存或提交成功后不得继续误拦截正常导航。

#### 4.8.3 取消发布与编辑保护

- 当前发布版本只能通过明确的“取消发布”操作转为未发布，不允许在已发布状态下直接保存修改或删除。
- 取消发布需要二次确认，明确说明流程将暂时没有发布版本、员工无法发起新实例，以及已有实例仍按原版本继续运行。操作需要记录版本、操作人、时间和取消原因。
- 取消发布确认框中的风险说明、原因标签、文本框、字数统计和底部操作按钮必须保持清晰间距；字数统计不得与按钮栏或其他信息重叠。
- 取消发布本身不修改版本快照；取消后版本仍使用原版本号，并根据自动校验结果显示“可发布”或“校验未通过”。流程定义的 `publishedVersionId` 清空，定义状态变为“未发布”；流程原先处于停用状态时继续保持“已停用”并标识无发布版本。
- 取消发布时必须基于该版本当前完整快照重新执行与保存、发布相同的完整校验并写回校验结果，不得继续使用发布前遗留的过期校验状态；在外部依赖未变化时，同一快照取消发布后的校验结果必须与基于该版本复制新建后的校验结果一致。
- 取消发布后，只有实例数为 0 的版本允许编辑和删除。已经产生任何实例的版本即使不再发布，也必须永久只读，只能作为复制新建版本的来源。
- 已有实例版本的“永久只读”只限制基本信息、表单、流程图、规则和其他完整快照内容，不限制重新执行校验。权限组状态、有效成员、角色或用户等外部主数据修复后，管理员必须能够在不编辑版本快照的情况下重新校验该版本，并在通过后再次发布。
- 零实例版本取消发布后可以直接编辑同一版本并反复保存，版本号不变；保存时自动重新校验。再次发布时必须通过完整校验，并记录首次发布时间和最近发布时间。
- 管理员也可以不先取消发布，直接将另一个校验通过的版本切换为发布版本；该操作必须原子完成，新版本发布后原版本自动变为未发布。
- 取消发布、切换发布版本和实例创建必须在后台事务中重新校验当前指针、目标版本校验结果和实例并发状态，不能只依赖页面加载时显示的数据。
- 页面保存的校验结果仅用于展示，不得作为禁用发布入口的唯一依据。未发布版本无论当前显示“可发布”还是“校验未通过”，具有发布权限的管理员都可以点击发布入口；客户端先通过版本最新 ETag 请求重新校验，校验通过后才显示发布确认，未通过时写回并展示最新问题。最终发布命令必须在后台事务内再次按最新外部主数据执行权威校验，防止校验后到发布前状态变化。
- 取消发布修改的是流程定义的发布指针，客户端必须在确认提交时重新获取流程定义资源，并使用其最新 ETag 作为 `If-Match`；不得错误使用流程版本 ETag。版本内容的保存、删除和发布校验仍使用对应版本资源的 ETag。

### 4.9 流程规则配置

首版只提供一个流程级可配置规则：“驳回后的处理方式”。审批节点自身可以配置处理方式、重复修改、执行条件和邮件通知；未明确列出的其他审核行为采用本文档规定的固定规则。

“驳回后的处理方式”提供以下选项：

1. 重新提交或关闭（默认）：流程进入“驳回待处理”，发起方可以修改后重新提交，关闭流程权限组也可以直接关闭流程。
2. 仅允许重新提交：流程进入“驳回待处理”，发起方可以修改后重新提交，关闭流程权限组也不能在该状态关闭流程。
3. 自动关闭流程：驳回结果提交成功后直接关闭流程，取消其他未完成待办，不进入“驳回待处理”。

- 该配置属于流程定义版本的一部分，发布后不可直接修改；当前发布版本必须先按 4.8.3 取消发布，并确认实例数为 0 后才能修改。
- 运行中的流程实例始终使用发起时版本所保存的配置，新版本不得改变已有实例的驳回行为。
- 流程设计器右侧属性面板通过“节点属性 / 流程属性”切换展示不同层级的配置，并与表单设计器“字段属性 / 系统字段”的顺序一致，将流程级属性放在后侧页签。“驳回后的处理方式”放在“流程属性”中，不与顶部的保存、校验、发布操作混排。
- 新建流程默认生成的开始、审批和结束节点，以及后来拖入画布的新节点，其“节点说明”默认均为空；节点库中的用途说明只用于帮助选型，不自动写入节点版本数据。
- 点击节点时右侧自动切换到“节点属性”；点击画布空白处时自动切换到“流程属性”；选中节点后也允许手动在两类属性之间切换。
- 发布预览和版本详情需要明确展示该配置。
- 后端提交驳回结果时，需要在同一事务中完成本轮结束、其他待办取消，以及进入驳回待处理或自动关闭等状态变化。
- “任一并行路径驳回立即结束本轮”“重新提交后全部审批节点重新处理”“首个审核结果产生后锁定发起内容”“通过意见选填、确认说明选填、驳回意见必填”等均为固定规则，不提供配置开关。

#### 4.9.1 审批节点执行条件

- 每个审批节点可以选择是否配置执行条件。条件引用初始表单字段的稳定字段标识，而不是字段显示名称。
- 示例：字段 A 的数值大于 `10` 时执行生产审核，否则生产审核节点自动跳过且不创建待办、不发送邮件。
- 节点即将激活时使用当时已经提交的最新表单值计算条件。条件满足时创建或激活待办；条件不满足时，运行时内部将节点记为“已跳过”，但不创建待办、不发送邮件，也不生成面向用户的流转记录。
- “已跳过”只作为流程引擎内部状态参与后续 AND 汇聚计算，不等同于审核通过。被跳过的审核节点在流程进度、流程详情、审批信息、流转记录、实例监控、打印及导出等用户可见内容中完全隐藏；如果本轮全部审核节点均被跳过，进度只显示开始和结束。
- 首位审核结果提交前，发起方修改并保存条件引用字段时，系统重新计算尚未处理的条件节点；已经产生审核结果后，继续按现有规则锁定发起内容。
- 下游条件可以引用前序审核节点填写的字段，因此每次前序审核结果与字段修改原子提交后，都需要重新计算新近就绪的条件节点。
- 多条条件支持“全部满足（AND）”和“任一满足（OR）”。数字比较支持等于、不等于、大于、大于等于、小于和小于等于；文本、下拉和单选支持等于、不等于；复选支持包含、不包含；所有适用字段支持为空、不为空。
- 当前表单没有单独的数字控件时，文本字段可以使用数字比较；运行时必须能解析为有限数字，否则该条比较判定为不满足。
- 发布校验必须阻止字段已删除、字段类型与操作符不兼容、比较值缺失或规则为空等无效条件。
- 流程设计器载入历史或异常条件快照时必须先规范化 AND/OR 模式与规则数组；缺少规则数组、字段标识或操作符时页面保持可操作并交由发布校验提示，不得因开启“按条件执行”导致页面崩溃。当前表单没有可用于条件判断的字段时禁用该开关并说明原因。
- 条件字段下拉的数据项不得使用 Ant Design Select 保留的 `options` 属性承载单选、下拉等字段的业务选项；业务比较值使用独立属性保存，避免“是/否”等字符串被误识别为分组选项并导致页面崩溃。
- 条件规则编辑器采用两行布局：字段选择独占第一行的完整宽度，操作符、比较值和删除操作位于第二行。字段下拉弹层应提供足够宽度并保留完整名称提示，长字段名不得被压缩到只显示一两个字符。

#### 4.9.2 审批节点处理方式

- 流程设计器“节点属性”提供“处理方式”单选项：“审批”（默认）和“确认”。该配置随完整版本快照保存。
- 审批节点显示“审核意见”，通过时选填、驳回时必填，并提供“通过并提交”和“驳回”。确认节点显示“确认说明（选填）”，唯一结果按钮为“确认并提交”，二次确认文案为“确认完成本节点？”。
- 确认提交后，任务和流程记录分别保存为“确认 / 已确认”，不得显示为“通过 / 已通过”。发布预览、版本快照、流程详情和打印记录均需明确展示节点处理方式及实际结果。
- “确认”和“通过”具有相同的正向流转效果：串行流程进入下一节点；并行流程等待其他分支通过、确认或跳过；全部前置路径满足后流程完成。
- 其他并行审批节点仍可以驳回。驳回时取消尚未处理的审批或确认任务，已经完成的确认记录继续保留。
- 审批和确认都允许在提交结果时原子保存该节点获授权修改的字段；确认节点只是不能驳回，不减少字段修改能力。

#### 4.9.3 审核结果提交后的重复修改

- 审批和确认节点均可配置“允许重复修改”，含义为“允许审核结果提交后继续修改”。默认关闭，旧版本行为不变；没有配置“审核人可修改字段”时开关禁用且必须保持关闭。
- 首次通过或确认仍正常完成节点。开启后，实际处理人可以在流程详情点击“继续修改”，反复修改本节点授权字段，直至流程进入驳回待处理或已关闭；流程已完成但未关闭时仍允许继续修改。
- 继续修改使用独立的“保存修改”，不能更改原来的通过或确认结果，不能改为驳回，不重新激活节点，也不重复生成审核记录。超级管理员可以按系统级处理权限代为修改，普通组内其他成员不能修改他人已经完成的节点。
- 每次保存必须记录修改人、修改时间、选填的修改说明，以及本次修改的稳定字段标识和当时的字段名称。系统只保存表单、表格和附件字段的最新值，不保存、展示或打印修改前值、修改后值及旧附件名称；页面时间线与打印记录只说明修改了哪些字段。
- 领域命令必须按节点字段白名单应用修改，不能依赖前端隐藏控件；表格列授权只能修改该列，整表授权才允许改变行结构。
- 重复修改后的最新值供尚未就绪的下游条件节点在实际激活时使用；已经激活、跳过或完成的节点不反向重置。

### 4.10 流程可见范围

流程查看采用“页面权限 + 流程数据范围”两层授权，不将额外的只读查看人员加入审批节点使用的流程权限组。

- 角色权限控制用户能否进入“流程清单”页面及使用流程查看功能。
- “流程清单”是指当前用户可见范围内的流程实例，不代表所有用户天然可以查看系统中的全部数据。
- 属于某个流程发起流程权限组的用户，可以查看该流程的流程实例和表单信息。
- 属于该流程审批节点所引用流程权限组的有效成员，可以查看该流程的流程实例和表单信息。
- 实际发起人、默认责任人和实际处理人始终可以查看自己参与的流程实例。
- 流程基本信息中增加“额外可见范围”配置，可以选择一个或多个角色，也可以直接选择具体用户。
- 通过“额外可见范围”获得权限的用户只有查看权限，不获得发起、通过、驳回、重新提交或关闭等操作权限。
- 审批节点使用的流程权限组不用于容纳纯只读人员，避免误授予审批操作能力。
- 可以设置系统级“查看全部流程实例”权限，供管理员或审计人员查看所有流程。
- 列表接口和流程详情接口均需要在后端校验页面权限与流程数据范围，不能只依赖前端隐藏菜单或按钮。

### 4.11 待办数字提醒

- 首版不提供站内通知功能，不显示顶部通知入口、未读通知数字、通知列表或已读状态，也不保存通知记录。
- 任务中心提供“我的待办”“可代办”“我的发起”三个主视图，并分别显示当前数量。“我的待办”和“可代办”数量直接由当前有效任务计算；“我的发起”显示当前用户本人发起且尚未完成或关闭的全部流程实例，包括审核中、驳回待处理和进行中的固定审批或自由协作流程。
- 固定审批流程被驳回并进入“驳回待处理”后，除继续保留在创建人的“我的发起”外，还必须作为“待我重新提交”进入实际流程创建人的“我的待办”，计入待办数字并直接进入修改、重新提交页面。流程管理员或超级管理员能够处理他人流程不等于承担该待办，因此不把所有人的驳回流程集中显示在管理员待办中。
- 正式任务中心以统一任务条目返回，但必须使用稳定 `taskType` 区分审批任务、自由协作受理任务和待重新提交任务。只有审批任务包含节点、处理方式和审批流程权限组；自由协作没有流程节点，只关联当前受理人；待重新提交任务只关联实际流程创建人。后端和前端不得通过伪造虚拟审批节点兼容后两类语义。
- “我的发起”继续支持按流程分类和关键词搜索。全部流程分类只显示公共列，选择单个流程后显示该流程配置的公共列与自定义列；不显示“任务归属”，操作只提供查看流程，不把发起人误判为当前审核人或代办人。
- 流程权限组成员变化后立即重新计算相关人员的待办范围和数字。

### 4.12 节点邮件通知

- 审批节点提供“进入节点时发送邮件”开关，默认关闭。启用后可选择是否通知该节点审核人，并可从已维护有效邮箱的启用用户中选择一个或多个额外收件人；额外收件人只获得邮件，不因此获得流程查看或处理权限。
- 额外通知用户选择器在多人场景下使用姓名短标签和折叠人数，选择器下方显示可滚动的“姓名 + 邮箱”清单，避免长邮箱标签挤压或无法确认已选人员。
- 结束节点也提供独立邮件通知开关，可选择通知流程发起人并添加额外用户。审批和结束节点的邮件配置均随版本快照保存，旧节点默认关闭，避免历史流程突然产生外发邮件。
- 流程实例进入已开启邮件通知的审批节点并激活待办后，按配置通知处理人员和额外用户。首次提交激活首批节点、上一节点通过或确认后激活后续节点、并行分支同时激活以及驳回后重新提交并重新激活节点，都适用同一规则；流程完成到达结束节点时按结束节点配置通知。
- 邮件通知属于 ASP.NET Core 后端职责。前端不直接连接 SMTP、不保存邮件账号密码，也不以浏览器是否保持打开作为发送条件。当前交互原型不实际发送邮件，正式后端接入时实现。
- 配置为通知审核人时，节点在发起时已经指定默认责任人则只向该默认责任人发送；未指定时向该节点流程权限组当前全部有效成员发送。额外用户与审核人、发起人之间按照用户标识去重。
- 收件人只包含账号启用且维护了有效邮箱地址的用户。超级管理员不属于流程权限组，因此不作为普通节点邮件收件人；同一批节点激活中同一用户命中多个节点时，应合并或去重通知。
- 流程权限组成员在待办已经激活后发生变化时，权限资格仍立即生效，但不追发该节点的“进入节点”邮件；后续节点再次激活时按照当时的有效成员重新计算收件人。
- 邮件至少包含流程名称、流程版本、实例编号、实例标题、节点名称及处理方式、发起人、进入或完成时间和可点击的流程链接。站点入口不再通过应用环境变量配置。触发邮件事件的浏览器写请求必须优先校验 `Origin`，只有该头缺失时才校验 `Referer`；后端将来源的 origin 与本机可信 IIS/ARR 覆盖后的 protocol 和 host 精确比较，通过后自动将 `${origin}/flowpilot` 随 Outbox 事件冻结。例如从外网 IP 入口发起、从内网 IP 入口审核时，各次事件分别使用当次已验证入口。没有浏览器请求上下文的事件只能继承该流程实例此前保存的已验证入口；实例没有可继承入口时必须明确记录通知失败，不得使用 `127.0.0.1`、服务器名、任意 `Host` 或其他猜测值。任务激活通知使用受控相对路径 `/processes/{instanceId}?taskId={taskId}`，最终链接到 `/flowpilot/processes/{instanceId}?taskId={taskId}`，结束通知使用 `/processes/{instanceId}`。链接不得包含密码、会话令牌、一次性授权码、邮箱或其他敏感查询参数。未登录用户先进入登录页，登录成功后只允许返回已校验的同源 FlowPilot 相对路径；进入详情和执行处理时后端仍重新校验权限、数据范围、任务状态与 revision。额外通知用户没有查看权限时点击后显示无权限，不因收到邮件自动获得权限。
- 后端使用 `MailKit`/`MimeKit` 封装 SMTP 和 MIME 邮件，不使用已不建议用于新开发的 `System.Net.Mail.SmtpClient`；发件人固定来自服务器配置，调用方不能覆盖，收件人和主题拒绝换行注入。SMTP 默认强制 TLS/STARTTLS 并校验证书；只有旧内网服务确实不支持 TLS 时才可通过独立显式开关接受明文风险，不能机会式静默降级。SMTP 地址、端口、加密方式、发件账号和凭据均从服务器外部配置读取，不写入前端、程序目录或代码仓库，也不迁移或调用 SQL Server `CDO.Message`/`sp_OA*` 发信过程。
- 审批任务创建或激活与邮件投递请求应采用事务消息或 Outbox 方式：业务事务内保存任务状态和待发送事件，事务提交后再异步发送邮件。邮件服务器异常不得回滚已经完成的流程流转，也不得阻塞审核接口响应。
- 每封邮件的发送记录必须持久化到 SQL Server。Outbox 保存业务事件、实例/任务、收件人用户及邮箱快照、主题、模板与最小模板数据、受控目标路径、首次发送时解析并冻结的绝对链接、状态、计划时间、尝试次数、发送时间和最后错误；每次 SMTP 尝试另存开始/结束时间、结果和脱敏响应摘要。数据库不保存完整 MIME 邮件、渲染后的完整正文、业务附件、密码或 SMTP 凭据。已发送记录默认保留180天，死信在问题解决或手工重试成功前保留。
- 每封节点通知使用“任务激活事件 + 收件人”作为幂等键，避免接口重试或服务重启造成重复邮件。发送失败需要自动重试并记录失败原因；达到配置的重试上限后写入操作审计日志，供管理员排查。
- 节点尚未激活、待办已取消、流程已关闭或并行分支因其他节点驳回而取消时，不发送进入节点邮件。邮件通知不恢复已经取消的待办，也不替代任务中心中的实时权限校验和待办数字。

### 4.13 编号生成

- 流程定义、流程版本、流程实例、审批任务、用户、角色、流程权限组和附件等相关内部标识均由后端自动生成。
- 流程定义编号和版本号等需要展示给用户的业务编号由后端自动生成，前端不提供手工编辑入口。
- 流程实例编号统一使用“前缀 + 两位年份 + 两位月份 + 四位流水号”的连续格式，不插入额外分隔符。例如前缀为 `ccc` 时，2026年8月的首个实例编号为 `ccc26080001`。
- 实例编号前缀在流程的基本信息设计步骤中必填，属于流程定义的基本配置入口；前缀支持英文字母、数字、横线 `-` 和下划线 `_`，允许多个不同流程重复使用，不作为流程定义的唯一标识。
- 未发布且实例数为 0 的版本可以直接修改编号前缀；当前发布版本必须先按 4.8.3 取消发布，并确认实例数为 0 后才能修改。每个版本保存自己的前缀快照，因此同一个流程的不同版本允许使用不同前缀。
- 修改前缀不改变已有实例编号，也不迁移或重算历史实例；新版本发布后创建的新实例使用新前缀。将来切回曾用前缀时，继续该前缀在当前年月的共享流水，不重新从 `0001` 开始。
- 流水号按“前缀 + 年月”建立共享序列，每月从 `0001` 开始。同一前缀下，无论实例来自哪一种固定审批或自由流程，都必须从同一个序列依次取号。例如流程A先生成 `ccc26080001`，流程B随后生成 `ccc26080002`。
- 复制新建进入预填编辑页时尚未创建流程实例，不占用实例编号。用户完成修改并确认提交后才视为新建流程实例，使用目标流程当前发布版本配置的前缀并从共享序列取得新编号；取消或离开复制编辑页不生成编号，重新提交、重新打开和节点流转也不得生成新的实例编号。
- 实例编号只在后端成功创建实例时正式分配；页面上的格式或下一编号预览不占用流水号。
- 业务编号在系统中保持唯一且生成后不可修改。
- 编号生成需要在数据库事务中锁定“前缀 + 年月”的计数记录，保证不同流程并发创建时不重复。

### 4.14 流程与版本删除

- 流程定义只有在没有发布版本且全部版本的实例总数为 `0` 时才允许删除。当前仍有发布版本时必须先取消发布，不能在删除确认中隐式取消发布。
- 删除流程定义会同时删除其全部正式版本、流程配置、菜单入口和设计引用；删除前必须再次校验 `publishedVersionId` 为空且总实例数为 `0`，并进行明确的二次确认。
- 只要流程任一版本存在运行中、已完成、已关闭或其他状态的实例，整个流程定义都不可删除，只能停用。
- 单个版本只有在当前未发布且实例数为 `0` 时才允许删除。版本曾经发布过不构成永久删除阻断条件，但当前发布版本必须先单独取消发布。
- 新版本曾从某个版本复制，不构成来源版本的删除阻断条件。例如 V4 基于 V1 创建后，只要 V2、V3 未发布且没有实例，就可以删除 V2、V3；V1 满足相同条件时也可以删除，而不影响 V4。
- 当前发布版本不提供直接删除操作。管理员可以先切换发布版本，或取消当前发布；原版本变为未发布且实例数为 `0` 后才显示删除入口。
- 删除流程定义中的最后一个版本时，同时删除空流程定义；系统不保留没有任何版本的定义容器。
- 有实例的版本不可删除，即使相关实例已经完成或关闭；这些实例需要永久保留其版本关联和完整配置快照。
- 删除流程或版本均属于不可恢复的高风险操作，需要独立“删除”权限并写入操作审计日志；流程启停继续归入“编辑”权限，版本发布、取消发布或切换发布版本继续归入“发布流程”权限。
- 创建实例与删除版本必须受数据库事务和外键共同保护：流程实例对流程版本使用限制删除关联；删除版本时在同一写事务内再次检查实例不存在、确认版本未发布并删除完整快照，避免并发创建实例与删除版本发生竞态。
- 版本来源关系使用可空引用或仅保存来源版本号快照，删除来源版本时不得级联删除派生版本，也不得因审计来源引用阻止业务允许的版本删除。

### 4.15 流程定义 JSON 导入导出

- 流程管理支持对单个流程定义导出 JSON 文件；导出范围包含该定义的全部版本，以及每个版本独立保存的基本信息、初始表单、系统列表字段、流程节点、连线、条件、邮件通知和驳回规则，不包含流程实例、附件内容、待办或审核记录。
- 页面按钮和操作菜单统一显示“导入”“导出”，不在界面操作文本中重复显示文件格式名称；文件选择对话框仅允许选择 `.json` 文件，读取前再次校验扩展名，其他格式直接拒绝。
- JSON 以业务人员可直接阅读为目标，使用有层级的中文属性名和缩进格式。流程权限组、角色、用户、表单字段、表格列、节点和条件引用全部使用当时的显示名称，不导出数据库主键、稳定字段标识、节点 ID、连线 ID、用户 ID、角色 ID、权限组 ID或流程内部编号。
- 导入时为流程定义、版本、字段、表格列、节点和连线重新生成内部稳定标识与流程编号；引用流程权限组、角色和用户时按显示名称精确匹配当前系统数据。同名信息存在则关联当前内部标识，不存在则自动省略，并在最终导入前汇总显示省略项。
- 导入不会覆盖现有流程，也不会自动发布。若已存在同名流程定义，则新定义名称自动追加“（导入）”；导入的全部版本均为未发布、零实例的独立完整快照，管理员检查并修正被省略的引用、通过校验后再选择版本发布。
- 导入文件必须带有明确的 FlowPilot 文件类型和格式版本；文件无法解析、类型不符、缺少流程名称或没有版本时拒绝导入，不创建任何部分数据。
- 导入确认必须通过正式 REST 导入命令提交完整导出文档，由后端在一个事务内完成解析、引用匹配、重新编号、完整校验和写入；前端不得绕过接口直接修改本地流程仓库。正式响应返回新定义及全部完整版本快照，便于客户端立即刷新统一缓存。

## 5. 初始表单设计器

### 5.1 基本能力

- 创建流程时可以同时配置该流程的初始表单。
- 初始表单不设置独立的“表单名称”；设计器、预览、发布确认和版本快照统一使用流程名称与“初始表单”标识。
- 每个初始表单固定包含一个稳定字段标识为 `title` 的“标题”文本框。该字段始终必填，不能删除或改变控件类型；字段名称、说明、提示文字、默认值、审核修改权限以及“在任务中心显示 / 在流程清单显示 / 导出到 Excel”等业务属性仍可配置。“作为查询条件”固定开启且不能修改，读取或复制历史版本时也必须自动恢复为开启。
- 发起人填写的 `title` 字段值作为流程实例标题，统一用于任务中心、流程清单、详情页和其他实例引用位置。标题字段的默认“字段说明”为空，不预填这段系统用途说明；管理员仍可自行填写说明。标题在任务中心和流程清单中的显示位置直接通过固定 `title` 字段的字段属性配置，不进入“系统字段”页签，也不能再作为动态业务列重复显示。
- 表单控件通过拖拽方式添加并排序。
- 表单采用统一单列布局。
- 选择控件后可以配置标题、字段标识、提示文字、默认值、是否必填和选项等属性。
- 新增表单项的“提示文字”默认统一为空，包括固定标题、文本、富文本、选择和附件控件；运行页面不得用“请输入/请选择”等通用文案替代空配置。管理员明确填写后才显示，复制历史版本时保留来源版本已配置的提示文字。
- 表单设计器支持在画布卡片中就地编辑字段标题；下拉框、多级下拉、单选框和复选框可直接录入或删除选项；“必填”和普通文本框“多行显示”等高频属性也可直接切换。默认值、输入权限、列表/查询/导出位置、附件限制和表格列等低频或复杂配置继续放在属性栏，画布和属性栏实时使用同一份字段数据。
- 下拉框、多级下拉框、单选框、复选框以及表格中的选择列，其每个选项都使用不可见的稳定选项标识与可编辑的显示名称分离保存。管理员可以直接把已有选项 A 改名为 A1；改名只更新名称，不更换稳定标识，因此默认值、显示条件、节点执行条件和已保存的实例选中值仍然指向同一选项。
- 新增选项必须生成新的稳定标识；删除后重新创建同名选项也视为新选项，不能自动继承已删除选项的历史选中关系。删除仍被默认值或条件引用的选项时，设计器应清理可安全清理的默认值，并由保存/发布校验明确提示仍然存在的失效条件引用。
- 运行时值、默认值和条件比较值保存稳定选项标识，不保存显示名称。实例详情与打印按照实例锁定的版本快照显示当时的选项名称；流程清单按当前发布版本的列口径显示时，同一稳定选项可以使用当前名称，当前版本已删除的选项不在该列展示为可选项，但原实例和值仍保留在历史版本快照中。
- 旧原型中以选项名称保存的定义、默认值、条件和值在读取时自动迁移为稳定选项标识；迁移必须确定、可重复，刷新或重复迁移不得生成不同标识。流程定义 JSON 导出继续只输出可读选项名称，导入时重新建立稳定标识并恢复默认值和条件引用。
- 普通文本框支持“多行显示”开关，关闭时运行页使用单行输入框，开启时使用可自动增高的多行文本框；该配置随版本完整快照保存。固定流程标题始终使用单行文本框。
- 除固定标题外，每个表单项可以配置“按条件显示”。条件只能引用当前项之前的文本框、下拉框、多级下拉、单选框或复选框稳定字段标识，支持多条规则以及“全部满足（AND）/任一满足（OR）”；比较操作与流程节点执行条件保持一致。
- 条件显示在设计器预览、流程创建、流程详情和审核编辑中使用同一套计算规则。条件不满足时表单项完全隐藏、不参与必填校验，其旧输入值在提交领域命令时清空，避免隐藏值继续进入列表、导出或驱动后续字段；条件重新满足后按空值展示并允许重新填写。
- 字段显示条件随流程版本完整快照保存。条件编辑器允许删除任意一条规则；删除最后一条规则时自动关闭该字段的“按条件显示”，不得保留无法删除或无法发布的空规则。发布校验必须阻止引用字段不存在、引用当前项或后续项、控件类型不支持、操作符不兼容、比较值缺失或规则为空等配置。
- 表单字段可以分别配置“在流程列表中展示”“允许作为查询条件”和“导出到 Excel”。导出配置与列表展示相互独立，均随版本快照保存。
- “流程清单”页面根据流程的表单定义动态生成列表列和高级查询条件；不适合列表或查询的控件类型不显示对应开关。固定 `title` 字段由基础查询区的关键词条件负责查询，不进入高级查询字段集合，不能在基础查询和高级查询中重复显示。
- 任务中心和流程清单统一使用标准数据表展示流程实例：表头、列边界、行边界、行高、隔行底色和悬停反馈保持一致，不使用散落文本或人为断开的卡片行模拟列表。动态字段必须按控件类型显示，普通文本保持省略和完整提示，单选、下拉和复选使用紧凑选项标签，附件显示文件名与数量，表格显示数据行数，空值统一显示为 `—`；不得将表格对象直接转换成无意义文本。
- 同一流程定义下的全部版本实例共用当前发布版本的流程清单列配置和动态查询配置。V2 发布或被手工切换为发布版本后，流程清单立即以 V2 配置为准，不再并列展示或自动合并 V1 的历史字段列。
- 例如 V1 的字段 A 曾配置为列表展示，V2 删除 A、增加字段 B 并配置为列表展示，则流程清单只显示字段 B；V1 实例因为没有 B 的历史数据而显示为空，不再显示字段 A 列。
- 列表字段按照稳定字段标识读取历史实例值：保留字段仅重命名时，旧实例值可以显示在当前字段名称下；字段被删除后重新创建同名字段时视为新字段，旧值不能自动映射。新字段的默认值不得反向填充旧版本实例。
- 下拉、级联、单选和复选字段显示旧实例值时，应使用该实例所属版本快照中的选项名称；当前发布版本删除或改名选项后，不得让旧实例显示无法理解的内部选项值。
- 字段控件类型发生不兼容变化时，例如文本改为下拉、单选改为表格，必须按“删除旧字段并创建新字段”处理并生成新的稳定字段标识，不能复用旧标识强行映射历史数据。
- 当前版本未展示的历史字段和值仍须保留在原实例数据和版本表单快照中，并可在流程详情中按照该实例所属版本查看；列表结构变化不得删除或改写历史数据。
- 除业务表单字段外，系统自动生成的实例编号、流程名称、流程版本、状态、当前节点、当前轮次、发起人、发起时间和更新时间，也需要支持分别配置“在任务中心显示”“在流程清单显示”和“导出到 Excel”。固定 `title` 字段仍在自身字段属性中配置这些位置，不属于系统字段配置。
- 系统字段配置属于流程定义版本的一部分，与业务表单字段的列表配置一起保存；当前发布版本不可直接修改，必须先取消发布且实例数为 0 后才能编辑，新版本配置不反向影响历史实例。
- 上述“不反向影响历史实例”是指不改变历史实例的数据和版本快照；流程清单的列结构属于当前展示口径，始终读取当前发布版本配置，因此可以改变历史实例在列表中的展示列。
- 系统字段不属于初始表单，发起人和审核人不能修改；“操作”列不参与显示配置并始终保留，避免用户无法进入流程详情。
- 轮次显示遵循统一简化规则：第一轮属于流程的默认初始状态，任务中心、流程清单、流程详情、流转记录、打印内容及其他用户可见位置均不显示“第一轮 / 第 1 轮”；从第二轮开始才显示“第 N 轮”。轮次数据仍正常保存，不影响审核、重新提交、查询或审计逻辑。
- 表单设计器右侧属性面板分为“字段属性 / 系统字段”两个页签；“系统字段”页签集中配置系统字段的列表显示位置，不在顶部的预览、保存和下一步等操作按钮中混放。业务表单字段继续在各自的字段属性中配置显示位置，二者不混为一组表单控件。
- 新建流程版本时，系统字段使用以下初始开关；实例编号、当前节点和发起人继续保持任务中心、流程清单、Excel 导出均默认开启。复制新建版本继续完整复制来源版本的实际配置，不用新默认值覆盖；已有版本也不因默认值调整被改写。

| 系统字段 | 任务中心 | 流程清单 | Excel 导出 |
| --- | --- | --- | --- |
| 流程名称 | 关闭 | 关闭 | 关闭 |
| 版本 | 关闭 | 关闭 | 关闭 |
| 状态 | 开启 | 开启 | 开启 |
| 当前轮次 | 关闭 | 关闭 | 关闭 |
| 发起时间 | 开启 | 开启 | 开启 |
| 更新时间 | 关闭 | 关闭 | 关闭 |
- 初始表单设计器不提供“恢复示例”按钮；新版本没有已保存表单时从空白表单开始，页面标题、表单说明和预览标签必须读取当前流程和版本名称，不显示 PDF 审核等其他流程的示例文本。
- 表单设计器左侧组件库必须在当前设计器高度内完整可访问；当组件数量超过可用高度时，组件列表使用独立纵向滚动，组件库标题和底部使用提示保持可见，不得因面板隐藏溢出或页面分辨率较低而裁掉后续组件。
- 表单设计器和流程设计器均提供“撤销 / 重做”操作，并支持 `Ctrl+Z`、`Ctrl+Shift+Z` 和 `Ctrl+Y` 快捷键。连续文字输入和一次节点拖动应合并为一个合理的历史步骤，不能按每个字符或每个拖动帧产生大量历史记录；切换流程版本或刷新页面后重新开始当前编辑会话的历史，不跨版本持久化撤销栈。
- 表单定义应作为结构化 JSON 保存，由表单运行页面读取并动态渲染。
- 表单预览页使用“校验表单”按钮检查当前发起人视角下的必填项和控件输入，不使用“模拟提交”文案；校验通过后明确提示预览不会创建流程实例。
- “预览、校验与发布”页面的“初始表单字段”区域只展示字段本身，不在字段名称后拼接“Excel 导出”，也不在版本概要中显示 Excel 导出字段数量；导出开关仍属于表单设计器和流程清单导出配置，不影响实际导出能力。
- 发起页面的动态附件字段统一使用表单组件标准的 `value/onChange` 契约；选择文件后必须立即显示在附件列表中，可删除、替换或继续添加，并在提交实例时保留所选附件名称。不得同时使用原生 Upload 的 `fileList` 注入方式覆盖动态字段值。

### 5.2 首版表单控件

- 文本框
- 下拉框
- 多级下拉框
- 单选框
- 复选框
- 富文本编辑框
- 附件上传
- 表格

### 5.3 多级下拉框

- 采用树状选项结构。
- 首版默认由管理员在表单设计器中手工维护选项。
- 接入数据字典或外部接口暂不纳入首版范围。

### 5.4 表格控件

- 表格字段可以自定义。
- 管理员可以增加、删除和调整表格列。
- 每列至少需要配置列名、字段标识和字段类型。
- 表格列至少支持文本框、单选框、复选框和下拉框类型。
- 单选框、复选框和下拉框列可以分别配置自己的选项列表。
- 表格中的每个列字段可以独立配置是否允许审核人输入。
- 每个表格列可以独立配置是否必填、默认值、列宽和对齐方式；对齐方式至少支持左对齐、居中和右对齐。
- 发起人填写表单时可以新增、删除和复制表格行；复制行生成新的行标识并复制原行字段值。
- 审核人在审核节点中不能新增、删除或复制整行，即使该表格存在允许修改的列。
- 审核人只能修改已有行中由当前审批节点授权修改的单元格。

### 5.5 附件上传与 PDF 展示

- 表单设计器提供附件上传控件，支持常见文档、图片、PDF和压缩包等附件。
- 系统默认单个文件最大100 MB、单个附件控件最多20个文件；附件控件可以配置更小的文件数量和大小限制。
- 默认禁止上传可执行文件和脚本文件，包括 `.exe`、`.dll`、`.msi`、`.com`、`.scr`、`.bat`、`.cmd`、`.ps1`、`.vbs` 和 `.js`。
- 后端需要同时校验文件扩展名、实际内容类型、文件数量和文件大小，不能只依赖前端校验。
- 附件控件可以配置是否必填、是否允许多文件和允许的文件扩展名。
- 附件实际文件保存到内网服务器的本地文件目录，当前配置选择的数据库保存文件标识、原始文件名、存储年份、相对存储键、文件大小、声明与识别内容类型、SHA-256、上传人、上传时间、状态和清理信息等元数据。数据库不得保存附件正文或客户端可直接访问的绝对路径。
- 所有附件查看和下载请求均通过后端进行流程可见范围校验，不能通过文件路径绕过权限访问。
- 当上传的附件是 PDF 时，附件控件可以配置“在流程页面中展示 PDF”。
- 附件控件增加“Excel 转 PDF 后展示”选项。开启后允许用户选择 `.xlsx` 文件；原始 Excel 只在当前浏览器内存中由前端读取，不通过附件接口上传，不写入 localStorage、IndexedDB、审计、详情、下载、打印或历史版本。用户确认预览后，只把生成的 PDF 作为该附件字段的正式文件上传。
- Excel 转 PDF 的页数配置统一命名为“最大页数”，默认值为 `1`，必须为正整数且不得超过系统上限。前端按照可见工作表顺序生成前 N 页；每个工作表优先读取自身打印区域、A4/Letter 等常用纸张、横纵方向、缩放或适应页宽、页边距、水平/垂直居中、打印标题行列和页面顺序，没有打印区域时再按工作表实际范围分页。该页数是 FlowPilot 前端输出页数，不承诺与特定 Excel 版本、打印驱动的分页结果逐像素一致。
- 浏览器使用 ExcelJS 读取 `.xlsx`，使用 Canvas 实现确定性的打印式分页绘制，并由 jsPDF 生成 PDF。转换覆盖单元格文本、富文本的合并文本内容、已保存在文件中的公式结果、日期和百分比基础显示、字体、颜色、填充、对齐、自动换行、缩小字体、行高、列宽、隐藏行列、合并单元格、常用边框及工作表内嵌图片；打印缩放必须同时作用于网格、字体和内边距。图表、透视表、宏、批注、条件格式、特殊页眉页脚及跨页图片仍不保证与 Excel 桌面版一致，但转换预览弹窗不显示原始文件上传边界、兼容性或预计总页数等说明文本，仅展示转换元数据、PDF 正文和转换错误。
- Excel 选择与转换采用“读取中 / 生成中 / 可预览 / 转换失败 / 上传中”状态。PDF 生成后先在页面内预览；用户点击“确认并上传 PDF”前不得向后端发送任何 Excel 或 PDF 内容，取消时直接释放本地预览。确认成功并获得 PDF 暂存附件后，该附件字段才视为已填写。
- PDF 上传请求必须携带锁定的流程定义、版本和附件字段稳定标识；服务端在暂存和实例提交时继续校验 PDF 扩展名、内容签名、大小、数量及安全黑名单。即使字段开启 Excel 转 PDF，正式和 Mock 附件接口也只接受最终 PDF，不接受 `.xls` 或 `.xlsx`，不能把前端转换当成服务端安全校验。`excelToPdf=true` 时，字段的 `allowedExtensions` 用于前端源文件选择；附件接口必须接受转换后的 PDF，即使历史版本的该数组只包含 `xlsx`。
- 浏览器端转换只支持未加密且结构有效的 `.xlsx`，不支持旧式 `.xls`；转换源文件最大 25 MB。前端同时限制可见工作表数、最大行列数、总单元格数和最大输出页数；转换失败或替换候选文件时释放 Blob URL 和临时对象，避免大工作簿持续占用浏览器内存。
- 启用 PDF 展示后，流程详情和审批页面在附件字段原位置提供可折叠的 PDF 预览，同时保留下载入口。预览默认收起，只有用户展开后才加载和显示 PDF 正文；重新收起后停止显示，并在短暂安全延迟后释放当前预览资源，避免页面初始化时同时请求多个大文件及浏览器异步读取尚未完成时地址被提前撤销。
- 流程详情与审核页的动态表单直接采用 Ant Design 标准纵向 `Form` 和 `Form.Item` 展示，不使用自制信息格、散落文本或卡片单元模拟表单。普通字段使用两列标准控件布局，富文本、附件和表格独占整行；不可编辑字段统一使用高对比度只读控件或只读值展示，不使用会灰化文字的禁用态。下拉、多级下拉、单选和复选在只读状态仍保留原控件类型及选项结构，通过统一只读渲染组件阻止鼠标、键盘和弹层交互，不传入 `disabled`，也不转换成文本框；表格中的选择类列遵循相同规则。只读文本框使用原生 `readOnly`，文字、边框和白色背景保持正常对比度。查看或审核流程表单时不显示红色星号、“必填”或“可选”等填写规则标识；流程创建页和表单设计器的发起预览仅对必填字段显示 Ant Design 默认红色 `*`，不使用“必填/可选”文字。字段说明、间距和校验视觉由统一表单规范呈现。
- PDF 预览必须在附件字段原有设计位置内展开，不得抽离到独立侧栏。附件字段明确开启“在流程页面中展示 PDF”且当前值包含 `.pdf` 文件时显示预览；关闭该配置或文件不是 PDF 时只显示附件行。为兼容旧原型快照，附件字段缺少 `inlinePdf` 值时按设计器当前默认值“开启”处理，设计器、发起页和详情页的解释必须一致。
- PDF 预览外框保持紧凑，只保留正文区域的一层细边框；缩小预览与附件行之间的空隙、圆角和页面外侧留白，不能让装饰性边框挤占正文可视区域。
- 流程详情和审核页中，PDF 文件名只在附件清单行显示一次并保持单行省略；“查看/收起”操作与下载操作合并在同一附件行，展开后直接显示 PDF 正文，不再增加折叠标题、预览工具栏、兼容状态或内部文件名等嵌套文本。浏览器预览使用带延迟释放的稳定临时地址，必须兼容开发模式重复挂载，不能因组件清理与 PDF 查看器异步加载竞态导致首次打开失败。
- 启用 PDF 展示的附件字段采用单文件替换模式：只保留一个文件；继续上传时在同一原子保存中移除原文件引用并保留新文件。正式后端在确认旧文件不再被其他版本或实例引用后安全清理物理文件。
- 未启用 PDF 展示或附件不是 PDF 时，页面显示附件名称、大小及下载入口，不进行内嵌预览。
- 附件控件属于普通表单字段。创建、详情和审核页面必须严格按照实例锁定版本的初始表单字段顺序渲染；附件控件配置在第几个位置，运行页面就显示在第几个位置，不能抽离成独立附件卡片、预览栏或页面侧栏。
- 表单没有附件控件时，运行页面不得显示任何附件区域、历史附件名称、数量、上传或更换按钮、“无附件”占位、附件说明和附件型图标，流转记录也不得生成附件相关文字。
- 表单配置了附件控件时，附件名称、下载、上传和删除操作均显示在该字段内部。附件字段处于可编辑状态时，每个已有附件显示删除操作并在确认后移除附件内容及实例引用；只读、内容已锁定或审核节点未获该附件字段修改权限时不得显示或调用删除操作。发起内容处于可编辑状态时允许上传；审核人仅在当前节点明确获准修改该附件字段时允许上传或删除，并将附件变更与保存、重新提交或审核结果一起提交；只读且没有文件时在原字段位置显示空值。最大数量为 `1` 的附件字段统一采用单文件替换交互，无论是否开启 PDF 内嵌展示，已有文件时继续上传都显示为“替换附件”并在保存时原子替换。自由协作流程遵循同一字段顺序和显示依据。
- 附件使用“先暂存、后引用”的两阶段模型。上传接口只创建不可变的暂存文件和元数据；流程提交、表单保存、重新提交或任务决定在业务事务中写入附件引用。替换附件必须生成新的附件标识，不能覆盖原物理文件。
- 附件内部状态统一为 `uploading | staged | active | cleanup-pending | failed | deleted`：完整上传但尚未建立业务引用为 `staged`，存在有效引用为 `active`。普通业务 API 只暴露 `staged | active | cleanup-pending`；上传中、失败和已删除仅用于内部恢复、清理和运维，不使用 `temporary` 或 `referenced` 作为另一套同义状态。
- 交互原型同样必须保存真实附件内容而不只保存文件名：发起时先将文件正文写入浏览器附件存储，提交实例时按附件字段保存附件标识并关联实例；具有实例查看权限的人员通过受控内容接口读取。开启页面内展示的 PDF 使用临时浏览器文件地址加载真实正文，并在组件卸载时释放；旧演示数据只有文件名且没有附件标识时才使用兼容占位预览。
- 附件根目录固定为 `{部署根目录}\Data\Attachments`，解析后必须是绝对路径；附件按创建时由服务端确定的四位上传年份分层。`attachmentIdN` 是附件 GUID 的小写 32 位 `N` 格式，`shard` 固定取其前两位；临时键为 `{yyyy}/.incoming/{attachmentIdN}.part`，正式对象键为 `{yyyy}/objects/{shard}/{attachmentIdN}`。例如 2026 年创建且 ID 以 `6f` 开头的附件位于 `{部署根目录}\Data\Attachments\2026\objects\6f`。年份根据服务端记录的上传时间和统一业务时区生成，一经写入相对存储键不得因后续读取时间或服务器时区变化重新计算。物理文件名不得使用原始文件名；数据库只保存服务器生成的相对存储键，后端解析后必须校验最终路径仍位于附件根目录内。
- 部署根目录不固定盘符或上级路径。可替换程序位于 `{部署根目录}\App`：每个不可变的 `releases\{releaseId}` 发布包同时包含 `api`、`web` 和 `release.json`，`current` 与 `previous` 使用指向本机 `App\releases` 内完整发布包的 NTFS 目录联接。Windows Service 固定执行 `App\current\api\FlowPilot.Api.exe`，IIS `/flowpilot` 固定指向 `App\current\web`；API 与 Web 必须统一切换和回滚，不得各自维护独立 `current`。附件根目录必须位于发布目录之外的 `Data\Attachments`，不得放入 API、IIS 网站物理目录或其他随发布切换和清理的位置。`Config`、`Secrets`、`Data`、`Logs`、`Temp` 和 `Backup` 均为部署根目录下的持久化目录；发布、升级、回滚和清理程序均不得移动、覆盖或删除这些目录。
- `{部署根目录}\flowpilot.root` 是不含秘密的唯一部署根标记。后端从 `AppContext.BaseDirectory` 开始，包含当前目录在内最多检查 6 层祖先目录，必须且只能找到一个该标记；不得固定取父目录，也不得使用 `Directory.GetCurrentDirectory()`、Windows Service 工作目录、`FLOWPILOT_HOME`、`FLOWPILOT_CONFIG_FILE`、`FLOWPILOT_SECRETS_FILE` 或其他路径参数决定根目录。启动时必须规范化并校验部署根不是磁盘根、当前 API 和目录联接目标位于本机 `App\releases` 边界内，以及所有持久化路径没有逃逸部署根。
- ASP.NET Core 服务启动时必须校验附件根目录为绝对路径、可由 Windows 服务账号读写、未落入代码或 IIS 静态目录，并确认临时目录与正式对象目录位于同一磁盘卷；任一强制条件不满足时服务不得就绪。IIS 应用程序池不得拥有附件目录权限，也不得把附件目录配置为虚拟目录或静态资源路径，所有附件内容只能通过经过鉴权的后端接口访问。
- 数据库只保存与附件根目录无关的相对存储键。以后整体更换部署根目录时，必须先停止 Windows 服务和 IIS 应用，复制所需持久化目录并按数据库清单、文件数量和 SHA-256 校验，再重新创建指向新根目录内发布包的 `current`/`previous` 联接、更新服务和 IIS 稳定路径、启动并执行完整性检查；不得批量修改数据库中的附件相对存储键来适配盘符变化。
- 上传使用 ASP.NET Core 流式 `multipart/form-data`，不把整个文件读入托管内存；首版不提供分片或断点续传，不进行附件内容去重，也不设计病毒扫描状态机。SHA-256 仅用于完整性校验和内容 ETag。
- 暂存且未被引用的附件超过24小时后进入清理；被替换且确认无任何业务引用的旧附件进入 `cleanup-pending`，保留24小时后由后台任务删除。删除失败需要保留失败原因并重试，任何仍有引用的文件不得物理删除。
- 年份目录用于按年盘点、备份、归档和缩小清理扫描范围，不代表附件到期策略。不得仅根据目录年份直接删除整个目录；清理前必须通过数据库元数据和业务引用生成校验清单，确认目录内不存在有效引用、未完成上传或待重试文件后，才允许删除已经为空的年份目录。
- 上传前需要检查附件磁盘可用空间；默认至少保留2 GiB，可通过部署配置调整。空间不足时返回 `507` 并在健康检查中显示降级状态。
- 附件内容接口支持单区间 HTTP Range；合法范围返回 `206`，非法范围返回 `416`，多区间请求不纳入首版。只有实际识别为 PDF 的文件允许内联预览，其他类型强制下载。

### 5.6 审核人修改表单

- 每个表单项使用一个“输入权限”下拉框，选项为“发起人、发起人/审核人、审核人”，默认选择“发起人”；删除原来独立的“允许审核人修改”复选框。
- “发起人”表示创建时显示并由发起人输入，审核人不能修改；“发起人/审核人”表示创建时由发起人输入，之后可由被授权审批节点修改；“审核人”表示创建时完全隐藏，流程创建后在原设计位置显示空值或默认值，并由被授权审批节点填写。
- 固定标题字段不能设置为仅“审核人”，但允许设置为“发起人/审核人”；自由协作流程的初始表单仍固定由发起人输入。
- 表格中的每个列字段继续独立配置是否允许审核人输入，以支持按列授权。表格项输入权限为“发起人/审核人”时，流程设计器只提供已开启列；输入权限为“审核人”时可把整表分配给审批节点；输入权限为“发起人”时不提供审核输入候选。
- 流程设计器的“审核人可输入字段”候选项实时读取当前初始表单输入权限和表格列配置，使用稳定字段标识保存；字段改回“发起人”、关闭列权限或删除后，节点中的失效选择自动移除。
- 当前初始表单没有任何审核人可输入字段时，审批节点属性中完全隐藏字段选择、重复修改及相关提示区域，不显示无实际操作价值的空配置块。
- 每个审批节点需要从审核人可输入字段中，进一步选择该节点实际可以输入或修改的字段。
- 不同审批节点可以配置不同的可修改字段，例如研发、质量和生产分别维护各自负责的内容。
- 审核页面仅允许修改已开启该配置的字段，其他字段保持只读。
- 审核或继续修改状态下，当前节点实际获得输入权限的表单项必须在字段名称旁显示“本节点可修改”文字提示；普通只读查看、发起人编辑以及当前节点未授权的字段不显示该提示。表格仅有部分授权列时，提示表示该表格存在本节点可修改的单元格。
- 审核人提交“通过”“确认”或“驳回”成功后自动返回任务中心；已处理任务从当前待办列表移除，用户可以继续处理下一项任务。成功提交后的自动导航不得触发未保存离开提示。
- 附件上传字段同样遵循审核人可修改配置。
- 审核人修改后的表单内容与通过、确认或驳回结果在同一次提交中保存，任何一部分失败都不能产生部分成功的数据。
- 审核提交需要保存本次修改的稳定字段标识及当时名称、实际修改人、审核节点、审核结果和提交时间，不保存字段修改前值或修改后值。
- 审核记录和流程时间线需要能够查看本次审核修改了哪些表单字段。
- 审核修改成功后，后续节点和流程详情读取最新表单内容；历史修改记录只保留修改字段和操作元数据，不能还原历史字段值。
- 位于不同并行路径中的多个审批节点不得配置同一个可修改字段，表格中的同一列字段也按相同规则处理。
- 流程发布校验需要根据连线拓扑识别并行路径，并检测各路径的可修改字段是否重叠；存在重叠时显示具体冲突节点和字段，并禁止发布。
- 串行审批节点可以依次修改同一个字段，后续节点读取前一节点提交后的最新值；历史记录保留每次修改的字段标识、当时名称、修改人和时间，不保留各次字段值。

#### 5.6.1 审核人填写字段

- 表单项使用“输入权限”统一表达创建和审核阶段的输入资格，不再使用“填写阶段 + 允许审核人修改”两套重复配置。审核人权限仍必须由具体审批节点再次授权，不能仅凭字段选择“发起人/审核人”或“审核人”就允许任意审核人修改。
- 审核人填写字段在创建流程页面完全不显示，也不参与发起提交时的必填校验；流程实例创建后，字段按照表单设计器中的原始顺序和位置出现在详情/审核页面。
- 审核人字段未填写时，详情页显示为空；字段配置了默认值时显示默认值，不使用“待填写”占位文本。
- 审核人填写字段自动具备“可由审核节点授权修改”的能力，仍必须在流程设计器中分配给具体审批节点。其他审批人和发起人只能查看，不能修改。
- 审核人填写字段配置为必填时，在负责该字段的审批节点提交通过或确认时校验；发布校验需要确保每个审核人必填字段至少分配给一个审批节点。
- 审核人填写的整表字段由获授权节点维护。该节点可以新增、复制和删除行，并填写表格列；普通的发起人填写表格仍沿用“审核人只能修改授权单元格、不能改变行结构”的规则。
- 审核人填写字段允许配置为任务中心或流程清单展示字段；未填写前显示为空或字段默认值，填写后显示最新提交值。

### 5.7 富文本编辑框

- 表单设计器增加富文本编辑框控件，主要用于自由协作流程的初始说明，也可用于其他需要长内容的初始表单。
- 富文本编辑器使用 Tiptap，不自行实现底层编辑器。
- 首版支持段落、标题、加粗、斜体、删除线、有序列表、无序列表、引用、链接、图片和视频。
- 富文本不作为流程清单列、任务中心动态字段或高级查询条件；列表只读取单独配置的结构化摘要字段。
- 正式实现中富文本保存 Tiptap JSON，并按需生成经过安全过滤的 HTML；原型可以使用 HTML 模拟展示。
- 图片、视频和其他媒体保存到内网附件服务，富文本仅保存受权限保护的文件标识或访问地址，不把 Base64 文件正文存入数据库。
- 交互原型中的富文本媒体正文保存到独立 IndexedDB 文件仓库，富文本 HTML 只保存 `data-media-id` 引用；页面加载时按引用生成临时 Blob URL，并在卸载时释放。无法写入或读取浏览器文件仓库时必须明确报错，禁止退回 Base64/localStorage。
- 服务端需要过滤不安全 HTML、脚本、事件属性和危险链接，并对媒体文件执行扩展名、内容类型、大小及访问权限校验。

## 6. 文档审核流程示例

首版原型应内置一个用于演示的文档审核流程：

1. 文控填写初始表单并上传 PDF 文件。
2. 研发、质量、生产进入并行审核。
3. 演示节点可分别配置为审批或确认；审批人可以通过或驳回，确认人只能确认。
4. 研发、质量、生产必须全部通过或确认，当前审核轮次才通过并完成流程。
5. 任意一方驳回后，当前审核轮次立即结束，其他尚未处理的并行待办自动取消，流程进入“驳回待处理”。
6. 驳回意见必填，通过意见和确认说明选填。
7. 实际创建该实例的文控可以修改表单、按需更换 PDF，并重新提交流程实例；同一发起流程权限组中的其他普通成员不能代为编辑或重新提交，超级管理员除外。系统创建新审核轮次，研发、质量、生产全部重新审核。
8. 关闭流程权限组的有效成员可以在任意状态关闭流程；驳回待处理时仍受所选驳回处理方式限制。
9. 流程详情应展示当前 PDF、审批进度、审核轮次及历史操作记录。

- 流程详情中的“流程进度”必须读取实例锁定版本的节点和连线，不得把全部审核人简单视为同一组并行节点。界面按拓扑层级依次展示“开始、步骤 1、步骤 2……、结束”；同一层存在多个实际执行节点时才标记为并行，单节点层标记为顺序处理。
- 流程详情中的“流程进度”整体放入独立折叠面板并默认收起，用户按需展开查看拓扑；折叠只影响页面展示，不改变流程状态、待办或审核结果。
- 尚未满足全部前序条件的节点显示“待激活”，待办已经创建的节点显示“待审核”，处理完成、驳回或取消后显示对应状态；默认责任人、实际处理人和代办标记继续按节点任务展示。条件不满足而跳过的节点沿用统一隐藏规则，过滤后某层只剩一个节点时不再错误标记为并行。

流程实例首次发起使用“提交”，驳回后使用“重新提交”，流程定义版本使用“发布”。流程实例不一定包含附件，因此重新提交不能以重新上传文件作为前提：

- 流程发起页面不提供保存草稿。用户填写完成后点击“提交”，系统只弹出简洁的“是否确认提交”提示；确认后在一次原子操作中保存表单并创建流程实例。确认框不重复展示流程版本、预计编号、字段数量、附件数量或节点数量等低价值摘要；未提交的页面内容不形成业务状态。
- 流程实例创建成功后自动返回“流程发起”中心，不停留在已创建实例的详情页；成功状态需要明确反馈，用户可以继续选择其他流程发起。
- 流程实例提交后，只要本轮还没有任何审批节点提交审核结果，实际创建人可以随时修改表单、表格或附件并保存；保存修改不创建新轮次，也不重新生成已有待办。
- 普通用户的实例发起内容编辑权不由发起流程权限组或关闭流程权限组继承。同组其他成员仍可按各自权限查看、审核、代办或关闭，但不能看到或调用“保存修改”“重新提交”等创建人专属操作；超级管理员拥有系统级全部权限，可以修改和重新提交其他用户创建的流程。
- 一旦本轮任一审批节点成功提交通过或驳回结果，当前提交内容立即锁定，发起方不能继续修改，避免审核依据在已有审核结论后发生变化。
- 发起方保存“首人审核前修改”时，后端必须在同一事务中再次检查本轮是否已有审核结果；如果审核人已经抢先提交，应拒绝修改并提示内容已锁定。
- 任一分支驳回后，本轮结束；系统根据流程版本的“驳回后的处理方式”进入“驳回待处理”或直接关闭。进入“驳回待处理”时，提交内容重新开放编辑。
- 实际创建人或超级管理员可以修改表单和表格；如果流程包含附件，可以选择保留原附件或更换附件，不强制重新上传。
- 实际创建人或超级管理员确认“重新提交”后创建新的审核轮次，并按照流程定义重新生成本轮全部审批分支待办，不只重开被驳回的分支。
- 流程未被驳回时不能通过接口绕过页面限制修改已锁定内容；后端需要同时校验实例状态和操作权限。
- 每次重新提交需要保存提交人、提交时间、修改字段标识及当时名称、发生附件变更的附件字段和新轮次编号，并在流转记录中展示；首次创建记录必须始终保留为“流程发起”及原创建时间，不能在进入新轮次后被改写成当前轮次发起记录，也不显示重复且无信息增量的“提交初始表单”说明；不得保存旧值、新值或旧附件名称。

## 7. 自由协作流程

### 7.1 流程定位

- 系统在固定审批流程之外增加独立的“自由协作流程”类型，交互模式类似 GitHub Issue。
- “自由协作”是流程定义的运行模式，不是系统中唯一或固定命名的一条流程；管理员可以创建多条名称、表单和权限配置各不相同的自由协作流程。
- 创建流程定义时需要选择“固定审批流程”或“自由协作流程”。两种类型都先使用同一个表单设计器配置初始表单。
- 固定审批流程完成表单设计后进入流程设计器配置审批拓扑；自由协作流程不进入流程设计器，完成表单、流程权限组和基本规则配置后即可校验并发布。
- 不同自由协作流程分别保存独立的流程定义、表单定义、流程权限组、状态和版本记录，运行实例固定引用发起时使用的定义版本。
- 正式数据模型和接口不得把自由协作流程硬编码为 `free-collaboration`、固定名称或单例记录，应通过流程定义标识与流程运行模式识别。
- 当前交互原型可以只用“自由协作”作为代表性示例，不要求为该架构差异重做现有原型页面。
- 自由协作流程不预先设计审批节点，不使用通过、驳回、重新提交或并行审核等审批术语。
- 自由协作流程使用“创建、回复、变更受理人、异常改派、关闭、重新打开”等操作。
- 自由协作流程不提供打印或导出 PDF；流程清单、详情页及直接打印地址均不得出现可用的打印功能。

### 7.2 创建与受理

- 自由协作流程仍需配置初始表单，发起人填写表单后选择一名首位受理人。
- 受理人从该流程配置的可受理流程权限组中的当前有效成员选择；人员较多时使用服务端搜索与分页。
- 创建成功后生成后台编号，实例进入“进行中”，并为首位受理人生成待办。
- 自由流程始终只有一名当前受理人，不支持多人同时受理。
- 实例锁定版本中发起流程权限组或审批/受理流程权限组的任一当前有效成员，都可以直接选择新受理人执行“变更受理人”，不要求操作人是当前受理人，也不要求填写处理内容或变更说明。超级管理员保留系统级越权资格。
- 协作处理区使用一个随输入状态变化的主按钮：只填写回复时显示“发表回复”，只选择新受理人时显示“变更受理人”，同时填写回复并选择新受理人时显示“回复并变更”；受理人选择支持清空。回复并变更必须作为一个原子命令提交，不能出现回复成功但受理人变更失败的部分状态。
- 变更成功后完成原受理人的待办，为新受理人生成新待办；时间线面向用户简洁显示“受理人变更为某人”及操作人、时间，不显示原受理人，后台仍保留原受理人用于审计。直接变更时独立回复草稿不得被提交或清空；选择“回复并变更”成功后才清空回复内容。
- 不允许将受理人变更为当前受理人本人，但允许变更为任意历史参与人。

### 7.3 回复与编辑

- 发起人、当前受理人和历史参与人可以在事项进行中随时新增多条回复，不要求自己是当前受理人。
- 普通“发表回复”只增加时间线内容，不改变当前受理人，也不完成当前待办。
- 每条回复使用 Tiptap 富文本编辑器，可包含文本、图片、视频和链接。
- 回复作者可以在事项进行中编辑自己提交的任意历史回复；其他用户不能编辑该回复。
- 页面显示“已编辑”，并在回复卡片的主时间位置显示最后编辑时间；首次发表时间保留在提示信息中。回复编辑后只保留最新正文；后台和时间线保存修改人、修改时间及“已编辑”事件，不保存修改前正文或回复版本历史。
- 编辑已有回复成功后必须同步更新事项的最后更新时间；越权编辑、无变化保存或编辑不存在的回复不得只更新事项时间形成伪变更。
- 不允许真正删除时间线回复。
- 事项关闭后所有内容锁定；重新打开后，参与人恢复新增回复权限，原作者恢复编辑本人历史回复的权限。

### 7.4 初始表单修改

- 自由协作事项处于“进行中”时，实际发起人可以修改自己创建的初始表单，包括结构化字段和初始富文本说明。
- 其他参与人和流程权限组成员不能代替实际发起人修改初始表单。
- 初始表单修改需要保存被修改字段的稳定标识及当时名称、修改人和修改时间，并在统一时间线中显示修改摘要；不保存字段修改前值或修改后值。
- 初始表单修改成功后必须同步更新事项的最后更新时间，流程清单与详情页读取同一实例更新时间。
- 事项关闭后初始表单锁定；事项重新打开后，实际发起人恢复编辑权限。
- 修改初始表单不改变当前受理人，也不重新生成待办。

### 7.5 关闭与重新打开

- 关闭流程权限组的当前有效成员可以关闭进行中的自由协作事项；当前受理人、实际发起人不因其身份自动获得关闭资格。
- 关闭必须填写理由；关闭后取消当前待办，实例进入“已关闭”状态且不再存在当前受理人。
- 发起流程权限组成员或任一历史参与人可以重新打开已关闭事项。
- 重新打开必须填写理由并指定一名受理人；成功后实例恢复“进行中”，为该受理人生成待办。
- 创建、回复、变更受理人、关闭和重新打开按照发生顺序展示在同一时间线，例如“用户A创建 → 用户B回复 → 用户B变更受理人 → 用户C关闭 → 用户D重新打开”。

### 7.6 异常改派

- 当前受理人离职、账号停用或无法继续处理时，发起流程权限组的当前有效成员可以强制改派。
- 异常改派必须填写理由并选择新受理人。
- 改派成功后取消原受理人的待办并为新受理人生成待办，不要求原受理人确认。
- 时间线保存原受理人、改派操作人、新受理人、理由和时间。
- 普通历史参与人不能执行异常改派。

### 7.7 状态与时间线

- 自由协作流程只使用“进行中”和“已关闭”两个主要实例状态。
- 自由流程清单中的“当前节点”列在进行中时只显示当前受理人的姓名，不追加“受理中”“自由协作”等说明文字；实例已关闭时该列保持空白。
- 自由流程详情页“协作时间线”中的创建、变更受理人、表单修改、异常改派、关闭和重新打开等流转事件采用单行紧凑样式，依次显示操作人、事件类型、事件摘要和时间；过长理由或变更摘要省略并可悬停查看完整内容。富文本回复仍使用独立内容卡片。
- 详情页右侧的“当前责任”和“参与人员”继续分区展示，不因时间线压缩规则而合并。
- 参与人由发起人、所有历史受理人和已经发表回复的用户组成。
- 初始表单、回复、变更受理人、异常改派、关闭、重新打开和内容编辑记录统一按时间展示。
- 富文本回复只保存最新正文，不保存修改前正文或回复版本历史；审计只保存回复标识、修改人、修改时间和“已编辑”事件，普通时间线显示最新内容和“已编辑”标识。

## 8. 首版交互原型范围

首版预计包含 19 个核心页面或屏幕。

### 8.1 页面清单

#### 员工工作区

1. 登录页：统一输入账号和密码，正式后端按用户配置的“域登录 / 密码登录”方式完成认证并建立会话。
2. 流程发起中心：直接选择允许当前用户发起的已发布流程。
3. 发起流程页：填写动态表单、选择指定审批人、上传 PDF 并提交。
4. 任务中心：包含“我的待办”“可代办”和“我的发起”三个 Tab，不设置“我的已办”Tab。
5. 流程清单页：查看当前菜单对应流程的实例及其表单信息，支持查询、筛选和按当前查询导出 Excel。
6. 流程详情/审批页：展示表单、附件、流程进度和操作记录，并根据节点处理方式和权限显示通过、确认、驳回、继续修改、重新提交或关闭操作。

员工工作区不设置工作台，也不单独设置“我的发起”页面；本人发起且未完成的流程统一收纳在任务中心的“我的发起”Tab。

流程发起中心只展示当前用户有权发起的已发布流程：无权限流程直接不渲染，不提供“仅看我可发起”开关，也不设置关键词、类型等查询栏。当前用户名称和身份只在系统公共顶栏显示，页面内容区不得重复展示。流程入口采用紧凑卡片，保留流程名称、类型、版本、简要说明、发起权限组和发起操作，删除“当前账号可发起”、更新时间等可由上下文推断或非必要的信息。

新建固定审批或自由协作实例统一从“流程发起”进入。流程清单只负责查询、查看和按既有规则复制已完成的固定审批实例，不提供“新建事项”等直接创建入口。

- 固定审批流程的发起页面必须根据发布版本的实际节点连线计算“流程处理”摘要，按拓扑层级依次显示处理阶段；同一层有多个审批或确认节点时明确标记“并行”，节点配置执行条件时标记“条件执行”。不得按节点数组保存顺序简单拼接名称，也不得把串行、并行或确认节点混成无顺序的文本集合。右侧人员选择区使用相同的拓扑顺序。

新流程首次发布并生效后，只要流程未停用且当前用户满足页面权限和任一发起流程权限组资格，就必须立即出现在流程发起中心；流程入口从流程定义状态动态生成，不依赖预置流程名称白名单。

任务中心使用紧凑布局：页面名称只在系统顶栏展示一次，内容区不再重复显示“任务中心”、当前用户名称或身份信息；不设置待办统计摘要卡和临时代办说明卡，直接以“我的待办 / 可代办 / 我的发起”Tab、查询条件和任务列表作为页面主体。

任务中心不设置或展示流程处理时限。任务列表的操作列使用统一的审核图标按钮进入流程详情/审批页面，不显示“去审核”等重复文字；图标按钮需要提供悬停提示和无障碍名称。

任务中心采用“任务归属 + 流程分类”两级导航：

- 顶部一级导航继续使用“我的待办 / 可代办”，切换后重新计算对应任务范围和各流程数量。
- 内容区左侧提供流程分类列表，首项为“全部待办”，其后只展示当前一级导航下待办数量大于 0 的流程，并显示各自待办数量。
- “流程分类”标题不显示流程种类总数；分类项以流程名称为主要信息、流程类型为辅助信息，使用清晰的字号、图标和待办数量徽标。
- 流程分类按照待办数量降序排列；流程超过 8 个时增加分类名称搜索并允许独立纵向滚动。
- 点击具体流程后，右侧任务列表只显示该流程，并以标准单层表格展示公共列及该流程版本配置的全部业务自定义列；不同流程可以拥有完全不同的自定义列集合。
- 点击“全部待办”时只显示实例编号、流程与标题、当前节点、发起人、任务归属和操作等公共列；“流程与标题”单元格在标题下方显示实例锁定版本的流程名称，帮助区分混合流程。该双行展示仅用于“全部待办”，不应用到“全部发起”或单个流程分类；页面不加载、不显示任何流程业务自定义列，也不渲染“流程信息”行或信息带。
- 系统按照用户保存最后选择的流程分类；切换到不存在该分类待办的一级导航时临时回到“全部待办”，原选择仍可在再次可用时恢复。
- 任务列表默认每页 20 条，并允许切换为 50 或 100 条，适配单个流程存在大量待办的情况。

任务中心支持不同流程配置不同的任务列表字段：

- 默认查看全部流程时，保留实例编号、流程与标题、当前节点、发起人、任务归属和操作等通用列，不设置固定宽度的“关键信息”列。
- 全部待办采用标准单层表格，不因某条流程配置了业务字段而追加第二行、展开行、“流程信息”标签或信息带。
- 当用户在任务中心选择一个具体流程后，该流程配置为“在任务中心显示”的业务字段全部展开为独立表格列；列数较多时使用横向滚动，不截断为固定6列。
- 表单设计器中的字段只配置是否“在任务中心显示”，不再设置任务列表显示名称、显示顺序或列宽。任务中心列名始终使用字段名称，业务字段严格按照初始表单中的先后顺序排列。
- 任务中心与流程清单使用同一套受控自动列宽规则：根据系统字段语义或业务字段类型及名称计算宽度，并设置合理的最小值和最大值；超长内容省略并可查看完整值，列较多时只在表格内部横向滚动。操作列继续固定在最右侧，不参与自动宽度计算。
- 任务中心选择具体流程后，系统字段列和业务字段列都按照该流程版本的列表字段配置展示。未选择具体流程时只保留实例识别及操作所需的公共列，避免不同流程的列结构互相干扰。
- 任务列表字段配置跟随流程版本保存；运行中的任务按照实例发起时的流程版本读取配置，旧版本缺少的字段显示“—”。
- 普通选择类和复选字段按照可读文本格式化；附件字段显示文件名或附件数量；表格控件不直接展开全部行，只显示行数或配置的汇总值。
- 任务列表接口只返回通用任务数据及被配置为需要展示的字段，不为列表加载完整流程表单。
- “我的待办 / 可代办”切换需要具有明确的选中状态：选中的“我的待办”使用实心蓝色，选中的“可代办”使用实心紫色，并配合白色文字、高对比计数与阴影；未选中项保持浅灰样式。

#### 流程配置

7. 流程管理列表。
8. 流程基本信息配置页。
9. 初始表单设计器。
10. 可视化流程设计器。
11. 流程发布页（包含发布前检查与完整快照预览）。
12. 流程版本记录页。

流程配置采用流程内向导，不把“表单设计器”和“流程设计器”作为左侧导航中的独立功能入口：

- 左侧“流程配置”只保留“流程管理”。管理员先在流程管理中创建或选择一个正式编号版本，再进入对应设计步骤。
- 流程管理的“新建流程定义”弹窗只负责收集流程名称、类型和说明；点击“继续填写基本信息”属于向导内部的正常交接，应直接带入已填写内容并进入基本信息页，不得再弹出“未保存修改”或离开确认。用户主动从当前页面导航离开时，才按未保存内容处理。
- 固定审批流程的步骤固定为“基本信息 → 初始表单 → 流程设计 → 发布”。
- 自由协作流程同样必须设计初始表单，但不使用节点流程设计器，步骤固定为“基本信息 → 初始表单 → 发布”。
- 各步骤顶部使用统一进度条，并在顶部操作区提供明确的“上一步：步骤名 / 下一步：步骤名”导航；基本信息作为第一步使用页面级“返回”退出向导。保存直接写入当前正式编号版本并自动校验，不能读写全局示例数据。
- 基本信息页和初始表单设计页不展示“校验未通过/可发布”等版本校验状态文本；校验状态只在流程设计页和发布页展示，具体阻断问题在发布页集中说明。
- 流程设计器中的独立检查动作命名为“检查流程”，下一步统一命名为“下一步：发布”，避免“发布校验”和“下一步：发布校验”被理解为两个不同页面或动作。
- 发布动作只在统一的“发布”页面执行；页面内部使用“发布前检查”描述阻断项和警告项。保存版本与发布版本是两个动作，表单设计器和流程设计器不提供重复的发布入口。
- 全新流程定义首次发布成功后，页面在提示成功前必须从统一流程定义状态中再次确认 `publishedVersionId` 已指向目标版本，并返回该流程的版本记录页显示目标版本“已发布”；不得出现发布成功后流程定义仍显示“未发布”的前端状态。
- 发布页的表单快照、审批拓扑、节点名称、连线层级、节点流程权限组、审核可修改字段和驳回规则必须读取当前正式编号版本，不得继续显示固定的 PDF 三部门示例数据。
- 新流程发布后，员工侧发起页面必须读取该流程当前发布版本对应的初始表单、审批拓扑、流程权限组和驳回规则；内置 PDF 与测试报告可以保留专用高保真发起页面，但未内置的新流程不得回退到任何专用模板。流程定义或发布版本不存在时应明确提示不可发起，而不是静默打开 PDF 审核页面。
- 版本记录不属于线性配置步骤，可从流程管理独立进入；未发布且无实例的版本提供继续编辑，任意版本都提供“复制新建版本”，当前发布版本额外提供“取消发布”。

#### 用户与权限

13. 用户管理页。
14. 部门管理页：通过“部门架构 / 职务管理”页签分别维护最多两级的部门树，以及全局职务字典的新增、重命名、排序、启停和删除。
15. 角色管理页。
16. 权限管理页。
17. 流程权限组管理及成员维护页。

用户、角色和流程权限组管理页需要支持以下界面：

- 用户列表展示登录方式；用户新增和编辑时可选择“域登录 / 密码登录”，普通用户默认域登录。域登录不填写本地密码且不能执行密码重置；密码登录新增或由域登录切换时必须设置密码。超级管理员固定为密码登录且该字段只读。
- 用户和角色列表提供删除操作，系统内置项禁用删除。点击后先说明引用检查范围，再提交带最新 `ETag` 的删除请求；服务端返回引用冲突时直接展示简洁中文原因，不在前端缓存中自行推断可删除性。
- 用户编辑时选择一个一级或二级部门节点，并从职务字典中选择一个启用职务。
- 用户编辑时使用支持远程搜索的角色多选控件，一个用户可选择多个角色。
- 角色编码作为系统内部稳定标识由后端自动生成并保持全局唯一，创建后不可修改；角色管理列表、新增/编辑抽屉和权限管理角色选择器均不向普通管理员展示角色编码。
- 角色名称必须全局唯一，并作为普通管理员识别和选择角色的主要名称；系统内部关联、接口和审计记录仍使用角色编码或内部标识。
- 角色新增和编辑抽屉只维护角色名称、说明、成员及状态，不展示内部角色编码，也不重复提供粗粒度的“权限范围”选项；新建角色默认不拥有任何页面或动作权限。
- 角色新增和编辑抽屉中的成员维护使用独立成员选择区，不把大量已选成员压缩在一个多选输入框中。选择区显示已选人数，支持按姓名、部门或职务搜索，支持“全部成员/已选择”切换、批量选择当前结果、清空已选和独立纵向滚动。
- 成员候选项直接显示完整姓名、部门和职务；正式版本在200+用户场景下采用服务端搜索与分页，已选择成员仍可单独查看和取消选择。
- 权限管理页是角色页面权限和动作权限的唯一配置入口。角色列表提供“配置权限”操作，进入权限管理页时自动选中对应角色；切换角色时加载各自独立的权限矩阵。
- 权限管理页在顶部使用支持搜索的角色下拉框切换当前配置角色，并显示角色名称和停用状态，不显示内部角色编码；通过角色列表进入时按路由参数自动选中对应角色。
- 权限管理顶部直接以角色下拉框作为当前角色标识，不再额外显示“当前配置角色”等重复说明文本。权限矩阵首版条目数量较少并完整展开，不提供单独的模块或页面搜索框。
- 权限矩阵发生修改后，页面明确显示“有未保存修改”，并仅在存在修改时启用“恢复”和“保存权限”。未修改时切换角色或离开页面不额外提示。
- 权限矩阵采用简化动作模型：资源维护页面原有的“新增/新建、编辑、启停/停用”统一合并为“编辑”权限；拥有“编辑”即可创建、修改及启用或停用该页面管理的记录。
- 权限矩阵的动作权限控件使用紧凑的四列固定网格排列；同一页面实际存在的“查看、编辑、发布、授权、删除”等复选框必须全部位于一行，并保持较小且一致的水平间距及跨行上下对齐，避免动作列换行或随可用宽度拉伸。
- 权限目录只保留能独立控制真实页面或操作入口的权限。表单设计器只保留“编辑”，不设置没有只读页面实现的“查看”和没有独立入口的“预览”；实例监控和操作审计首版只保留“查看”，不设置页面中不存在的“导出”。旧浏览器权限记录读取时删除这些失效权限，升级不得补回管理员曾主动移除的用户或角色删除权限。
- 发布流程、角色授权、重置密码、删除、导出、审核和驳回等具有独立业务含义或更高风险的动作继续单独授权，不并入“编辑”。
- 用户、角色、部门/职务和流程权限组删除分别使用 `org-user:删除`、`org-role:删除`、`org-department:删除`、`org-group:删除`，均加入权限矩阵；拥有普通编辑权限不能替代删除权限。
- 升级已有角色权限时，只要角色原来拥有同一页面的“新增/新建、编辑、启停/停用”中的任意一项，就迁移为该页面的“编辑”权限，并去重旧动作，避免升级后权限意外丢失。
- 浏览器 Mock 升级权限存储版本时保留各角色已保存的自定义权限；只有升级前拥有全部既有权限的系统管理员自动补齐新增加的独立删除权限，其他角色不得因升级被自动扩权。
- 存在未保存修改时切换角色，必须提供“保存并切换、放弃修改并切换、取消”；通过系统导航离开页面时提供“保存并离开、放弃修改并离开、留在当前页”；刷新或关闭浏览器使用浏览器原生未保存提醒，不得静默丢失或静默保存权限修改。
- 角色列表中的“页面权限”和“动作权限”数量必须根据已保存的权限矩阵计算，不允许维护一套与权限矩阵无关的统计值。保存权限后统计同步更新。
- 勾选页面内的非查看动作时自动授予该页面的查看权限；取消页面查看权限时同时取消该页面下的其他动作，避免出现有操作权限但无法访问页面的无效组合。
- 权限矩阵在“员工工作区”增加“流程发起”页面，包含“查看”和“发起”动作。用户只有同时拥有这两个角色权限且属于具体流程的发起流程权限组，才能显示流程发起菜单并提交对应流程；缺少页面权限时隐藏菜单且直链返回403。
- 流程权限组编辑时分别提供“直接成员”和“关联角色”两个配置区域，二者不能混为一个无法区分来源的人员列表。
- 页面提供“有效成员预览”，展示去重后的成员数量、姓名、账号状态以及成员来源；200+ 用户时使用服务端分页和搜索。
- 流程权限组编辑页中的“有效成员预览”直接显示成员完整姓名，不使用仅显示姓名末字的头像图标代替姓名；成员来源使用“直接加入”“角色带入：角色名称”等文字标签明确表达。预览支持按完整姓名搜索，并在成员较多时使用独立滚动区域。
- 流程权限组列表的状态列需要为统一状态标签预留完整宽度并居中展示，标签的圆角边框、状态圆点和文字不得被相邻单元格裁切。
- 流程权限组列表不展示“运行待办”数量；停用等可能影响运行任务的操作仍在确认提示中说明影响。
- 关联角色选择器展示角色名称、有效用户数和状态；停用角色不可新增选择。
- 已被关联角色自动纳入的用户不能在“直接成员”区域单独删除；界面应提示需要移除角色关联或调整用户角色。

#### 系统运维

18. 流程实例监控页：只允许查看和查询，不提供强制关闭、重新指派、跳过节点等干预操作。
19. 操作审计日志页：直接展示查询条件和日志列表，不设置“今日操作、失败操作、敏感变更、日志保留”等概览统计卡片。面向用户的列表和详情使用简洁中文展示模块、动作、操作内容、结果和变更字段摘要，不显示事件 UUID、资源内部标识、账号编码、英文动作码、IP 占位列或原始 JSON；底层不可变审计仍完整保留内部标识和结构化详情，供后端追溯与授权查询使用。

新增、编辑、成员选择、通过、驳回、重新提交、关闭和发布确认等操作优先使用抽屉或弹窗，不额外计算为独立页面。

“流程清单”页面至少支持按实例编号、标题、发起人、状态、发起时间和当前节点进行基础查询，并可以使用该流程中除固定标题外标记为“允许作为查询条件”的表单字段进行高级查询。标题只出现在基础查询中，不作为高级查询字段重复出现。列表可以展示标记为“在流程列表中展示”的动态表单字段，进入详情后可以查看当前用户有权查看的完整表单信息。

- 流程清单提供“导出 Excel”，导出范围为当前流程、当前用户数据权限和全部已应用查询条件共同筛选后的全部结果，不受当前表格分页限制；导出只包含当前版本配置为“导出到 Excel”的系统字段与业务字段。
- Excel 文件统一在浏览器中生成真正的 `.xlsx`。ASP.NET Core 后端只负责重新校验页面权限、流程数据范围及全部查询条件，并返回列定义、文件元数据和全部匹配行；后端不得生成、保存 Excel 文件，也不得返回 Excel 文件或临时下载地址。
- 导出数据接口只返回当前用户有权查看、且当前版本配置为“导出到 Excel”的字段，不得因浏览器生成文件而放宽字段或实例数据权限。历史实例缺少当前版本字段时返回空单元格。
- 单次 Excel 导出最多 `10000` 行；超过上限时后端返回明确错误，页面提示用户缩小查询范围。该限制用于避免浏览器生成大型工作簿时长时间占用内存或页面失去响应。
- 导出期间按钮显示加载状态，并显示“正在查询导出数据并生成 Excel”等待提示；成功后提示后端实际返回的导出条数，失败时说明原因，当前查询无结果时不生成空文件。
- 所有包含时间范围的查询列表统一默认“之前 30 天至今天”，起止日期不可清空；用户点击重置后恢复该默认范围。流程清单、流程实例监控和操作审计的时间范围必须真正参与结果筛选，不能只展示空控件。

流程清单始终由左侧子菜单固定为单个流程定义，因此其系统字段列和业务表单字段列均直接按照对应流程版本的列表字段配置生成。字段被配置为隐藏只影响列表展示，不影响详情页数据、查询权限、审核记录或打印内容。

“流程清单”页面使用紧凑布局：页面名称只在系统顶栏展示一次，内容区不再重复显示“流程清单”、说明文字或当前数据范围摘要，直接以查询面板和流程实例列表作为页面主体。

左侧导航中的“流程清单”是不可直接进入的父菜单，其下按当前用户有权查看的流程定义显示子菜单，例如“PDF审核”“测试报告审核”。不提供“全部流程”子菜单。父菜单图标需要与“任务中心”等同级菜单图标保持相同的起始位置、尺寸和间距。

- 流程子菜单按流程定义生成，不按流程版本生成；同一流程存在 V1、V2 等多个版本时只显示一个菜单入口。
- 点击流程子菜单后进入公用的流程实例列表页面，并由菜单固定当前查询的流程定义。
- 页面展示当前流程定义下、当前用户有权查看的全部版本实例，版本号作为列表字段和可选查询条件。
- 无论列表中实例来自哪个历史版本，页面列结构和动态查询条件都以该流程当前发布版本为准；版本筛选只过滤实例范围，不切换回历史版本的列表布局。
- 历史实例不存在当前列表字段时单元格显示为空；当前版本已经删除的历史字段不再作为列表列显示，但仍可进入详情按实例所属版本查看。
- 因当前流程已经由菜单确定，页面中不显示“流程名称”查询框。
- 流程子菜单按用户的流程查看权限和数据范围生成，无权查看的流程不显示。
- 菜单和路由使用稳定的流程定义标识，不使用可能变化的流程名称或版本号作为关联标识。
- 自由协作流程也按照流程定义生成独立子菜单。例如管理员创建“生产异常协作”和“客户问题跟踪”两条自由协作流程时，“流程清单”下分别显示两个菜单；同一自由流程的多个版本仍合并为一个菜单入口。
- 流程实例列表中的状态统一使用等高、等宽视觉规格的浅色胶囊标签；每种状态都包含状态圆点、语义文字、浅色背景和细边框，只通过语义颜色区分“审核中、驳回待处理、已完成、已关闭”，不得混用有背景与无背景样式。
- 流程清单的操作列使用与任务中心相同视觉规格的图标按钮，使用“查看流程”图标语义，不显示重复的“查看”文字；按钮需要提供悬停提示和无障碍名称。
- 流程清单中状态为“已完成”的流程实例提供“复制新建”操作。该操作仅对同时拥有“复制新建”动作权限和目标流程发起资格的用户开放。
- 点击“复制新建”只把已完成实例中发起人可填写字段的最终内容带入目标流程当前发布版本的发起编辑页，不得直接创建流程实例、生成实例编号或创建审批待办。用户可以在编辑页继续修改，确认后点击“提交”才正式创建新流程；“发布”仅用于流程定义版本，不作为运行实例的提交按钮文字。
- 复制预填默认且固定不复制原附件，界面不提供复制附件选项；同时不复制原实例编号、审批人员选择、审批任务、审核记录、审核结果、轮次和流转历史。用户需要在编辑页重新上传所需附件并重新选择需要指定的处理人员。
- 复制新建的目标实例始终使用目标流程当前发布版本；源实例与发布版本不同时，仅复制稳定字段标识相同且控件类型兼容的普通字段和表格列。当前版本已删除的字段忽略，当前版本新增字段使用默认值或留空，新增必填字段必须由发起人补充后才能提交。
- 表格数据按照稳定表格标识和稳定列标识映射；表格或列类型不兼容时不复制对应内容，并在复制结果页明确提示未带入的字段数量。
- 复制创建的新实例由后端生成新的实例编号，从第1轮开始并重新生成全部审批分支待办。
- 新实例创建后处于本轮尚无人审核的状态，发起方仍可继续修改并保存；首位审批人提交结果后按照既定规则锁定。
- 复制编辑页提交时必须把来源实例稳定标识随新建请求传给后端；后端在创建新实例的同一事务中重新校验来源实例仍为“已完成”、来源与目标属于同一流程定义、当前用户仍有来源查看权限、“复制新建”动作权限和目标流程发起资格。任一条件不满足时不得生成实例、编号或待办，不能只根据前端按钮和预填页面控制。
- 流程清单操作列和流程详情页提供“打印为 PDF”功能。原型通过系统打印窗口选择“另存为 PDF”，正式实现需要提供统一的打印视图。
- 打印内容包含流程实例编号、流程名称及版本、状态、发起人、发起和更新时间、当前轮次、当前表单字段和表格数据、节点处理方式、审批/确认进度、审核意见或确认说明，以及审核过程中涉及的字段名称；不设置独立的“流程流转记录”章节，不打印字段历史旧值、新值或旧回复正文，“确认 / 已确认”不得显示为“通过 / 已通过”。
- 打印章节标题只显示内容名称，不添加“一、二、三、四”等中文序号。打印内容不展示流程定义中的“节点处理与通知配置”配置表，不输出允许重复修改、邮件通知开关或通知收件人等设计配置；实际发生的审批或确认方式、处理结果、处理人、时间与说明仍作为审核记录保留。
- 打印文档页眉只显示流程实例标题作为唯一主标题，不额外显示“流程审核记录”等固定标题，也不在主标题下方重复显示实例标题；流程实例编号继续独立显示。
- 打印页的“审批进度与审核信息”只显示已经由人员实际提交审批或确认结果的节点；待处理、未激活、已取消、按条件跳过以及仅配置了权限组或默认责任人但尚未处理的节点均不显示。没有任何实际审核记录时，整个章节不显示。
- 打印页的“审批进度与审核信息”按实际处理完成时间升序排列；处理时间相同或历史异常数据缺少完成时间时，再按轮次和节点名称提供稳定顺序，不能按流程图位置代替实际时间顺序。
- 打印页不再单独设置“审核修改记录”章节。某个实际审核节点存在表单字段修改时，将该节点历次修改和随审核结果提交的字段名称合并去重后，显示在对应审核记录的“处理说明”中；没有字段修改时不显示该项，且始终不打印修改前后的字段值。
- 附件正文、附件图片和 PDF 预览页面不进入打印内容；打印版按实例锁定版本的附件字段读取当前最终值并显示附件名称，可以附带文件大小、类型等附件元数据，不得继续使用可能过期的历史快捷字段覆盖审核后替换的名称。表单没有附件字段时不生成“附件清单”章节。
- 打印操作遵循与流程详情相同的数据查看权限；不能通过打印入口绕过流程可见范围或附件名称访问权限。
- 打印内容以用户执行打印时有权查看的流程实例当前数据为准，并使用适合 A4 页面、分页和黑白打印的布局。
- 打印使用独立的流程打印页面，不直接打印或通过 CSS 裁剪交互详情页。打印页面使用纯文本、标准表格和分页友好的结构重新呈现数据，确保表单控件值、审批信息和长文本在 PDF 中稳定输出。
- 打印前提供预览页面，由用户确认内容后调用系统打印窗口；打印视图必须隐藏导航、查询、审批按钮和其他交互控件。

原型应支持切换模拟用户或角色，以演示文控、研发、质量、生产和管理员的不同操作。

### 8.2 200+ 用户的界面要求

- 用户管理列表使用服务端分页，不在页面初始化时一次加载全部用户。
- 用户列表支持按姓名、账号、部门、状态等条件搜索和筛选。
- 角色成员和流程权限组成员维护使用可搜索、可分页的选择界面，支持批量添加与批量移除。
- 流程权限组成员维护界面需要清晰显示已选人数、当前成员和待选用户，翻页或搜索后不得丢失已选结果。
- “发起时指定人员”只查询所选流程权限组的有效成员，并支持输入姓名或账号快速搜索。
- 人员下拉框或列表需要使用远程搜索、按需加载或虚拟滚动，避免渲染全部用户选项。
- 人员搜索输入需要防抖，避免每次按键都产生重复请求。
- 用户和成员接口需要支持分页、关键字、状态、部门、角色及流程权限组等过滤条件。
- 用户姓名、账号、部门、状态及流程权限组成员关系等常用查询字段需要建立合适的数据库索引。
- 所有人员关联均保存稳定的用户唯一标识，姓名和账号仅用于展示，避免人员信息变更后关联失效。

### 8.3 原型制作方式

- 采用 React + TypeScript + Ant Design 直接制作可运行、可点击的交互原型。
- 不以 Figma 设计稿作为开始开发的必要前置条件，避免原型确认后重新完整开发页面。
- 原型使用与正式前端相同的组件和页面结构，后续可以直接连接 ASP.NET Core 与 Microsoft SQL Server。
- 第一轮先完成6个代表性页面：登录页、任务中心、流程清单、流程详情/审批页、初始表单设计器、可视化流程设计器。
- 第一轮重点确认主导航、页面布局、视觉规范、查询交互、审批交互及两个设计器的基本操作方式。
- 第一轮确认后，再将相同设计语言和公共组件扩展到其余页面。
- 当前交互原型已覆盖本节定义的全部19个核心页面；新增页面使用真实结构的模拟数据，并具备筛选、分页、抽屉、弹窗、表单校验、状态切换或只读详情等与场景相符的交互，不使用仅有标题的占位页面。
- 原型导航按演示身份显示流程发起、流程配置、用户与权限、系统运维等菜单；隐藏菜单的同时必须对直接访问路由再次校验权限，并展示明确的403状态。
- 原型阶段可以使用真实结构的模拟数据演示不同角色、流程权限组、待办、代办和流程状态。
- 第一轮原型需要在浏览器刷新后保留当前演示身份、审批/驳回/关闭/重新提交结果以及两个设计器已保存的正式版本配置。`pnpm dev` 和 `pnpm build:debug` 中，只要当前生效身份或真实登录操作者是系统内置超级管理员就显示“重置演示数据”入口，普通用户不显示；`pnpm build` 正式构建不显示该入口，也不提供重置演示数据的正式后端接口。
- 全部页面使用统一的高完成度企业中后台视觉体系：主次文字对比明确，导航选中状态清晰，卡片、查询区、表格、操作区和状态标签具有稳定一致的层级、边界、间距和反馈。
- 所有用户界面中的日期时间统一显示到分钟；包含日期时使用 `YYYY-MM-DD HH:mm`，仅显示当日时间的紧凑位置使用 `HH:mm`，均不显示秒、毫秒、ISO 8601 的 `T` 或时区后缀。列表、详情、时间线、校验时间、审计日志、设计器保存状态、打印页面和带时间的查询控件均遵循此规则。接口、数据库、审计原始值、排序和并发判断仍保留原始时间精度，不能因界面截断而丢失数据精度。
- 全部列表查询区采用统一的底部留白：标准查询卡中，查询控件或查询表单行至卡片下边界保留24px；直接嵌在列表顶部的紧凑查询工具栏保留20px。查询项换行时不得挤在一起，行间距统一不小于16px。
- 视觉增强以清晰度和信息密度为目标，使用克制的阴影、边框、背景层次和品牌色；不改变既有信息架构，也不使用影响阅读的装饰效果。
- 应用左上角和登录页使用统一的横向纯文字 `MOONS'` 品牌字标；以清晰的现代粗体、克制的品牌色强调和简洁装饰线形成识别，不模拟或重绘公司图形商标。继续显示 FlowPilot 流程审核中心副标题，不再显示独立的字母 `M` 图标、“公司内网 · 生产环境”及“原型”环境标签。
- 普通业务页面、审批详情、自由协作详情、登录页、表单设计器和流程设计器共用同一套颜色、圆角、阴影与交互反馈规范。
- 页面级返回入口统一使用左箭头和“返回”文案，并采用相同尺寸、边框、圆角及悬停反馈；各页面可以按来源保留不同返回目标，但不得在按钮上重复展示“返回流程管理”“返回流程清单”“返回发起中心”等不同文案。流程配置向导内部的步骤切换统一使用“上一步：步骤名”，不属于页面级返回入口；弹窗内的“返回检查”“返回编辑”等上下文操作也可以保留明确语义。
- 流程配置向导的步骤名称与顺序由同一份公共定义维护：固定审批为“基本信息、初始表单、流程设计、发布”，自由协作为“基本信息、初始表单、发布”；页面不得单独复制步骤数组或混用“表单设计”和“初始表单”。
- 相同业务状态跨流程清单、任务中心、流程详情、流程管理、版本记录及运维页面使用统一的胶囊标签和颜色语义：进行中、待处理或可发布为蓝色，驳回、失败或校验未通过为红色，完成、通过、已发布或启用为绿色，关闭、取消、未发布或停用为灰色。类型、数量、版本号等非状态信息继续使用普通标签，不与状态标签混用。
- 列表操作列统一使用图标按钮、悬浮说明和可访问名称；图标按钮的尺寸、圆角、边界与悬停反馈一致，复制等少数具有明确语义的操作可以保留独立颜色，但不得改变基本几何规格。
- 登录后的页面标题只在系统顶栏展示一次；页面内容区不得重复同名大标题。详情页可以展示具体实例标题，设计器内部工具栏可以展示当前流程名称和版本，但不能再次重复“表单设计器”“流程设计器”等页面名称。
- 普通审批详情与自由协作详情使用一致的“返回操作、实例摘要、主体内容”层级；自由流程可以保留时间线结构，但卡片边界、页头摘要、操作按钮和状态标签需与普通流程一致。
- 表单设计器和流程设计器使用相同高度的流程工具栏、统一三栏面板边界、圆角、间距和阴影；工具栏左侧显示当前流程及版本，右侧显示保存、校验或下一步等操作。
- 在常见桌面宽度下页面不得产生无意义的整页横向滚动。宽数据表可以在卡片内部横向滚动，设计器画布可以在自身区域内缩放或滚动，但不得推动整个系统框架横向溢出。
- 登录页和打印页允许根据场景使用独立布局：登录页保留品牌展示与登录卡，打印页保留 A4 文档结构；两者仍使用统一的品牌色、字体、表单控件和按钮规范。
- 上述持久化仅用于交互原型演示，可以使用浏览器本地存储；正式系统接入 ASP.NET Core 后，业务数据与配置以服务端数据库为准。

## 9. 技术方案

### 9.1 基础技术

- 前端框架：React + TypeScript
- 前端构建：Vite
- 包管理器：pnpm，项目中不使用 npm 或 yarn 管理依赖
- UI 组件库：Ant Design
- 运行数据库：Microsoft SQL Server
- ORM 与数据访问：EF Core 10 + `Microsoft.EntityFrameworkCore.SqlServer` + `Microsoft.Data.SqlClient`；支持 Microsoft SQL Server 2016 SP2 及之后版本，数据库兼容级别不低于 130，共享实现以 2016 SP2/兼容级别 130 为最低能力基线
- 后端框架：.NET 10 / ASP.NET Core 10 Web API，采用 Controller 架构
- 流程设计器：React Flow
- 表单拖拽与排序：dnd-kit
- 动态表单状态与校验：React Hook Form + Zod
- 设计器状态管理：Zustand
- 富文本编辑器：Tiptap（基于 ProseMirror）
- 邮件发送：`MailKit`/`MimeKit`（连接公司内网 SMTP）
- HTTP 客户端：前端生成客户端使用 `axios`；后端如需访问已登记的远端服务，统一使用 `IHttpClientFactory`

### 9.2 UI 组件库选择说明

选择 Ant Design，原因如下：

- 面向企业级中后台场景，适合流程管理平台。
- 已提供表单、级联选择、上传、表格、抽屉、弹窗、人员选择基础控件等常用能力。
- TypeScript 支持完善，适合与 React Flow、dnd-kit 组合使用。
- 支持主题定制，可以在保留成熟交互能力的同时形成项目自身的视觉风格。

### 9.3 配置数据原则

- 流程定义和表单定义均采用 JSON 数据结构。
- 流程设计器只负责编辑流程定义，不直接绑定某一种后端流程引擎。
- 表单设计器负责生成表单定义，运行时表单负责解释和渲染定义。
- 首版只使用 Microsoft SQL Server 保存流程配置、表单配置、流程实例和审批记录等结构化数据，不实现 SQLite 运行时或数据库切换配置。
- 浏览器本地数据仅用于未提交的临时编辑状态，不作为业务数据的正式数据源。

#### 9.3.1 统一领域模型与运行时数据链路

- 用户、角色和流程权限组属于共享身份主数据。用户通过稳定用户 ID 关联多个角色；流程权限组通过稳定组 ID 关联直接成员和角色，运行时动态计算有效成员，不在流程版本中复制人员名单。
- 流程定义使用稳定且防碰撞的定义 ID 表示左侧菜单和版本家族；定义 ID 不得只依赖毫秒时间戳，快速连续创建或复制流程也必须生成不同标识。每个正式编号版本都保存一份完整、自包含的配置快照，快照至少包含基本信息、表单字段、系统列表及 Excel 导出字段、审批节点、连线、节点权限组、处理方式、审核可修改字段、重复修改、邮件通知和驳回规则。
- 设计器页面的未保存输入只属于临时界面状态；点击保存后直接更新当前正式版本并自动校验。发布、发起、任务中心、流程清单、详情和打印不得把未保存输入作为事实来源。
- 流程实例必须保存稳定实例 ID、流程定义 ID、发起时版本 ID、发起人 ID、表单值和附件元数据。实例创建后永久锁定原版本；当前发布版本变化只影响后续新实例和流程清单的列/查询口径。
- 固定审批实例创建时，由锁定版本的拓扑展开节点任务。任务保存稳定任务 ID、实例 ID、版本 ID、节点 ID、流程权限组 ID、默认责任人、实际处理人、轮次、处理动作和状态；任务状态至少区分“未激活、待处理、已完成、已取消、已跳过”。
- 实例审核记录、待办和流程版本节点必须使用同一稳定节点 ID。旧原型数据中的 `rd`、`qa`、`production` 等历史键在读取或持久化迁移时，优先按流程权限组映射到实例锁定版本的真实节点 ID，并同步节点名称、流程权限组和有效默认责任人；不得依赖节点数组下标或在详情页临时猜测。迁移后任务提交、进度、打印和审核记录统一读取规范化数据。
- 只要仍存在运行中或历史实例，其锁定的流程版本就必须保留完整、自包含且可解释的表单和流程拓扑，即使该流程定义当前没有发布版本。取消发布只阻止新实例，不得导致已有实例因版本快照缺失而无法查看或处理。
- 运行时按连线解释拓扑：审批节点的全部审批前置节点通过、确认或跳过后才激活；同一节点的多条出线自动并行，多条入线按 AND 汇聚；任一节点驳回时取消本轮其他未完成任务，并按实例版本快照中的驳回规则进入重新提交、仅重新提交或自动关闭。
- 发起实例必须在一个事务或等价原子命令中完成版本锁定、编号生成、表单保存、任务展开、实例计数和时间线；首次审核/确认提交必须原子保存授权字段修改、处理结果、实际处理人、后续任务激活、实例状态和时间线；重复修改必须更新同一任务并追加不可变的修改事件。修改事件只记录字段标识、当时名称、修改人、时间、节点、轮次和说明，不保存旧值或新值。
- 菜单、页面路由和领域命令使用同一套多角色权限并集；流程发起、审批/受理、关闭和查看还必须叠加相应用途的流程权限组或额外可见范围。详情、打印和附件访问每次都按实例数据范围重新校验。
- 当前交互原型允许使用带 schema 版本和迁移函数的 Zustand/localStorage 仓库模拟上述聚合和原子命令；正式 ASP.NET Core 实现必须把同一领域边界迁移到服务端事务、数据库外键和接口鉴权，不能依赖前端本地状态形成安全边界。
- 正式 REST API 的统一基础路径为 `/api/flowpilot/v1`，其中 `flowpilot` 是系统命名空间，`v1` 是接口主版本；不得使用拼写错误的 `/api/flowpiolt/v1` 或缺少系统命名空间的 `/api/v1`。在正式后端完成前，前端开发与演示环境使用 MSW 提供相同的领域端点和 HTTP 语义；可部署 Debug 包继续使用只在页面内拦截的 `/flowpilot/mock-api/v1` 专用地址，该地址不属于正式 API，也不发送到 IIS。页面统一通过 `flowPilotApi` 访问服务；目标传输层由 Orval 根据正式 OpenAPI 生成 TypeScript 类型和 Axios 客户端，过渡期兼容层可以保留现有 `fetch` 实现，但页面不得直接调用 `fetch`、Axios 或 Mock handler。Mock API 需要覆盖会话、组织权限、流程定义与版本、发起配置、实例、审批任务、自由协作、附件、邮件 Outbox、审计和 Excel 导出，并提供分页、Problem Details、ETag/If-Match、幂等键以及可控延迟和错误场景。
- Mock API 是浏览器内兼容适配层，允许暂时复用带迁移的原型仓库；正式后端契约以仓库中的 OpenAPI 3.1 文档为准。Mock Bearer 身份、浏览器持久化和模拟邮件状态只用于原型，不能作为生产认证、数据库、文件存储或 SMTP 的替代方案。浏览器生成 Excel 则是正式架构要求，正式后端同样只提供已鉴权导出数据集。
- Debug Mock 可以继续使用统一成功响应包；正式 OpenAPI 接口直接返回 DTO。前端传输适配层必须同时正确解析两者，并把正式接口中的分页元数据、嵌套用户/组织引用、状态枚举和流程完整版本快照转换为共享领域模型，页面不得依赖仅由 Mock 提供的私有聚合接口。
- 正式模式启动只加载当前会话、权限、当前用户必要的角色/流程权限组和少量组织字典；任一关键请求失败时保留原缓存并使登录失败，不得用空数组覆盖持久化数据。用户、实例、任务、审计、邮件 Outbox 等持续增长的数据必须由页面服务端分页、筛选和排序，不得在登录时全量下载到 Zustand。流程定义列表只返回摘要，进入设计器、发布页、详情或历史实例时按需读取完整版本。
- 需要乐观并发的编辑页面在打开实体时保存该次 GET 返回的 ETag，并在用户提交时原样写入 If-Match；不得在提交前重新 GET 最新 ETag 来绕过过期编辑冲突。服务端返回 412 后由用户重新加载数据再编辑。
- 页面权限与动作权限使用同一份权限目录；新增、编辑、启停、发布、授权、重置密码和删除等独立控件同时受动作权限控制。流程清单导出属于已授权列表查询的输出形式，继续由流程清单查看权限和服务端数据范围共同约束，不额外设置空置的运维导出权限。角色权限修改后必须刷新当前服务端会话权限，避免菜单、路由与按钮继续使用旧授权。

### 9.4 正式后端与 SQL Server 持久化

正式后端使用 .NET 10、ASP.NET Core 10 Controller Web API 和 REST API；前端继续作为 pnpm workspace 管理，后端位于同仓库 `apps/api`，但使用独立的 .NET solution/project 与 NuGet 依赖。OpenAPI 3.1 文档仍是接口契约的唯一事实来源。

- 生产目标框架固定为 `net10.0`。ASP.NET Core 使用共享框架；直接 NuGet 依赖保持最小集合并锁定版本，至少包括 `Microsoft.EntityFrameworkCore.SqlServer`、`Microsoft.EntityFrameworkCore.Design`（仅设计/开发）、`Microsoft.Data.SqlClient`、`Microsoft.Extensions.Hosting.WindowsServices`、`Microsoft.AspNetCore.OpenApi`、`System.DirectoryServices.Protocols`、`MailKit`、`Serilog.AspNetCore`、`Serilog.Sinks.File` 和编译期映射生成器 `Riok.Mapperly`。
- 不引入完整 ASP.NET Core Identity 数据库模型、Dapper、AutoMapper、MediatR、FluentValidation、Hangfire、Quartz.NET、Redis、消息队列或额外依赖注入容器。优先使用 ASP.NET Core/.NET 内置的依赖注入、Options、DataAnnotations、`System.Text.Json`、Health Checks、Rate Limiting、`BackgroundService`、`IHttpClientFactory`、`Guid.NewGuid()`、加密和文件流能力。
- 后端外部 HTTP 调用统一通过命名或类型化 `IHttpClientFactory` 客户端，设置连接/响应超时、最大响应体、受控重试和目标地址白名单；不得将用户输入直接拼接成请求目标，不得把入站 Cookie、Authorization 或其他敏感请求头透传给外部地址。`axios` 仅用于前端 Orval 生成客户端。
- 生产依赖和测试/生成依赖必须分离并锁定版本；OpenAPI 生成、契约检查和测试工具不进入服务器发布包。依赖升级不得自动改变数据库结构、认证算法、文件格式或业务行为，升级 .NET Runtime、EF Core、SqlClient 或新增原生依赖前必须在 Windows Server 2016、SQL Server 2016 SP2/兼容级别 130 最低基线及实际部署的较新 SQL Server 版本上复验。
- `@redocly/cli` 必须对正式 OpenAPI 3.1 文档执行 lint，`orval` 必须从同一契约可重复生成前端 TypeScript 类型和 Axios 客户端。ASP.NET Core 使用 `Microsoft.AspNetCore.OpenApi` 生成实现侧 OpenAPI 文档，并与仓库规范做语义契约比较；不引入 NSwag 或服务器桩生成器。C# 请求 DTO 使用 DataAnnotations 和领域校验，不能把前端 Zod 生成物作为服务端验证器。
- 正式 OpenAPI 只使用 `flowpilot_session` HttpOnly Cookie；Mock Bearer 只存在于 Debug Mock 适配层。正式模式不得在浏览器保存或发送访问令牌。登录成功响应必须设置该 Cookie，注销响应必须以相同名称和 Path 清除 Cookie；登录、注销与其他状态修改请求都执行 Origin/Referer 同源校验并声明 `403 CSRF_VALIDATION_FAILED`。
- 健康检查固定为匿名存活 `/health/live`、匿名就绪 `/health/ready` 和受系统运维查看权限保护的 `/health/details`。投影重建、附件清单及完整性检查、迁移、种子校验和超级管理员离线重置使用受控 CLI，不开放普通 REST 维护写接口。
- 流程定义 JSON 导出使用 `GET /process-definitions/{definitionId}/export`；自由协作初始表单修改使用 `PATCH`；附件删除遵循 `If-Match`，附件内容支持单区间 Range 和 `200/206/416`，附件卷空间不足返回 `507`。
- 空查询结果属于成功结果；实例 Excel 数据集允许返回零行。OpenAPI 动态查询对象必须由生成客户端或经过契约测试的 `deepObject` 编码器生成，禁止转换为 `[object Object]`。

数据库建模、流程版本 JSON 与类型化投影、SQL Server 数据访问、本地附件状态机、会话安全、后台任务、部署和验收细则统一记录在 [`document/BACKEND_IMPLEMENTATION_DESIGN.md`](./document/BACKEND_IMPLEMENTATION_DESIGN.md)；最终表结构基线见 [`document/BACKEND_DATABASE_SCHEMA.md`](./document/BACKEND_DATABASE_SCHEMA.md)，开工参数与部署填写项见 [`document/BACKEND_IMPLEMENTATION_CHECKLIST.md`](./document/BACKEND_IMPLEMENTATION_CHECKLIST.md)。本节保留首版必须满足的核心需求，实施文档不得降低本节约束。

首版后端只实现 Microsoft SQL Server 数据持久化，不实现 SQLite 适配器、双方言迁移、运行时数据库选择或 SQLite 到 SQL Server 数据迁移：

- ORM 固定使用 EF Core 10，通过 `Microsoft.EntityFrameworkCore.SqlServer` 和 `Microsoft.Data.SqlClient` 访问数据库，并显式设置 `UseCompatibilityLevel(130)`。服务器地址、端口、数据库名、SQL 账号、密码和连接安全选项通过部署配置提供，修改连接配置并重启服务即可连接既定 SQL Server 实例，不需要修改业务代码或重新构建。
- 支持范围从 Microsoft SQL Server 2016 13.x SP2 起：13.x 接受 SP2/SP3，主版本 14 及以上视为受支持；数据库兼容级别最低为 130，允许更高值。所有环境均使用 SQL 账号认证；共享 SQL、EF Core 查询、原生 SQL 和迁移脚本必须以 SQL Server 2016 SP2、兼容级别 130 为最低能力基线，不得因部署在较新版本而使用会破坏最低基线的能力。
- SQL Server TCP 连接模板默认启用 TDS 加密并校验受信服务器证书。远程 SQL Server 不得以关闭加密或无条件信任自签名证书作为默认配置；只有 API 与 SQL Server 确认在同机 `127.0.0.1` 回环链路且部署方记录例外时，才可显式使用未加密/信任服务器证书配置。
- 当实际部署使用 Windows Server 2016 与 SQL Server 2016 SP2 时，其生命周期风险由部署方明确接受；本项目不制定服务器或 SQL Server 升级计划。支持较新的 SQL Server 版本只是软件兼容范围，不代表要求升级。所有部署仍必须实施内网隔离、最小权限、受信 TLS、适用补丁和备份恢复等控制。
- EF Core 采用 Data Mapper 风格，持久化实体与领域模型、API DTO 分离；对象间的结构映射统一使用 `Riok.Mapperly` 在编译期生成。Mapperly 只负责无副作用的数据复制和显式字段转换，不负责权限、领域不变量、状态迁移、默认值决策或数据库查询。新增/修改命令不得把请求 DTO 直接覆盖到 EF Core 跟踪实体，必须先通过应用服务校验并调用领域行为；复杂映射允许在 Mapperly partial mapper 中手工实现。业务服务只依赖领域仓储和统一 `PersistenceUnitOfWork`，不得直接依赖 `DbContext`、`DbSet`、`DbConnection` 或驱动类型。普通 CRUD 和稳定关系查询优先由 EF Core/LINQ 实现；编号分配、版本发布、任务抢占、复杂动态投影和 SQL Server 锁提示等能力允许在基础设施层执行参数化 `SqlCommand`。
- `PersistenceUnitOfWork` 持有同一个作用域 `DbContext` 和显式事务；原生 SQL 必须复用该上下文的 `DbConnection` 与当前 `DbTransaction`，不得另开连接或在同一事务中混用无关上下文。在必要场景显式使用 `SERIALIZABLE` 或 `UPDLOCK/HOLDLOCK`，数据库事务内不得执行 SMTP、附件写入或其他外部 I/O。
- 应用启动不得自动迁移数据库。每次逻辑结构变更提交人工复核的幂等 SQL 部署脚本，并保留对应 EF Core migration 作为结构演进和脚本生成依据；由 DBA 使用高权限迁移账号执行经审查脚本，常驻应用只校验结构版本，结构落后时拒绝就绪。EF migration bundle 仅可作为受控部署选项，不能由 Windows 服务启动时调用。
- UUID、布尔值、UTC 时间、JSON、枚举和整数 revision 在领域层使用统一类型，并映射到约定的 SQL Server 类型；乐观锁统一使用整数 revision，不使用 SQL Server `rowversion`。
- 流程版本和实例表单使用 JSON 保存完整结构或最新值，统一映射到 `nvarchar(max)` 并增加 `ISJSON` 约束。SQL Server 2016 SP2 提供 `JSON_VALUE`、`JSON_QUERY` 和 `OPENJSON`，但没有原生 JSON 类型和通用 JSON 索引，且 `JSON_VALUE` 的标量文本返回存在 4000 字符限制；EF Core 不使用高版本 JSON 列映射，JSON 函数只用于校验、诊断或受控迁移，业务查询不得依赖动态扫描 JSON。
- 允许查询、列表展示或导出的动态标量字段需要同步写入类型化投影表，并按照流程定义、稳定字段标识和值类型建立普通关系索引。JSON 最新值与投影值必须在同一数据库事务中更新，任一写入失败时整体回滚；系统需提供可重复执行的投影校验和重建命令。数字范围、日期范围、排序和跨版本查询均读取投影表，不直接扫描 JSON。
- 业务表统一放在独立 `flowpilot` schema。实体主键使用应用生成的 `uniqueidentifier`，时间保存为 UTC `datetime2(3)`，业务显示和附件年份使用 `Asia/Shanghai`。账号和各类业务编码使用规范化列建立唯一约束，不能只依赖数据库排序规则。
- 用户、角色、部门、职务和流程权限组等仅具有启用/停用语义的主数据，数据库物理列统一使用 `is_enabled bit`，API 继续映射为 `status: enabled | disabled`；流程实例、任务、版本、附件、Outbox 等具有三个及以上生命周期阶段的对象继续使用受 CHECK 约束的状态枚举列。流程定义单独保存 `is_disabled bit`，接口展示状态由该标志与当前发布版本指针推导。
- SQL Server 地址、数据库、应用账号、连接安全选项、预期排序规则、连接池和超时全部由外置配置提供。迁移账号与应用运行账号分离；迁移账号不作为常驻应用秘密保存。

所有客户端均通过 ASP.NET Core REST API 访问业务数据，不允许绕过后端直接连接 SQL Server。首版继续按单个后端服务实例设计，预计每年新增流程实例不超过2万、附件总量不超过500 GB。

### 9.5 IIS 与 Windows 服务部署

- 系统仅部署在公司内网，不发布到公网或 Sites 托管环境。正式服务器使用 Windows Server 2016，IIS 提供前端静态文件，并只将同源 `/api/flowpilot/*` 请求反向代理到作为原生 Windows 服务运行的 ASP.NET Core 进程，避免与同一主站的其他系统 API 冲突；不再使用 WinSW。
- 前端正式构建统一使用 `pnpm build`，固定排除浏览器 Mock 并读取同源 `/api/flowpilot/v1` 后端；用于无 HTTPS 环境下原型演示的 `pnpm build:debug` 使用页面内 Fetch/XHR 拦截器提供 `/flowpilot/mock-api/v1` Mock API，不注册 Service Worker。debug 包的数据和附件仍只属于当前浏览器，不得作为正式业务数据源或多人共享数据使用。
- 前端以 IIS 子应用 `/flowpilot` 部署，静态资源、React Router、打印新窗口和 debug Mock API 均必须使用一致的子路径。FlowPilot 应用级 SPA 回退不得接管 Mock API 或正式 API 请求；正式 `/api/flowpilot/*` 反向代理由 IIS 主站在 SPA 回退之前处理。
- `/flowpilot` 和 `/flowpilot/` 均可作为应用入口；不带末尾斜杠的 `/flowpilot` 必须保留查询参数并重定向到 `/flowpilot/`。开发服务器、构建预览和 IIS 部署保持一致，不得向用户返回 Vite 基址 404 提示。
- IIS 必须对前端入口 `index.html` 返回 `Cache-Control: no-cache`，确保收藏夹、根地址和 React Router 回退入口在新版本发布后重新验证；带内容哈希的 JS、CSS 和其他静态资源不统一禁用缓存。
- ASP.NET Core/Kestrel 只监听 `127.0.0.1`，不直接暴露到局域网；Windows 服务使用独立低权限账号，程序目录只读，数据库、附件和日志目录分别授予必要的读写权限。
- 客户端 IP 和外部请求 authority 只能由本机 IIS/ARR 反向代理提供。IIS 必须覆盖并丢弃浏览器或其他外部客户端传入的 `X-Forwarded-For`、`X-Forwarded-Host` 和 `X-Forwarded-Proto`：前者写入与 IIS 建立连接的真实客户端地址，后两者写入当前已匹配站点绑定的主机与协议；不得保留、追加或透传外部原值。IIS 必须显式限制允许访问 FlowPilot 的内外网绑定并在反代前拒绝未知 `Host`，使用裸 IP 绑定时也必须把 Host 限制到对应 IP 与端口。ASP.NET Core `ForwardedHeadersMiddleware` 只信任 loopback 代理，不得信任任意网络来源。上线验收必须使用两个不同来源地址确认限流桶彼此独立，并伪造全部三个转发头，确认伪造值既不能改变来源 IP，也不能绕过同源校验或改变邮件链接入口。
- 后端发布为自包含或框架依赖的 `net10.0` Windows x64 产物，并通过 `Microsoft.Extensions.Hosting.WindowsServices` 原生注册为 Windows 服务，不使用 WinSW。应用使用 Serilog 输出按日期和大小滚动的结构化 JSON 文件，并配合 Windows 服务恢复策略自动重启；实际发布模式和真实服务账号在部署时填写。
- 当前正式部署使用 HTTP。会话 Cookie 设置 `HttpOnly` 和 `SameSite=Strict`，当前配置关闭 `Secure`；系统必须保留以后通过配置启用 IIS HTTPS 和 `Secure` Cookie 的能力。部署文档需要明确 HTTP 会使登录密码和会话在内网链路上缺少传输加密。
- 正式后端中的流程定义、流程版本、流程实例、任务、附件、用户及治理记录等领域主键统一由后端应用生成 `uniqueidentifier`，浏览器不得为这些正式实体指定主键。前端只允许为流程版本快照内部的节点、字段、选项、表格行等局部设计器对象，以及幂等请求键生成 UUID；当前 HTTP 原型在 `crypto.randomUUID()` 不可用时可以使用 `crypto.getRandomValues()` 生成 UUID v4，并提供非安全用途的最终防碰撞降级，但该降级不得用于正式领域主键、密码、会话令牌或其他安全凭据。
- 正式 API 只接受同源浏览器前端调用，关闭跨域访问。所有修改状态的请求优先使用 `Origin`，只有其缺失时才使用 `Referer`，并将其 origin 与可信代理后的 protocol 和 host 精确比较；两者缺失、格式非法或不匹配时返回 `403 CSRF_VALIDATION_FAILED`。
- 部署时只需确定部署根目录，不固定为 `D:\FlowPilot`，也不要求固定盘符。标准程序布局为 `App\releases\{releaseId}\api|web`，稳定入口为 `App\current\api|web`；发布目录名称使用不可变且不会与 REST `/v1` 混淆的 releaseId，例如 `2026.08.26.1`，不得覆盖已有目录。`release.json` 至少记录 releaseId、产品版本、构建时间、源代码提交、API 契约版本、所需数据库结构版本、允许兼容的结构版本范围、迁移校验和及发布文件校验信息；启动时必须拒绝超出兼容范围的数据库。应用按唯一 `flowpilot.root` 标记定位部署根，路径解析不得依赖 Windows Service 当前工作目录，也不提供根目录或 Config/Secrets 文件位置覆盖参数。
- 稳定且非敏感的运行默认值保存在当前发布包 `api\appsettings.json` 中，该文件不得包含 SQL/LDAP/SMTP 等部署环境地址、数据库名、账号、密码或证书私钥。站点入口由每次写请求自动验证和解析，不进入配置。外置 `{部署根目录}\Config\appsettings.Production.json` 只保存部署时必须确认的非敏感环境值和少量显式覆盖；`{部署根目录}\Secrets\secrets.Production.json` 只保存 AD/LDAP、SMTP、SQL Server 环境地址与凭据及首次初始化超级管理员密码。配置优先级为标准 .NET 进程环境变量值覆盖 > Secrets JSON > Config JSON > 随程序默认 `appsettings.json`，但环境变量不得改变目录或配置文件位置。首版不使用 `.env` 解析器、DPAPI 加密文件、外部密钥平台或 Data Protection 密钥文件；明文秘密仅依靠仓库外目录和 NTFS 最小权限保护。
- `Config` 与 `Secrets` 不属于发布包，真实配置文件禁止提交 Git；仓库只能提供不含真实值的 JSON 示例模板，并同时忽略本地真实配置、证书私钥和备份。`.gitignore` 只是防误提交措施，若秘密曾进入 Git 历史必须立即轮换对应凭据。ASP.NET Core 启动时通过 Options 验证配置结构和必填项，但不得在日志、错误响应或健康接口中输出密码、连接字符串或完整敏感配置。
- IIS、ASP.NET Core Windows 服务和附件目录按单服务器、单 API 实例运行；后端通过敏感配置文件中的连接参数访问既定 SQL Server 2016 SP2 或之后版本实例。标准目录结构和权限要求见 [`document/IIS_DEPLOYMENT.md`](./document/IIS_DEPLOYMENT.md)。
- 应用版本发布与数据库结构变更允许计划停机。新版本先完整写入新的 `App\releases\{releaseId}` 并校验清单，停止 Windows Service 和 IIS 应用后，使 `previous` 保留原完整发布包，再将 `current` 统一切到新发布包；首次安装没有旧版本时只创建 `current`，不得创建无效 `previous`。启动后必须检查数据库结构兼容性、API 健康、IIS 页面和关键业务，至少保留当前与上一个完整发布目录；浏览器必须重新打开或强制刷新，版本不匹配时不得继续提交写操作。链接切换只回滚程序与静态文件，不能回滚数据库结构或数据；只有数据库仍与旧程序兼容或 DBA 已完成批准的恢复方案时，才可把 `current` 切回 `previous`。不要求零停机或双版本兼容部署，这里也不包含 Windows Server 或 SQL Server 产品版本升级。

### 9.6 后台任务、保留与运维

- 邮件发送、附件清理、过期会话和幂等记录清理由 API 进程内调度器执行。任务必须使用数据库状态和租约领取，进程重启后可以恢复超时的处理中任务，不能只依赖内存定时器状态。
- 后台任务默认每分钟扫描一次、每批最多50条、租约5分钟；邮件默认并发5、单次 SMTP 超时15秒。以上属于非敏感运行参数，可在 `Config\appsettings.Production.json` 调整。
- 邮件首次发送失败后按照1、5、15、60、360分钟延迟重试，累计6次仍失败时进入死信；管理员可以在运维页手工安排重试。SMTP 异常不回滚已提交的业务事务。
- 流程定义版本、流程实例、审批结果、自由协作当前内容和不可变业务审计永久保留，不提供实例自动删除、实例人工删除或归档。
- 技术日志默认保留30天，已发送邮件 Outbox 记录保留180天，幂等记录保留7天，已过期会话保留7天后物理清理。死信记录在问题解决或手工重试成功前不得自动清理。
- 技术日志采用结构化格式并包含 traceId，但不得记录密码、会话令牌、完整表单值、附件正文或其他秘密配置。全局操作审计只记录被修改字段的标识和名称，不保存字段业务值。
- 提供存活、就绪和受权限保护的详细运维健康接口。数据库不可用或结构版本落后时服务不就绪；磁盘空间不足、附件清理失败或邮件死信属于降级状态，并在系统运维页显示明确告警。
- 正式数据库首次初始化只创建超级管理员、内置角色权限、必要字典和结构版本，不创建演示用户、示例流程、流程实例或演示附件。
- 首版不提供应用内自动定时备份。升级或一致性备份前先停止 Windows 服务，由 DBA 负责 SQL Server 备份和恢复；系统提供附件清单及 SHA-256 完整性校验，管理员负责将附件备份复制到其他位置，并确保数据库备份与附件清单属于同一停机时间点。

## 10. 暂不纳入首版的内容

- 正式组织架构同步，以及 Windows 集成认证或其他免密单点登录（按用户配置使用 AD/LDAP 校验域账号密码已纳入首版）
- PostgreSQL 等高并发数据库及分布式对象存储
- 企业微信、钉钉等其他通知集成
- 独立条件分支/互斥网关、抄送、自动任务（审批节点本身的执行条件已纳入首版）
- 外部数据源驱动的下拉选项
- 完整 BPMN 引擎集成
- 流程、用户、角色和流程权限组的归档功能
- 应用内 SQL Server 数据库和附件目录的自动定时备份功能（保留 DBA 数据库备份及应用提供的附件校验能力）

## 11. 待确认事项

- 当前没有未决的产品或总体架构选择。
- 实际部署根目录、Windows 服务账号、SQL Server 主机/数据库/账号/排序规则、LDAP 或 LDAPS 地址/Base DN/UPN 后缀、SMTP 地址/账号/发件人、IIS 主机名和首次超级管理员密码，均属于部署环境值。后端完成后由部署人员填写外置配置文件，不写入需求、源代码、Git 或发布包。
- 若目标域不允许用户 UPN 直接绑定后再搜索，需要在部署联调时启用可配置的只读域搜索账号；这只改变认证提供方配置，不改变用户表、登录接口或权限模型。

## 12. 变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| 2.23 | 2026-08-26 | 完成后端文档一致性收口：附件统一采用年份目录下 `.incoming`/`objects` 两阶段布局和 `uploading/staged/active/cleanup-pending/failed/deleted` 内部状态；权限组增加用途关系；流程版本状态由发布指针与校验结果派生；任务中心区分审批、自由协作和待重新提交；补齐自由协作受理人、任务、参与人和时间线持久化，回复只保留最新正文及编辑审计元数据。正式领域主键统一由后端生成，前端目标客户端统一为 Orval/Axios；同时补充登录/注销 Cookie 与 CSRF 契约、SQL TLS、Outbox 收件人幂等键和发布数据库结构版本元数据。 |
| 2.22 | 2026-08-26 | 采用统一不可变发布包：`App\releases\{releaseId}` 同时包含 `api`、`web` 和 `release.json`，本机 NTFS 目录联接 `current`/`previous` 对整包统一切换与回滚，避免前后端契约错配。Windows Service 与 IIS 使用稳定的 `current\api`/`current\web` 路径；部署根改为从 `AppContext.BaseDirectory` 向上有限查找唯一 `flowpilot.root`，兼容联接路径和真实目标路径。Config、Secrets、Data、Logs、Temp、Backup 继续位于发布目录之外；程序回滚明确不等于数据库回滚。 |
| 2.21 | 2026-08-26 | 取消 `FLOWPILOT_HOME`、`FLOWPILOT_CONFIG_FILE` 和 `FLOWPILOT_SECRETS_FILE` 路径参数。API 发布文件直接位于实际程序目录，前端位于其 `web` 子目录；生产路径以 `AppContext.BaseDirectory` 确定程序目录并取父目录作为部署根目录，禁止依赖 Windows Service 当前工作目录。Config、Secrets、Data、Logs、Temp 和 Backup 均按固定同级目录推导。 |
| 2.20 | 2026-08-26 | 确认对象映射库使用 `Riok.Mapperly`。Mapperly 作为编译期源码生成器和私有构建依赖，不进入服务器运行时依赖；用于持久化实体、领域模型与 API DTO 的无副作用结构映射。请求 DTO 不得直接覆盖 EF 跟踪实体，权限、领域不变量和状态迁移仍由应用服务/领域行为显式执行。 |
| 2.19 | 2026-08-26 | 修正数据库兼容范围：SQL Server 2016 SP2 是最低版本，不是唯一版本；13.x 接受 SP2/SP3，主版本 14 及以上受支持，数据库兼容级别最低为 130并允许更高值。共享 SQL、EF Core 查询和迁移仍以 2016 SP2/兼容级别 130 为最低能力基线；支持较新版本不代表制定或要求服务器升级计划。 |
| 2.18 | 2026-08-26 | 正式后端目标架构从 NestJS/Node.js 切换为 .NET 10、ASP.NET Core 10 Controller Web API、EF Core 10 和 Microsoft.Data.SqlClient；Windows 服务改为原生 .NET Windows Service，SMTP 改用 MailKit，日志改用 Serilog，后端配置改为外置 JSON；前端保留 Orval/Axios。数据库范围表述已由 2.19 修正；Windows Server 2016 与 SQL Server 2016 SP2 生命周期风险明确接受，不制定升级计划。仓库现有 NestJS 代码标记为待替换旧骨架，本次只同步文档。 |
| 2.17 | 2026-08-26 | 修正认证提供方分流：所有已配置为密码登录的用户只校验本地 scrypt 散列，成功、失败或停用场景均不调用或探测 LDAP；LDAP 可用性只影响域登录和未知账号处理。 |
| 2.16 | 2026-08-26 | 取消站点根地址环境变量并取代 2.14/2.15 的显式列表方案：后端把写请求 Origin（缺失时才用 Referer）与本机 IIS 覆盖后的 protocol/host 精确比较，通过后自动冻结 `${origin}/flowpilot`；IIS 覆盖 For/Host/Proto 并限制站点绑定，无请求事件只继承实例已验证入口，缺失时明确失败而不猜地址。 |
| 2.15 | 2026-08-25 | （已由 2.16 取代）原方案让邮件链接跟随触发业务事件命中的显式站点入口，并为无浏览器请求的事件配置默认回退入口。 |
| 2.14 | 2026-08-25 | （已由 2.16 取代）原方案通过显式站点地址列表支持同一部署的多个内外网入口，并禁止从未经校验的请求头推导地址。 |
| 2.13 | 2026-08-25 | 精简后端部署配置：稳定且非敏感的端口、超时、连接池、兼容门槛和安全开关移入随代码发布的 `api/config/defaults.env`；外置 `application.env` 与 `production.env` 只保留必须确认的环境值、凭据及少量显式安全覆盖，且继续保持更高读取优先级。 |
| 2.12 | 2026-08-25 | 固化 AD/LDAP 与 SMTP 后端安全边界：域 bind 后必须以同一 sAM 账号和 UPN 确认唯一身份；匿名 RootDSE 探测、短时共享负缓存和独立 503 IP 限流统一域故障响应；LDAPS、SMTP TLS 和 SQL Server 受信 TLS 均改为安全默认，旧明文内网端点必须通过显式开关和风险接受启用。邮件固定发件人并拒绝头注入，不迁移 SQL OLE/CDO 发信。 |
| 2.11 | 2026-08-25 | 固化 IIS/ARR 客户端 IP 信任边界：反向代理必须丢弃外部传入的 `X-Forwarded-For` 并以真实连接来源覆盖，NestJS 只信任 loopback 代理；上线使用两个来源地址和伪造转发头验证登录限流不能串桶或被绕过。 |
| 2.10 | 2026-08-25 | 将 SQL Server 2016 SP2 确认为最低数据库版本，而非版本白名单：13.x 仅接受 SP2/SP3，主版本 14 及以上正常通过；数据库兼容级别最低为 130，允许 140/150/160 等更高值。共享迁移和查询仍以 SQL Server 2016 SP2、兼容级别 130 为最低能力基线。本变更扩展 1.89 的版本范围，但不恢复 SQLite 或多数据库 provider。 |
| 2.09 | 2026-08-25 | 修复正式模式 REST 与分页边界：正式构建统一请求 `/api/flowpilot/v1`；自由协作、附件、审计、角色及权限组响应通过适配器进入页面，回复与附件删除携带 ETag；流程定义完整版本改为进入具体流程后按需加载，流程清单、任务中心、实例监控、审计和流程管理使用服务端分页。启用账号必须至少有一个启用角色；移除没有真实入口的表单查看/预览和运维导出权限，并采用保守权限迁移避免恢复曾主动移除的删除权限。 |
| 2.08 | 2026-08-25 | 修复目录删除与权限数据同步：停用的已有用户允许清空角色后再接受服务端引用检查；浏览器 Mock 权限存储升级采用保守兼容策略并保留自定义角色权限；角色列表权限统计始终从已保存矩阵即时计算；用户密码提示统一为 Node.js scrypt。 |
| 2.07 | 2026-08-25 | 同步后端开工前文档基线：`apps/api` 固定采用 ESM 与 TypeScript NodeNext；Redocly lint、Orval 类型/Zod/Axios 生成及无漂移检查成为契约门禁；用户、角色、部门、职务和流程权限组的数据库启停列统一为 `is_enabled bit`，流程定义保存 `is_disabled bit` 并推导接口状态，多阶段领域对象继续使用状态枚举。 |
| 2.06 | 2026-08-25 | 完成全权限删除动作审查：新增独立的部门/职务删除和流程权限组删除权限；前端按钮、Mock 鉴权及正式契约不再复用编辑权限。部门/职务和流程权限组仍执行 ETag 与引用检查，流程实例、任务、审计及编辑器内部子项不新增独立删除权限。 |
| 2.05 | 2026-08-25 | 权限管理动作权限改为紧凑四列单行网格，新增删除权限后仍与查看、编辑、授权等动作位于同一行，并保持各页面动作列上下对齐。 |
| 2.04 | 2026-08-25 | 确认后端最小依赖白名单与升级约束：生产依赖优先采用纯 JavaScript/TypeScript，SQL Server 固定使用 `mssql` 默认 `tedious` 驱动；本地密码由原生 Argon2id 模块调整为 Node.js 内置异步 `scrypt`，邮件改为直接使用 `nodemailer`，并按要求加入直接封装的 `axios`；禁止原生编译模块、额外消息基础设施和重复框架封装，开发工具不进入生产包。 |
| 2.03 | 2026-08-25 | 用户和角色管理增加受引用保护的删除：内置项和当前账号不可删除；用户检查角色、流程权限组、流程版本、实例、任务和附件等引用，角色检查成员、流程权限组和所有流程版本引用；历史业务对象只能停用。新增独立的用户删除与角色删除动作权限，并要求 ETag 并发校验和中文冲突原因。 |
| 2.02 | 2026-08-25 | 明确流程通知邮件包含可点击的内网流程链接：任务通知指向实例并携带非授权性质的 taskId，结束通知指向实例；未登录时登录后安全返回，详情和处理继续执行服务端鉴权。邮件 Outbox、收件人与邮箱快照、链接、状态、时间和每次 SMTP 尝试持久化到 SQL Server，不保存完整 MIME 正文或附件。 |
| 2.01 | 2026-08-24 | 固化后端开工决策：新增 SQL Server 完整结构基线和实施检查清单；后端使用 `apps/api`、Node.js 24 LTS、WinSW、独立 `flowpilot` schema、UTC 存储及 `Asia/Shanghai` 业务时区；正式 OpenAPI 仅使用 Cookie，会话健康接口分级，危险维护能力使用 CLI；持续增长的数据改为服务端分页和按需加载。真实数据库、域、SMTP、IIS、账号、密码和部署路径统一留待后端完成后通过外置配置填写。 |
| 2.00 | 2026-08-24 | 正式后端增加并确定 TypeORM：通过 `@nestjs/typeorm` 集成，MSSQL 底层继续使用 `node-mssql`；采用 Data Mapper、领域仓储和事务专属 `QueryRunner`，普通 CRUD 使用 ORM，SQL Server 锁与复杂投影保留参数化原生 SQL；生产禁用 schema synchronize 和启动自动迁移，结构变更使用人工复核的 TypeORM migration。 |
| 1.99 | 2026-08-24 | 修正程序目录约定：不再固定为 `D:\FlowPilot\App`，盘符、路径和目录名称均由实际部署确定；`FLOWPILOT_HOME` 动态取实际程序目录的父目录，配置、敏感参数、附件、日志、临时文件和备份目录继续作为程序目录的同级目录。 |
| 1.98 | 2026-08-24 | 确认 `D:\FlowPilot\App` 为程序目录，`Config`、`Secrets`、`Data\Attachments\{yyyy}`、`Logs`、`Temp` 和 `Backup` 均放在其父目录并由 `FLOWPILOT_HOME` 统一推导；敏感参数集中放在仓库外的 `Secrets\production.env`，首版不使用 DPAPI 或外部密钥平台，仅通过 NTFS 最小权限和运维流程保护。 |
| 1.97 | 2026-08-24 | 正式 REST API 基础路径统一为 `/api/flowpilot/v1`，以系统命名空间避免共享 IIS 主站下的接口冲突；IIS 只代理 `/api/flowpilot/*`，OpenAPI、正式构建、远程模式、健康检查和附件接口同步使用新前缀。Debug 页面内 Mock 保留 `/flowpilot/mock-api/v1` 专用路径，不属于正式 API。 |
| 1.96 | 2026-08-24 | 强制将正式附件、配置、日志和备份目录放在版本化代码及 IIS 静态目录之外；NestJS 启动时校验附件绝对路径、服务账号权限、目录边界和同卷原子移动条件，IIS 不得直接暴露附件目录；补充升级不触碰附件及停机迁盘、清单和 SHA-256 校验流程。 |
| 1.95 | 2026-08-24 | 修复 Excel 转 PDF 最终附件被源文件扩展名规则误拒绝：转换字段即使历史配置只允许 xlsx，附件接口仍接受有效 PDF 并继续拒绝原始 Excel；同时移除转换预览中的浏览器读取、上传边界、兼容性和预计页数说明。 |
| 1.94 | 2026-08-24 | 增强纯前端 Excel 转 PDF 为工作簿打印式排版：读取打印区域、纸张、方向、缩放、页边距和标题行列，正确处理合并单元格、富文本内容、图片、边框、隐藏行列及基础格式；打印区域同时纳入浏览器资源上限校验。 |
| 1.93 | 2026-08-21 | 调整任务中心动作权限：将“驳回”合并进“审核”，新增独立“关闭”权限；关闭操作同时校验系统动作权限与关闭流程权限组，文控专员默认获得审核和关闭权限，并迁移旧角色权限数据。 |
| 1.92 | 2026-08-21 | 调整 IIS 前端缓存策略：仅对 `index.html` 禁用客户端缓存，确保重新打开应用时及时加载新版本；保留带内容哈希静态资源的正常缓存。 |
| 1.91 | 2026-08-21 | Excel 转 PDF 改为纯前端链路：浏览器读取 `.xlsx`、生成并预览 PDF，用户确认后只上传 PDF；移除 LibreOffice 与服务端 Excel 暂存/转换契约，服务端继续对最终 PDF 做权威安全校验。本变更取代 1.35、1.37 中的服务端转换决策。 |
| 1.90 | 2026-08-21 | 统一 FlowPilot 子应用根地址：开发服务器、构建预览和 IIS 均将不带末尾斜杠的 `/flowpilot` 重定向到 `/flowpilot/`，并保留查询参数，避免 Vite 基址 404。 |
| 1.89 | 2026-08-21 | 正式后端数据库收敛为仅支持 Microsoft SQL Server 2016 SP2：取消 SQLite/Drizzle、运行时数据库选择、双方言迁移和 SQLite 到 SQL Server 迁移工具；保留领域仓储与事务单元边界，结构迁移、事务、备份、测试和部署统一按 SQL Server 实现。本变更取代 1.5 和 1.59 中的双数据库决策。 |
| 1.88 | 2026-08-21 | 优化操作审计日志的业务展示：英文动作码统一转换为简洁中文，移除事件与资源编码、IP 占位和原始 JSON，操作内容去除业务编号，详情仅概括中文变更字段；底层审计原始数据保持不变。 |
| 1.87 | 2026-08-21 | 解除已有实例版本的校验死锁：只读仅锁定版本快照，不锁定校验刷新；未通过版本的发布入口改为“重新校验后发布”，按最新权限组校验通过后进入确认，最终发布时再次权威校验。 |
| 1.86 | 2026-08-21 | 统一全部界面时间显示为分钟精度 `YYYY-MM-DD HH:mm`：覆盖列表、详情、时间线、流程版本、校验、审计、设计器保存状态、时间筛选和打印页面；底层时间戳继续保留秒及更高精度。 |
| 1.85 | 2026-08-21 | 修正取消发布后的版本校验同步：取消发布事务重新校验当前完整快照并写回结果，避免已发布状态遮蔽的过期校验导致原版本错误显示“校验未通过”，同时保证与基于同一版本复制新建的校验结果一致；持久化数据升级时一并重新计算旧版本校验，无需重置全部模拟数据。 |
| 1.84 | 2026-08-21 | 修正取消发布弹窗的布局重叠：为取消原因文本框的字数统计预留独立底部空间，避免 `0 / 200` 与底部按钮栏重叠。 |
| 1.83 | 2026-08-21 | 修正取消当前发布时的并发标识：页面改为获取流程定义最新 ETag 后提交，不再误用流程版本 ETag 导致固定返回 412“请重新获取资源后再提交”。 |
| 1.82 | 2026-08-21 | 优化自由协作处理：受理人事件文案简化为“受理人变更为某人”；协作区合并为状态化主按钮，支持“发表回复”“变更受理人”“回复并变更”三种状态，回复并变更采用单次原子提交。 |
| 1.81 | 2026-08-21 | 流程权限组更新时间改为分钟精度，统一显示 `YYYY-MM-DD HH:mm`，不再显示秒。 |
| 1.80 | 2026-08-21 | 自由协作的受理人变更与回复彻底拆分：有权限的用户选择新受理人后可直接变更，无需填写处理内容或说明；时间线仅记录操作人、原受理人、新受理人和时间，已有回复草稿不受影响。 |
| 1.79 | 2026-08-21 | 修正自由协作历史回复编辑时间：编辑后回复卡片直接显示最新编辑时间，不再继续以首次发表时间作为主时间；首次发表时间保留在悬停提示中。 |
| 1.78 | 2026-08-21 | 统一流程权限组更新时间显示：正式接口返回的 ISO 8601 时间按浏览器本地时区转换为 `YYYY-MM-DD HH:mm:ss`，不再直接显示 `T`、毫秒和 `Z`。 |
| 1.77 | 2026-08-21 | 收敛自由协作入口与流转权限：流程清单删除“新建事项”，所有新实例统一从流程发起进入；删除侧栏“流程版本保存后自动校验”；发起或受理权限组任一有效成员均可切换当前受理人，时间线记录原受理人、操作人和新受理人；编辑已有内容同步更新事项时间并拒绝伪变更。 |
| 1.76 | 2026-08-21 | 完成正式接口接入风险收口：前端兼容 Debug 响应包与 OpenAPI 直接 DTO，移除 Mock 私有目录快照依赖；流程导入改为后端原子命令；正式流程版本补取完整快照；编辑器保存初始 ETag 并正确暴露并发冲突；关键远程水合改为全成全败；用户列表服务端分页；权限目录与动作控件统一，并增加正式 DTO/导入契约测试及全源码覆盖率门禁。 |
| 1.75 | 2026-08-21 | 正式附件存储目录增加上传年份分层：临时文件与正式对象统一保存到 `ATTACHMENT_ROOT/{yyyy}` 下，年份由服务端按上传时间固化到相对存储键；年份目录用于盘点、备份和清理定位，但任何删除仍必须先校验数据库元数据和业务引用。 |
| 1.74 | 2026-08-21 | 统一 Debug 演示身份与用户管理数据源：顶栏展示全部启用用户、排除停用用户，超级管理员固定置顶，并继续支持账号、姓名和角色搜索。 |
| 1.73 | 2026-08-21 | 重新设计公司名称展示：左侧导航和登录页统一改为清晰的纯文字 `MOONS'` 字标，移除拼接双环和大面积渐变底板，以现代字重、品牌色撇号和简洁强调线提升协调性。 |
| 1.72 | 2026-08-21 | 收紧权限管理“动作权限”的固定列宽和列间距，继续保持各行复选框上下对齐，避免动作列随页面宽度拉伸产生过大空隙。 |
| 1.71 | 2026-08-21 | 收敛 Debug 初始登录入口：登录页固定只使用超级管理员账号并预填演示密码，移除登录页身份选择；登录后继续通过顶栏直接切换演示身份。 |
| 1.70 | 2026-08-21 | 增加按用户配置的登录方式：普通用户默认域登录，也可改为本地密码登录；超级管理员固定使用本地密码。正式超级管理员模拟身份和 Debug 演示身份切换均跳过目标用户认证，直接返回按目标身份生效的会话状态。 |
| 1.69 | 2026-08-21 | 参考公司标识统一侧栏与登录页品牌展示：改为横向 `MOONS'` 字标和双环形 `O`，使用系统蓝色主题底板，取消独立 `M` 方块。 |
| 1.68 | 2026-08-21 | 优化详情、任务中心和权限管理展示：流程发起记录移除“提交初始表单”；仅“全部待办”在标题下显示流程名称；动作权限复选框按固定列对齐；左上角改为 `MOONS'` 品牌并删除环境/原型标签；补齐生产一、二车间及旧组织数据迁移。 |
| 1.67 | 2026-08-21 | 修复旧版浏览器身份持久化数据中稳定角色/成员标识为空时，普通用户被错误识别为无角色、无法看到任何页面的问题；启动时按仍保留的角色和成员名称自动恢复关联，不影响超级管理员独立授权。 |
| 1.66 | 2026-08-21 | 将“重置演示数据”严格限制在浏览器 Mock 环境：`pnpm dev` 和 Debug 构建仅超级管理员可见，正式构建不显示，并撤销正式后端重置接口。 |
| 1.65 | 2026-08-20 | 恢复超级管理员的“重置演示数据”入口：统一 `pnpm dev`、Debug 和正式构建的 Mock 模式识别；当前生效身份或真实登录操作者为超级管理员时均显示，普通用户隐藏；正式重置改走后端系统接口，完成后重新加载会话与业务数据。 |
| 1.64 | 2026-08-20 | 登录成功后仅记住并回填上一次登录账号，不保存密码且登录失败不覆盖；Debug 演示身份和正式超级管理员模拟身份选择均支持账号、姓名和角色文本搜索，选择后直接切换、不显示确认框；正式切换仍由后端建立会话并保留双身份审计。 |
| 1.63 | 2026-08-20 | 确认正式后端模拟身份机制：仅真实登录的系统内置超级管理员可切换；后端会话区分真实操作者与生效用户，权限和数据范围按生效用户计算，审计双身份留痕，切换后清理并重取身份相关缓存；debug 浏览器 Mock 继续保留直接演示身份切换。 |
| 1.62 | 2026-08-20 | 修复当前 HTTP 部署中 `crypto.randomUUID()` 不可用导致流程定义导入、新建流程和其他客户端写操作报错：统一使用 HTTP 兼容 UUID v4 生成器，并覆盖导入、版本、实例、治理、附件、审计和请求标识。 |
| 1.61 | 2026-08-20 | 修正流程定义与流程清单实例数口径：流程定义总数及版本实例数统一由实际实例稳定关联核算，Debug Mock 启动时自动修复旧浏览器中的独立预置统计。 |
| 1.60 | 2026-08-20 | 明确 IIS `/flowpilot` 双构建部署：`pnpm build` 固定连接正式同源后端，`pnpm build:debug` 使用无需 Service Worker 和 HTTPS 的页面内 Fetch/XHR Mock；同步约束静态资源、打印路由、API 代理和浏览器本地数据边界。 |
| 1.59 | 2026-08-20 | 将本轮正式后端讨论整理为独立实施文档，完整记录流程版本 JSON 与类型化投影、SQLite/SQL Server 2016 SP2 配置切换、本地附件状态机、会话与事务、Outbox、部署运维和双数据库验收基线，并在技术方案中建立引用。 |
| 1.58 | 2026-08-20 | PDF 操作按钮由“预览”改为“查看”；预览临时地址增加引用复用和延迟释放，并在下一绘制帧挂载 PDF，避免 React 开发模式重复挂载与 Chrome PDF 查看器异步读取产生竞态，降低首次打开偶发“未能加载 PDF 文档”。 |
| 1.57 | 2026-08-20 | 修正流程打印附件名称：打印页与详情页共用锁定版本动态附件解析，优先显示审核或重复修改后的当前附件名称，旧快捷字段仅作缺失数据兼容；没有附件字段时不生成附件清单。 |
| 1.56 | 2026-08-20 | 精简详情页 PDF 展示为单行附件操作与直接正文预览，删除折叠标题、内部工具栏及兼容预览假正文等多余嵌套；修正流转记录不再把实例当前附件倒挂到首次“流程发起”；最大数量为1的附件字段统一支持原位替换，确保开启重复修改的审核节点可以再次上传附件。 |
| 1.55 | 2026-08-20 | 继续落实全面审查整改：运行实例强制保存定义/版本/发起人稳定关联并将旧数据隔离为迁移 DTO；审批锁定和完成判定以任务为事实源；富文本媒体改用 IndexedDB 文件引用；附件配置补齐允许扩展名、Excel 转 PDF 和最大页数并实施双重校验；多版本导出保持家族级字段引用；治理查询统一为“编辑条件后查询”；生产构建排除 MSW；新增 REST Problem Details、发布校验和独立删除权限的 API 合约测试。 |
| 1.54 | 2026-08-20 | 按全面代码审查收紧实现边界：设计器、版本状态和发布动作共用完整拓扑/权限组校验；详情附件改为暂存并随保存、重新提交或审核命令一次提交；流程删除增加独立权限；额外可见角色改存稳定角色 ID；多版本导入复用兼容字段与表格列标识；邮件 Outbox 改由任务激活命令生成且查询只读；统一领域时间格式并修正流程清单查询提交行为。 |
| 1.53 | 2026-08-20 | 精简 PDF 折叠预览：文件名只在附件行单行显示一次，折叠标题和展开后的预览工具栏不再重复文件名，页面不显示 PDF 内部文件名。 |
| 1.52 | 2026-08-20 | 修复旧演示实例的审核节点映射：审核记录、待办和锁定版本统一使用稳定节点 ID，持久化数据升级到 v16 时自动迁移，重置演示数据也直接生成规范化运行时数据；补齐测试报告历史 V1/V2 的完整锁定快照，确保无当前发布版本时已有待办仍可处理。 |
| 1.51 | 2026-08-20 | 优化流程详情与设计器操作：PDF 预览和流程进度默认折叠，PDF 仅在展开时加载；审核授权字段显示“本节点可修改”；表单及流程设计器增加撤销/重做和快捷键；打印移除流程流转记录，实际审核信息按完成时间升序排列。 |
| 1.50 | 2026-08-20 | 重构复杂审批实例的流程进度：基于实例锁定版本的节点连线按拓扑分层展示，区分顺序、并行、待激活、待审核和实际结果；条件跳过节点完全隐藏，不再把所有审核节点错误显示为一组并行会签。 |
| 1.49 | 2026-08-20 | 修复新建流程上传 PDF 后无法预览：详情页将表单文件名与字段级或实例级附件 ID 兼容配对，旧实例无需重新发起即可读取仍存在的附件内容；同时将 `.pdf` 文件的预览 MIME 类型统一为 `application/pdf`，兼容 Windows 返回通用二进制类型的情况。 |
| 1.48 | 2026-08-20 | 选择类字段和表格选择列采用稳定选项标识与可编辑名称分离模型；支持原位重命名而不破坏默认值、条件和历史选中状态，并补充新增、删除重建、历史版本显示、旧数据迁移及 JSON 导入导出规则。 |
| 1.47 | 2026-08-20 | 简化任务中心列表字段设计：取消显示名称、显示顺序和手工列宽配置，列名使用表单项名称并按表单顺序排列；任务中心与流程清单统一采用有最小/最大边界的自动列宽。 |
| 1.46 | 2026-08-20 | 删除“恢复历史设计”功能：移除版本页入口、历史设计扫描、恢复版本创建能力及相关原型样式；正常的版本查看和复制新建功能保持不变。 |
| 1.45 | 2026-08-20 | 简化流程定义导入导出界面文案：操作入口统一显示“导入”“导出”，不再显示格式名称；文件选择器仅接受 `.json`，读取前再次校验扩展名。 |
| 1.44 | 2026-08-19 | 增加单流程定义 JSON 导入导出：导出全部版本的可读中文完整快照且不包含内部 ID/编码；导入按流程权限组、角色和用户显示名称匹配，缺失项自动省略并提示，重新生成全部内部标识，遇到同名定义追加“（导入）”，所有版本保持未发布。 |
| 1.43 | 2026-08-19 | 增加原型流程设计数据的安全恢复机制：版本页可只读扫描当前流程遗留的工作副本和版本备份，展示可恢复表单字段与流程节点；选中候选后创建新的未发布恢复版本，保留当前异常版本及发布状态。 |
| 1.42 | 2026-08-19 | 修正复杂固定审批流程发起页的“流程处理”摘要：按发布版本实际连线生成有序阶段，同层节点标记并行，区分审批/确认并标注条件执行；人员选择区同步使用拓扑顺序，不再按节点数组顺序简单拼接。 |
| 1.41 | 2026-08-19 | 简化表单设计器：新增字段提示文字默认空，画布内可直接编辑字段标题、选择项、必填和文本多行显示，删除最后一条字段显示条件时自动关闭条件；新流程节点说明默认空；首次发布后复核发布指针并返回版本记录。统一流程权限组的稳定标识与名称展示，发起中心、发起页、流程设计、发布预览和版本详情不再暴露内部编码。 |
| 1.40 | 2026-08-19 | 固化流程标题查询规则：表单设计器中固定 `title` 字段的“作为查询条件”始终开启且不可修改，历史快照与复制版本自动恢复该值；流程清单使用基础关键词查询标题，并从高级查询字段中排除标题，避免重复条件。 |
| 1.39 | 2026-08-19 | 统一运行时表单只读模式：不可编辑不再使用 Ant Design `disabled` 或统一转换为文本框；文本字段使用正常对比度的 `readOnly`，下拉、级联、单选、复选及表格选择列保留原控件形态并由统一组件阻止交互，所有只读控件采用白色背景和清晰文字。 |
| 1.38 | 2026-08-19 | 隐藏条件不满足而跳过的审核节点：后台继续保存“已跳过”状态用于拓扑推进和 AND 汇聚，但不创建待办、邮件或业务流转记录，流程进度、详情、审批信息、实例监控、打印及导出均不显示该节点；全部节点跳过时进度仅显示开始和结束。 |
| 1.37 | 2026-08-19 | 确认 Excel 转 PDF 使用 LibreOffice Headless；页数配置改为默认 `1` 的“最大页数”，通过 `calc_pdf_Export` 的 `PageRange=1-N` 直接生成所需页面，不生成完整 PDF；原始 Excel 仅临时用于转换，成功或失败后均删除，转换成功前禁止提交表单。 |
| 1.36 | 2026-08-19 | 流程详情和审核表单的只读选择字段恢复原控件形态：单选、复选、下拉、多级下拉及表格选择列保持选项结构，通过高对比度不可操作样式呈现，不再统一替换为文本框。 |
| 1.35 | 2026-08-19 | 附件控件增加 Excel 转 PDF 内嵌预览与“全部页/前 N 页”配置；明确保留原始 Excel、派生 PDF、异步转换状态与安全限制，并将 LibreOffice Headless 与 Aspose.Cells 的转换引擎选择列为待确认事项。 |
| 1.34 | 2026-08-19 | 修复驳回待处理流程未进入实际创建人“我的待办”的问题，增加“待我重新提交”任务标识与入口；压缩 PDF 预览外框和工具栏；表单字段新增可版本化的多规则条件显示，贯通设计预览、流程创建、详情、审核、领域清洗与发布校验。 |
| 1.33 | 2026-08-19 | 完成流程定义与运行实例全生命周期测试并修复两个问题：流程定义创建/复制改用防碰撞 UUID；重新提交独立保存轮次、提交人、提交时间和修改字段，详情、打印及审计记录不再把首次创建错误改写为后续轮次发起。新增覆盖创建、复制、发布切换、删除保护、发起、审核、驳回、重新提交、条件跳过、确认、关闭及自由协作的集成测试。 |
| 1.32 | 2026-08-19 | 合并打印审核与字段修改信息：删除独立的“审核修改记录”章节，将同一审核节点涉及的修改字段名称去重后并入“处理说明”，不再重复展示节点、处理人和时间。 |
| 1.31 | 2026-08-19 | 精简打印审批信息：审批进度与审核信息只显示人员已经实际提交结果的节点；未处理、已取消或按条件跳过的节点不打印，没有实际审核记录时隐藏整个章节。 |
| 1.30 | 2026-08-19 | 精简流程打印页眉：移除固定的“流程审核记录”文字和重复副标题，只保留流程实例标题作为唯一主标题，实例编号仍独立显示。 |
| 1.29 | 2026-08-19 | 重构已完成实例的“复制新建”：点击后只将兼容的最终表单内容带入当前发布版本发起编辑页，不再立即创建实例、编号和待办；附件及审批信息不复制，用户修改后点击“提交”才正式生成新流程。 |
| 1.28 | 2026-08-19 | 精简流程打印页面：全部章节标题移除中文数字编号，并删除“节点处理与通知配置”配置表；保留实际审批/确认结果、处理人、时间和意见等审核记录。 |
| 1.27 | 2026-08-19 | 为流程详情与审核表单的可编辑附件字段增加删除操作：每个附件可经确认后删除并同步移除实例引用；只读、锁定或未获字段修改权限时不显示删除入口。 |
| 1.26 | 2026-08-19 | 优化流程详情与审核表单的只读显示：取消下拉、级联、单选和复选字段的灰化禁用态，统一转换为高对比度只读输入外观；审核节点获准修改的字段仍保留原类型编辑控件。 |
| 1.25 | 2026-08-19 | 收紧流程实例发起内容编辑权：普通用户中只有实例实际创建人可以在首个审核结果前保存修改或在驳回后重新提交；同一发起权限组成员不继承编辑权。超级管理员保留修改和重新提交任意实例的系统级权限，并在界面、运行时命令、附件接口和模拟后端中统一校验。 |
| 1.24 | 2026-08-19 | 修复 PDF 上传后无法显示：发起页保存真实文件内容和字段附件标识，实例创建时建立附件关联，详情与审核页通过受控内容接口加载真实 PDF，并保留旧文件名数据的兼容预览。 |
| 1.23 | 2026-08-19 | 统一轮次显示规则：首轮不显示轮次文本，从第二轮开始显示“第 N 轮”；任务中心、流程清单、详情、流转记录和打印内容使用同一格式化规则。 |
| 1.22 | 2026-08-19 | 确认全站视觉与交互统一改造：建立共享色彩、间距、卡片、查询区、表格、页签、抽屉、状态和反馈规范；普通业务页保持简洁企业后台风格，表单与流程设计器保留全高三栏但统一工具栏和面板语言；详细规范见 `document/UI_DESIGN_GUIDELINES.md`。 |
| 1.21 | 2026-08-19 | 调整流程清单入口与版本口径：没有发布版本时隐藏流程清单入口，即使存在历史实例也不展示；存在发布版本时列表包含该流程全部版本实例，列配置只读取当前发布版本，历史实例缺失字段显示为空。 |
| 1.20 | 2026-08-18 | 统一运行时表单必填标识：详情和审核页不显示必填/可选信息；创建页及发起预览仅对必填字段显示红色星号，不再显示文字标记。 |
| 1.19 | 2026-08-18 | 流程详情与审核页的动态表单改为 Ant Design 标准纵向 Form：普通字段双列，富文本、附件和表格整行展示，并统一只读态、编辑态、字段说明与必填标识。 |
| 1.18 | 2026-08-18 | 允许流程创建后在首个审核结果产生前重新选择已配置的默认审核人员；保存时校验流程权限组实时成员并同步更新当前轮待办，已有审核结果后保持锁定。 |
| 1.17 | 2026-08-18 | 任务中心新增“我的发起”，集中展示本人发起且未完成或关闭的流程，复用流程分类和动态列表列，并采用只读查看操作。 |
| 1.16 | 2026-08-18 | 重做流程详情中的动态表单为统一网格表单，按字段类型优化只读和编辑展示；恢复附件字段原位 PDF 预览，并统一历史附件配置缺少 `inlinePdf` 时的默认开启规则。 |
| 1.15 | 2026-08-18 | 统一改造任务中心和流程清单的实例列表：使用清晰的标准数据表、完整网格与统一行态，并按文本、选项、附件、表格和空值分别提供可读的字段展示。 |
| 1.14 | 2026-08-18 | 修复新建流程时动态附件字段无法正常选择文件：统一附件组件与表单之间的 `value/onChange` 数据契约，避免文件列表被空值覆盖。 |
| 1.13 | 2026-08-18 | 优化条件规则编辑器的长字段名显示：字段选择独占整行，判断操作放在第二行，并加宽字段下拉及完整名称提示。 |
| 1.12 | 2026-08-18 | 修复带单选或下拉字段的流程开启“按条件执行”时崩溃：条件字段业务选项不再使用 Ant Design Select 的分组保留属性 `options`。 |
| 1.11 | 2026-08-18 | 精简“预览、校验与发布”页面：初始表单字段只显示字段名称，移除字段后的“Excel 导出”标记及版本概要中的导出字段数量，实际导出配置保持不变。 |
| 1.10 | 2026-08-18 | 流程设计器在没有审核人可输入字段时隐藏整段字段配置；规范化历史条件快照并修复开启“按条件执行”可能导致的崩溃；额外通知用户采用姓名短标签、折叠人数和可滚动姓名邮箱清单。 |
| 1.9 | 2026-08-18 | 表单预览页将“模拟提交”改为“校验表单”，执行发起人视角的必填项校验，并明确提示预览不会创建流程实例。 |
| 1.8 | 2026-08-18 | 初始表单将“填写阶段 + 允许审核人修改”合并为“输入权限”，支持发起人、发起人/审核人、审核人三种选择；标题默认字段说明改为空；调整新建版本的系统字段默认显示项；流程设计器右侧改为“节点属性 / 流程属性”，将流程属性置于后侧。 |
| 1.7 | 2026-08-18 | 统一流程权限组用途为“发起/审批\/受理/关闭”，新增独立关闭权限组并迁移既有发起组的关闭资格；任务中心改为全部待办仅公共列、单流程公共列加该流程全部自定义列的标准表格；基本信息和初始表单隐藏校验状态，仅流程设计和发布页展示。 |
| 1.6 | 2026-08-18 | 修正新建流程进入基本信息页时被未保存离开保护误拦截：点击“继续填写基本信息”直接进入向导并保留弹窗已填内容，不再显示无意义的离开确认。 |
| 1.5 | 2026-08-18 | 确认正式后端实施边界：NestJS 作为 IIS 后的单实例 Windows 服务运行，采用 HTTP 同源会话；首版同时实现 SQLite/Drizzle 与 SQL Server 2016 SP2/node-mssql 适配、双方言迁移和停机数据迁移；动态查询使用类型化投影；附件采用本地目录暂存引用和延迟清理；确定会话、后台任务、人工停机备份、永久业务数据保留及不保存字段旧值/新值和回复旧正文。 |
| 1.4 | 2026-08-18 | 明确 Excel 由浏览器使用 ExcelJS 生成真正的 `.xlsx`；后端只返回经过页面权限、流程数据范围及完整查询条件筛选的数据集，不生成、不保存 Excel 文件或下载地址；单次导出上限为 10000 行，历史实例缺少当前版本字段时输出空单元格。 |
| 1.3 | 2026-08-18 | 增加无后端开发阶段的前端 Mock REST API：使用 MSW 拦截 `/api/v1`，提供类型化客户端、OpenAPI 3.1 契约、分页与统一错误、ETag 乐观锁、写命令幂等、权限/数据范围校验、附件与邮件 Outbox、导出数据接口及确定性故障场景，并明确正式 NestJS 迁移边界。 |
| 1.2 | 2026-08-18 | 增加 Excel 导出字段与当前查询全量导出、不可为空的默认 30 天查询范围；审批节点新增审批/确认处理方式及结果提交后的重复字段修改与差异留痕；增加可选的审批/结束节点邮件通知和额外收件人；启用 PDF 展示的附件字段改为单文件替换。 |
| 1.1 | 2026-08-18 | 增加审批节点执行条件和审核人填写字段：节点按最新表单值判断执行或跳过，跳过参与 AND 汇聚并留痕；支持 AND/OR 与常用比较操作符；发起页面隐藏审核人字段，详情按原布局显示空值或默认值，授权节点填写并校验必填，审核人整表允许维护行结构。 |
| 1.0 | 2026-08-17 | 增加审批节点邮件通知：新节点待办激活后由 NestJS 后端使用 `@nestjs-modules/mailer` 异步发送；明确收件人、去重、Outbox、重试、审计、安全内容及内网 SMTP 配置规则。 |
| 0.99 | 2026-08-17 | 精简流程权限组列表，移除“运行待办”列，相关数量仅在停用确认等必要的操作影响提示中使用。 |
| 0.98 | 2026-08-17 | 修正流程权限组列表状态列宽度与对齐方式，确保状态标签的完整圆角边框不再被单元格裁切。 |
| 0.97 | 2026-08-17 | 精简操作审计日志页：移除今日操作、失败操作、敏感变更、日志保留等概览统计卡片，页面直接进入查询与日志列表。 |
| 0.96 | 2026-08-17 | 流程配置向导的上一步、下一步增加按需自动保存：有修改时先保存并自动校验，保存失败则阻止跳转；完整校验未通过仍允许进入后续步骤处理。 |
| 0.95 | 2026-08-17 | 优化“全部待办”混合列表：流程未配置任何任务中心业务字段时，完全隐藏该任务的空白“流程信息”行和容器。 |
| 0.94 | 2026-08-17 | 将流程标题的“在任务中心显示 / 在流程清单显示”开关从系统字段迁回固定 `title` 字段属性；任务中心和流程清单直接读取该字段配置，并兼容迁移旧版本中的系统标题开关。 |
| 0.93 | 2026-08-17 | 统一流程操作完成后的导航：新实例创建成功后返回流程发起中心；审核人提交通过或驳回后返回任务中心，并绕过已经成功提交内容的未保存离开拦截。 |
| 0.92 | 2026-08-17 | 统一附件字段运行时布局：创建、详情和审核页面按表单设计顺序原位渲染附件控件，附件名称、下载和上传均保留在字段内部，不再生成独立附件卡片；同时精简流程表单卡片标题，移除“动态表单”和硬编码可修改数量。 |
| 0.91 | 2026-08-17 | 精简附件显示规则：审核页首先按实例锁定版本是否设计附件控件决定是否存在附件区域；未设计时完全隐藏，已设计但当前为空时只在发起内容可编辑或审核节点获准修改附件字段时保留上传入口。 |
| 0.90 | 2026-08-17 | 统一流程实例标题：取消初始表单的独立表单名称；所有表单固定包含不可删除、类型与必填规则锁定的 `title` 文本框，其业务属性仍可编辑；发起时填写值统一同步为实例标题，并避免在任务中心和流程清单中形成重复动态列。 |
| 0.89 | 2026-08-17 | 简化流程发起提交：主按钮统一改为“提交”，确认框只询问是否确认提交；增加统一未保存离开保护，覆盖流程发起、流程配置向导、设计器、实例内容、自由协作及用户与权限治理编辑页面，并支持浏览器刷新/关闭提示。 |
| 0.88 | 2026-08-17 | 完善流程版本只读查看：版本详情增加概览、初始表单和审批流程/协作规则页签，直接读取版本完整快照，展示字段属性、表格列、系统列表字段、实际节点连线、节点权限和驳回规则；发布版本及已有实例版本可完整查看但不可修改。 |
| 0.87 | 2026-08-17 | 收敛流程管理概览：移除流程定义列表中的实例数和直接编辑入口，每条流程默认通过“查看”进入版本记录，再根据具体版本状态执行复制新建、编辑、发布、取消发布或删除。 |
| 0.86 | 2026-08-17 | 重构流程定义与版本生命周期：彻底取消流程实例、流程定义和流程版本的草稿状态；流程实例改用“提交并发起/重新提交”；基本信息首次保存即生成正式 V1，保存时自动校验；版本统一显示“校验未通过/可发布/已发布”，一个流程最多一个或零个发布版本；任意版本可复制新建后续版本；发布版本必须先取消发布且实例数为0后才能编辑或删除。 |
| 0.85 | 2026-08-17 | 取消站内通知功能：移除顶部通知入口、未读数字、通知列表、已读状态、通知模板校验及运行时通知数据；保留任务中心“我的待办/可代办”数字提醒。 |
| 0.84 | 2026-08-17 | 根据代码审核 CR-001～CR-012 建立统一领域模型和运行时数据链路：共享用户/多角色/流程权限组主数据；草稿与发布版本保存完整快照；实例稳定关联定义与版本；拓扑解释器生成并激活任务；登录、菜单、路由和领域命令复用权限引擎；发起、审核、驳回、重新发布、关闭、列表、详情和打印使用同一运行时数据；补充本地原型 schema 迁移规则。 |
| 0.83 | 2026-08-14 | 明确并实现草稿删除：全新 V1 草稿删除时移除空流程及设计数据；已发布流程的新版本草稿删除后恢复当前生效版本且版本号不复用；撤回形成的同版本草稿删除时放弃修改并恢复撤回前快照和启停状态。 |
| 0.82 | 2026-08-14 | 修正新建流程误用 PDF 审核定义：新流程的配置草稿、表单、流程图、权限组和列表定义保持独立；发布后的通用发起页读取当前生效版本及其设计器快照；未知流程不再回退 PDF，并明确显示不可发起。 |
| 0.81 | 2026-08-14 | 统一流程配置向导导航：基本信息的保存和下一步操作移至顶部；初始表单、流程设计及发布页显示带目标步骤名称的上一步按钮；上下步按钮由公共组件统一文案、图标与样式。 |
| 0.80 | 2026-08-14 | 增加零实例生效版本撤回编辑：当前生效版本实例数为0且没有其他草稿时，可撤回为同编号草稿并暂停发起；重新发布不增加版本号，放弃修改恢复原发布快照，保留首次与最近发布时间，历史版本和已有实例版本仍不可修改。 |
| 0.79 | 2026-08-14 | 删除可视化流程设计器中的“恢复示例”按钮及其重置草稿逻辑，避免当前流程设计被演示拓扑覆盖。 |
| 0.78 | 2026-08-14 | 统一跨页面交互与视觉规范：页面级返回按钮收敛为同一文案、图标和样式；流程配置步骤改为共享定义并统一使用“初始表单”；流程实例、定义、版本、账号及审计结果使用同一状态胶囊与颜色语义；流程管理、版本记录、任务/流程清单和治理页面的表格图标操作统一尺寸、悬停反馈、提示与可访问名称。 |
| 0.77 | 2026-08-14 | 完善流程创建向导的数据贯通：发起流程权限组改为可搜索多选；初始表单移除恢复示例并使用当前流程名称；流程设计增加自动布局，审核可修改字段同步当前表单；步骤统一命名为“发布”，发布页读取实际表单、拓扑和规则；新发布流程动态进入发起中心。 |
| 0.76 | 2026-08-14 | 统一版本号为V1/V2整数流水；切换生效版本时同步使用该版本的流程名称更新菜单显示但保持稳定路由；删除唯一生效版本且仍有草稿时允许流程退回草稿态。并补充已有草稿换来源确认、旧实例继续使用原版本、跨版本复制字段映射、历史选项名称显示和不兼容字段类型处理规则。 |
| 0.75 | 2026-08-14 | 支持从任意历史版本创建后续版本：流程存在版本1、2、3时可基于版本1的完整快照创建版本4；派生版本不运行时依赖来源版本，无实例的版本2、3及来源版本均可独立删除；版本号使用不可回退的单调计数器，并通过事务和限制删除外键防止实例创建与版本删除竞态。 |
| 0.74 | 2026-08-14 | 重构流程定义与版本生命周期：每个版本改为可独立运行的完整配置快照；版本使用“草稿/生效/失效”状态并保证只有一个生效版本，发布或手工切换生效版本会自动使其他版本失效；流程总实例数为0时可删除流程，单版本实例数为0时可删除该版本，删除生效版本必须明确选择替代版本，版本号不重排或复用。 |
| 0.73 | 2026-08-14 | 明确流程定义与版本边界：全局新建/复制新建生成独立流程及菜单，流程内创建版本继续使用原菜单；编号前缀在基本信息中维护并允许随新版本改变，各发布版本保留前缀快照；跨版本流程清单统一使用当前发布版本的列和查询配置，旧实例缺少当前字段时显示为空。 |
| 0.72 | 2026-08-14 | 统一主要页签视觉：部门管理与任务中心共用同一套切换控件样式，任务中心取消“我的待办/可代办”两种不同选中色，统一为浅蓝主题选中态，保留任务数量徽标。 |
| 0.71 | 2026-08-14 | 增强部门管理页签的选中状态：当前页签使用主题色文字和图标、浅蓝背景、清晰边框、阴影及加粗字重，并为未选中项增加悬停反馈。 |
| 0.70 | 2026-08-14 | 增加全局职务字典：在部门管理页新增“职务管理”页签，支持职务新增、重命名、排序、启停和受引用保护的删除；用户新增/编辑动态读取启用职务，停用职务保留历史显示。 |
| 0.69 | 2026-08-14 | 精简用户编辑抽屉：编辑已有用户时移除密码和账号状态字段，分别统一由列表中的“重置密码”和启用/停用操作维护；新增用户仍保留初始密码和初始状态。 |
| 0.68 | 2026-08-14 | 改进角色成员维护：角色编辑页使用独立成员选择区，显示已选人数、完整姓名、部门和职务，支持搜索、全部/已选切换、批量选择、清空及滚动查看，避免成员较多时挤成不清晰的多选标签。 |
| 0.67 | 2026-08-14 | 隐藏用户编号：用户管理列表和查询区域不再展示或使用内部用户编号，保留登录账号与员工姓名作为管理员可见的识别和搜索信息。 |
| 0.66 | 2026-08-14 | 隐藏角色编码：角色管理列表和编辑抽屉、权限管理角色选择器不再显示内部编码；编码仍由后台生成并用于内部关联，角色名称改为全局唯一的主要用户标识。 |
| 0.65 | 2026-08-14 | 精简权限管理顶部工具栏：删除角色下拉框前重复的“当前配置角色”说明文字，移除低价值的权限矩阵搜索框，保留可搜索的角色切换以及保存状态和操作。 |
| 0.64 | 2026-08-14 | 简化角色动作权限：资源维护页面的新增/新建、编辑、启停/停用合并为“编辑”；发布、授权、重置密码、删除、导出等敏感动作保持独立，并自动迁移已有角色的旧动作权限。 |
| 0.63 | 2026-08-14 | 调整流程权限组适用范围配置：用途改为“发起/审批/自由流程受理”多选；适用流程不再手工维护，改为根据流程设计中的实际引用自动统计，并支持一个权限组关联多个流程或暂未关联。 |
| 0.62 | 2026-08-14 | 优化流程权限组编辑页的有效成员预览：取消含义不清的单字头像，直接显示完整姓名、有效状态和“直接加入/角色带入”来源；增加姓名搜索与独立滚动列表。 |
| 0.61 | 2026-08-14 | 扩展实例编号前缀字符范围，除英文字母和数字外增加横线 `-` 与下划线 `_`，编号生成时保留配置的前缀原样。 |
| 0.60 | 2026-08-14 | 统一流程实例编号规则：流程设计时必填编号前缀，实例编号按“前缀 + YYMM + 四位月序号”生成；相同前缀跨固定审批和自由流程共享月度序列，复制新建取新号，重新发布和重新打开沿用原号。 |
| 0.59 | 2026-08-14 | 优化任务中心流程分类：移除标题后的流程种类总数，放大分类名称、类型说明、图标和待办数量徽标，并调整侧栏宽度与条目间距以提高可读性。 |
| 0.58 | 2026-08-14 | 调整复制新建与自由流程详情：复制已完成流程时固定不复制原附件并删除附件复制选项，创建后由发布方重新上传；自由流程详情移除“打印：不支持 PDF 打印”的冗余信息项。 |
| 0.57 | 2026-08-14 | 任务中心增加两级分类：顶部保留“我的待办/可代办”，左侧按流程显示待办数量并提供“全部待办”；具体流程使用专属列表字段，混合视图保留流程信息带；支持流程分类搜索、记忆最后选择及 20/50/100 条分页。 |
| 0.56 | 2026-08-14 | 精简流程发起中心：删除重复的当前用户信息、查询栏和“仅看我可发起”；无发起权限的流程直接隐藏；流程入口改为四列优先的紧凑卡片，并移除“当前账号可发起”和更新时间等冗余内容。 |
| 0.55 | 2026-08-14 | 增加系统内置超级管理员账号与演示身份：始终拥有全部系统和流程权限，可发起、审核及管理全部流程；账号、角色和权限只读不可修改；不加入或显示在流程权限组、审批人和受理人候选名单中，但实际操作仍完整留痕。 |
| 0.54 | 2026-08-13 | 重整流程定义配置链路：新建弹窗数据直接进入基本信息；状态改为生命周期自动计算；固定审批使用“基本信息→初始表单→流程设计→发布校验”，自由协作使用“基本信息→初始表单→发布校验”；设计器移出左侧菜单并绑定当前流程草稿；统一在发布校验页发布；已发布流程编辑时创建或复用下一版本草稿，版本页支持从历史版本复制创建草稿。 |
| 0.53 | 2026-08-13 | 完善权限管理交互：角色下拉框支持搜索并显示编码/状态；增加权限修改脏状态、角色切换和离开页面三选确认、刷新关闭提醒；新增“流程发起”的查看/发起权限，并与具体流程发起权限组共同控制菜单、发起中心及直链访问。 |
| 0.52 | 2026-08-13 | 移除角色编辑中的重复“权限范围”配置；权限管理页改为唯一权限来源，角色列表增加“配置权限”入口，按角色保存独立矩阵并同步页面/动作权限统计，新角色默认无权限。 |
| 0.51 | 2026-08-13 | 角色编码改为系统自动生成：新增角色时只读展示预计编码，保存时生成唯一编码；已有角色编码保持只读且创建后不可修改。 |
| 0.50 | 2026-08-13 | 修正表单设计器组件库显示不全：组件列表改为独立纵向滚动，标题和底部提示保持可见，并移除设计器容器在较矮屏幕下导致整体裁切的最小高度限制。 |
| 0.49 | 2026-08-13 | 统一全部页面查询区的垂直留白：标准查询卡底部间距调整为24px，任务中心及角色/权限等列表内嵌查询栏调整为20px，换行查询项行距统一为至少16px。 |
| 0.48 | 2026-08-13 | 补齐剩余13个核心原型页面：流程发起中心与发起页、流程管理/基本配置/发布校验/版本记录、用户/部门/角色/权限/流程权限组、只读实例监控和操作审计；完成身份化菜单、完整路由及直链权限边界，当前原型已覆盖全部19个核心页面。 |
| 0.47 | 2026-08-13 | 完成全页面视觉一致性复查：统一业务页、自由流程和两个设计器的页头层级、容器宽度、间距、圆角、阴影及桌面适配；移除自由流程创建页重复标题，并明确登录页和打印页的场景化例外。 |
| 0.46 | 2026-08-13 | 调整表单设计器中的系统列表字段入口：移出顶部操作按钮区，改为右侧属性面板的“字段属性 / 系统字段”双页签，系统字段直接在侧栏内配置。 |
| 0.45 | 2026-08-13 | 强化任务中心混合流程列表的记录分隔：主任务行与流程信息行组成统一视觉块，通过连续左侧标识、清晰边界和记录间留白区分相邻任务。 |
| 0.44 | 2026-08-13 | 系统自动生成的实例编号、流程名称、版本、状态、当前节点、轮次、发起人和时间等信息增加任务中心/流程清单独立显示开关；配置跟随流程版本保存，表单设计器增加统一的系统列表字段入口。 |
| 0.43 | 2026-08-13 | 用户增加所属部门和职务；部门为最多两级且用户可直接归属一级节点，职务固定为“经理/员工”，并明确部门、职务与多角色权限相互独立。 |
| 0.42 | 2026-08-13 | 用户改为支持多角色；流程权限组可关联多个角色，角色中的有效用户动态加入组内，与直接成员取并集并去重，角色及成员变化立即影响运行中资格。 |
| 0.41 | 2026-08-13 | 对全部原型页面执行统一视觉升级：强化导航、顶栏、卡片、查询区、表格、表单、详情页及两个设计器的层级、对比度和交互反馈，不改变业务结构。 |
| 0.40 | 2026-08-13 | 修正“流转人员项”的范围：压缩的是协作时间线中的流转事件，统一为单行记录；恢复右侧“当前责任 / 参与人员”分区，富文本回复仍保留卡片。 |
| 0.39 | 2026-08-13 | 压缩自由流程详情页的流转人员信息：当前受理人与历史参与人合并为单行展示，超长名单省略并支持悬停查看。 |
| 0.38 | 2026-08-13 | 简化自由流程清单的“当前节点”列：进行中仅显示当前受理人姓名，已关闭时不显示内容。 |
| 0.37 | 2026-08-13 | 明确自由协作是可重复创建的流程定义模式而非固定单例；所有流程共用表单设计器，自由流程跳过流程设计器，并按不同流程定义名称在流程清单下分别生成菜单。当前原型无需因此重做。 |
| 0.36 | 2026-08-13 | 增加独立自由协作流程：结构化初始表单、Tiptap 富文本、多次回复、单一当前受理人逐人转交、关闭与填写理由重新打开、统一时间线、发起人编辑初始表单、回复版本历史、异常改派，并明确不提供 PDF 打印。 |
| 0.35 | 2026-08-13 | 调整流程设计器信息层级：顶部只保留流程操作，右侧属性面板增加“流程属性 / 节点属性”切换；点击节点或画布时自动切换对应属性。 |
| 0.34 | 2026-08-13 | 增加唯一的流程级可配置规则“驳回后的处理方式”：重新发布或关闭、仅允许重新发布、自动关闭流程；其他审核行为继续采用固定规则。 |
| 0.33 | 2026-08-13 | 取消独立“并行分支”节点；同一节点的多条出线自动表示并行，多条入线自动表示 AND 汇聚，并同步调整拓扑及并行字段冲突的发布校验。 |
| 0.32 | 2026-08-13 | 修正打印实现：不再裁剪详情页，改为独立打印预览页面，用纯文本和标准表格完整输出流程表单、附件名称、审批信息、修改记录及流转历史。 |
| 0.31 | 2026-08-12 | 流程清单和详情页增加“打印为 PDF”；打印流程摘要、表单、审批进度和记录，附件仅显示名称及元数据，不打印附件正文或 PDF 预览页面。 |
| 0.30 | 2026-08-12 | 流程清单为已完成实例增加“复制新建”；复制最终表单内容并可选附件，生成新编号和第1轮待办，不复制审批记录与历史，并受流程发布权限控制。 |
| 0.29 | 2026-08-12 | 修正发布内容锁定时点：本轮尚无人提交审核结果前，发布方可自由修改并保存且不创建新轮次；首个审核结果提交后立即锁定，驳回待处理时再重新开放编辑。 |
| 0.28 | 2026-08-12 | 将驳回后的通用操作明确为流程实例“重新发布”，不再依赖重新上传文件；审核开始后发布内容锁定，只有驳回待处理时发布方可修改表单并按需更换附件，重新发布后全部分支进入新一轮审核。 |
| 0.27 | 2026-08-12 | 流程清单操作列改为与任务中心相同视觉规格的查看图标按钮，并增加悬停提示与无障碍名称。 |
| 0.26 | 2026-08-12 | 混合任务列表由拥挤的“关键信息”列改为双层任务行；动态字段在横跨整行的信息带中显示最多6项，支持长文本提示和剩余字段展开。 |
| 0.25 | 2026-08-12 | 强化任务中心 Tab 选中状态：我的待办使用实心蓝色、可代办使用实心紫色，并增加白色文字、高对比计数和阴影。 |
| 0.24 | 2026-08-12 | 确认任务中心支持流程级动态字段：混合流程使用“关键信息”列显示最多3项，选择单个流程后展开最多6列；表单设计器增加任务列表显示名称、顺序和列宽配置。 |
| 0.23 | 2026-08-12 | 任务中心移除时限字段；操作列改为带悬停提示和无障碍名称的统一审核图标按钮。 |
| 0.22 | 2026-08-12 | 将“所有流程”统一更名为“流程清单”，并要求带子菜单的父菜单图标与其他同级菜单保持相同的起始位置、尺寸和间距。 |
| 0.21 | 2026-08-12 | 统一流程列表状态视觉：四种状态均采用圆点、文字、浅背景和细边框组成的同规格胶囊标签，仅使用语义颜色区分。 |
| 0.20 | 2026-08-12 | “所有流程”调整为按流程定义生成子菜单；不同版本合并为一个入口，不提供“全部流程”；各子菜单复用公用实例列表页面并固定流程范围，页面移除流程名称查询框。 |
| 0.19 | 2026-08-12 | 精简“所有流程”布局：移除内容区重复标题、说明文字与当前数据范围摘要，保留顶栏标题并让查询面板直接成为页面主体。 |
| 0.18 | 2026-08-12 | 精简任务中心布局：移除内容区重复标题、用户身份信息、待办摘要卡与临时代办说明卡，保留顶栏标题及列表上方的“我的待办 / 可代办”Tab。 |
| 0.17 | 2026-08-11 | 确认表格列支持必填、默认值、列宽和对齐方式；确认第一轮原型在刷新后保留演示身份、流程操作结果及设计器草稿，并提供重置演示数据入口。 |
| 0.16 | 2026-08-11 | 将审核组统一升级为流程权限组；确认发起与关闭权限、并行驳回与重审、版本规则、表格行操作、附件限制、自动编号、站内通知、最低密码要求，以及不提供归档和备份。 |
| 0.15 | 2026-08-11 | 确认各审批节点分别选择可修改字段；禁止并行节点配置重叠字段，并在流程发布校验中阻止冲突配置。 |
| 0.14 | 2026-08-11 | 确认采用 React + Ant Design 直接制作可交互原型，并分两轮实施；第一轮先完成6个代表性页面。 |
| 0.13 | 2026-08-11 | 增加任意附件上传、PDF内嵌展示配置、表格单选/复选/下拉列，以及普通字段和表格列允许审核人修改并随审核结果提交的要求。 |
| 0.12 | 2026-08-11 | 确认流程查看采用角色页面权限与流程数据范围两层授权；增加额外可见角色/用户、系统级全量查看权限，以及动态表单字段的列表展示和查询开关。 |
| 0.11 | 2026-08-11 | 确认18个核心页面；取消工作台与“我的发起”；任务中心仅保留“我的待办/可代办”；增加“所有流程”；确认实例监控只读及本地账号密码登录。 |
| 0.10 | 2026-08-11 | 明确用户规模为 200+；增加用户列表、成员维护、人员选择、服务端分页、搜索、批量操作及数据库索引等界面和接口要求。 |
| 0.9 | 2026-08-11 | 确认代办审核不需要填写额外的代办原因，仍保留实际处理人与代办标记等审计信息。 |
| 0.8 | 2026-08-11 | 明确发起时指定人员仅作为默认责任人；增加审核组待办/可代办视图、临时代办权限、代办审计字段及并发处理规则。 |
| 0.7 | 2026-08-11 | 确认审核组任意一人处理即完成节点、成员变化立即影响运行中待办、引用后不可删除，以及停用不影响已发布和运行中流程。 |
| 0.6 | 2026-08-11 | 增加独立审核组概念；明确审核组与角色权限的边界、节点引用方式、成员维护、指定具体审批人及审核记录要求。 |
| 0.5 | 2026-08-11 | 增加未来支持 Microsoft SQL Server 的兼容目标；明确 Drizzle 方言、数据访问层、迁移脚本及数据迁移约束。 |
| 0.4 | 2026-08-07 | 确认公司内网部署；锁定 NestJS、SQLite、Drizzle 与本地 PDF 文件存储方案；补充 SQLite 单实例及内网部署原则。 |
| 0.3 | 2026-08-07 | 确认首版数据库使用 SQLite；补充 SQLite 数据范围、文件保存方式及 Sites 部署兼容性说明。 |
| 0.2 | 2026-08-07 | 补充后端方案状态、Sites/Cloudflare 推荐方案、内网私有化备选方案及部署环境待确认项。 |
| 0.1 | 2026-08-07 | 创建需求文档；记录可配置流程、可视化节点、动态表单、文档审核示例、流程状态、版本记录、角色审批规则、pnpm 与前端技术选型。 |
